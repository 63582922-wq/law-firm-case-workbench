# 深刻需求理解

## 1. 用户真正需要的不是法律聊天，而是可靠交付

律师现在的困难不是不会问 AI，而是案件资料进入律所后呈现出以下状态：

- 法院送达材料、借条、聊天记录、微信流水、银行流水混在不同文件夹；
- 同一页可能在多个年度账单或导出文件中重复；
- 昵称、真实姓名、银行卡、微信账户之间缺少可靠身份映射；
- 原告诉请、当事人承认的事实、真正争议事项没有清晰边界；
- 借款、付息、还本、退款和不明款项容易被混为一谈；
- 利率规则跨越不同时点，适用版本、过渡规则和计算方式容易出错；
- 答辩状、证据目录、计算表、授权材料之间容易出现姓名、金额、日期、币种或角色冲突；
- 文件夹中常出现多个“最终版、最新版、修订版”，临近提交时无法确认哪一份有效。

因此，产品价值不是“生成一篇看起来像律师写的答辩状”，而是建立一条受控生产线：

> 混乱材料 → 可核验事实 → 明确诉请与争点 → 适时法律依据 → 可复算金额 → 律师审批 → 一致文书 → 唯一提交包。

## 2. 产品定位

工作定位：**律师办案 Agent OS / 证据驱动诉讼工作台**。

首个验证场景仍以民间借贷案件为主，因为它能同时验证法院文书、聊天/流水证据、跨期法律规则、逐笔冲抵、Word/Excel/PDF 和法院提交包；但产品架构不能把“民间借贷一审被告”写死为全产品边界。案件代理地位、程序阶段、诉请、证据状态、法院事件、律师目标和当前有效法源共同决定动态办案计划。

这里的 Agent 不是一个分析按钮或聊天窗口，而是“目标 → 观察 → 动态规划 → 使用受控文件/图片/网络/命令 Skill → 记忆 → 重规划 → 独立校验 → 交付”的持续执行系统。完整架构以 `docs/LAWYER_AGENT_OS_ARCHITECTURE.md` 和 ADR-0027 为准。

目标用户：

- 3—20 人的诉讼团队；
- 承办律师、协办律师和律师助理；
- 经常办理民间借贷、欠款、买卖合同等金钱给付纠纷；
- 当前主要使用 WPS、PDF 编辑器、Excel、文件夹和通用 AI 工具手工协作。

首个用户角色：

| 角色 | 主要目标 | 关键权限 |
|---|---|---|
| 承办律师 | 确定案件立场并完成提交 | 批准事实、规则、计算和提交版 |
| 协办律师 | 复核争点、法律和文书 | 评论、提出修改、执行部分审批 |
| 律师助理 | 整理材料和台账 | 上传、校对、建立候选事实，不得锁定立场 |
| 合伙人/复核人 | 控制风险与质量 | 终审、查看审计和团队指标 |
| 律所管理员 | 管理账号、模板和数据政策 | 不默认获得全部案件内容访问权 |

## 3. 第一版业务范围

### 首个场景包含

- 首版操作界面、审批提示、审计字段说明和默认文书模板均以简体中文呈现；英文或其他语言只可作为后续明确立项的辅助显示，不能替代中文可用性验收；
- 民间借贷案件一审应诉作为首个商用验收样本，但领域模型和 Agent 总控不得固定为被告或一审；
- 单案原则上不超过 200 页材料、100 笔交易；
- 法院送达材料、起诉状及附件、借条、聊天记录、微信及银行流水；
- 人民币为默认币种，但每笔金额必须显式保存币种；
- 材料盘点、OCR、页级去重、相关页筛选、关键记录红框；
- 主体和别名映射、诉请、时间线、争点、交易台账；
- 按律师批准的法律规则进行利息及冲抵计算；
- 答辩状底稿、证据目录、利息核算说明、计算表和提交清单；
- 唯一提交版锁定、导出和完整审计。

### 暂不包含

- 面向当事人的自动法律意见；
- 全案由通用法律助手；
- 胜诉率或裁判结果预测；
- 自动承认诉请、签章、发邮件、立案或提交法院；
- 法院网站自动化抓取；
- 任何移动端查看、审批、锁定或导出（v1 仅桌面 Web）；
- 自研基础模型或第一阶段微调；
- 第一版即支持所有律所私有化部署和所有 OA 连接器；
- 将商业数据库、文章或普通搜索结果直接作为最终法源。

## 4. 核心用户承诺

对律师承诺的是：

1. 每一项关键事实都能找到原始证据位置。
2. 每一笔金额都能进入统一台账并进行对账。
3. 每一条法律规则都能看到官方来源和适用日期。
4. 每一个计算结果都能由独立程序复算。
5. 每一个 AI 结论都区分“候选、已确认、已锁定、已失效”。
6. 每一份提交材料都来自同一组已批准事实、规则和数字。
7. 最终目录只有一套可以提交的版本。

产品不承诺胜诉，也不把生成结果包装成律师已经认可的结论。

## 5. 核心产品原则

### 证据优先

聊天窗口不是首页。首页展示当前案件状态、下一项律师决定、程序期限、材料缺口和风险。

### AI 提议，人类决定

AI 可以提取、解释、建议和发现矛盾；律师决定身份映射、付款性质、法律路径、计算参数、诉讼立场和最终文字。

### 代码掌控流程

代码状态机管理步骤、前置条件、权限、审批、重试和版本失效。主 Agent 不能自行跳过任何审批门。

### 计算必须确定

金额、天数、利率、本息冲抵、分币舍入和合计必须由版本化代码执行；LLM 只提出并解释参数。

### 原件与派生件分离

原始文件保持不可变。相关页 PDF、红框 PDF、OCR 文本和法院提交包均为可追溯的派生件。

### 一次解析，多处复用

OCR、页码、人物和交易结果进入案件事实库，后续 Agent 不重复读取全套材料。

## 6. 完成的定义

产品“完成”不等于：

- 模型返回成功；
- 页面出现绿色状态；
- 构建测试通过；
- PDF 文件能够打开。

一个案件流程完成，必须同时满足：

- 材料范围经律师确认；
- 关键事实和诉请范围经律师确认；
- 法律版本和计算参数经律师批准；
- 数字通过独立复算；
- 跨文书一致性检查无阻断项；
- 提交包通过真实 PDF/DOCX/Excel 渲染检查；
- 律师明确执行“锁定提交版”；
- 审计记录可以完整回放。

### 2026-08-28 当前项目事实

此前 6 份混合材料的脱敏合成案只证明了受管规划、来源绑定和 DOCX/XLSX/PDF 生产链能够运行；其输出主要是案情审阅意见和付款台账，不能单独证明 Agent 已经具备律师需要的争点判断、举证责任分析、攻防策略和行动规划能力。原先把它定级为 `PROVEN_FOR_INTERNAL_REVIEW` 过宽，现已纠正。

本轮改用 88 页、11 个文件、47 条原始交易记录的全合成民间借贷金标案，验收对象改为“律师案件决策包”。恢复候选已经满足内容合同：5 项首要风险、8 项争点/举证责任矩阵、4 项对抗分析、2 套策略、6 个当事人问题、6 项行动、3 项必须由律师决定的事项和 5 段文书蓝图；8 类关键风险全部覆盖，证据和法源引用错误均为 0。四种金额情景全部由确定性工具生成并独立核对 341 个字段，差异为 0，模型没有正式金额、事实批准、策略批准、锁包或提交权限。

“新鲜模型调用到完整决策包”的端到端验收仍为 `FAIL`。2026-08-28 的独立运行 `fresh-v3-run-01` 已由 `qwen3-vl-plus` 正常返回完整响应，供应商回执确认 16,947 tokens、费用 0.055638 元、无重试；模型真实完成 8 个争点、4 组对抗分析、2 套策略、6 个补证问题和 3 个律师决定分析，并识别了材料提示注入。但硬控制器拦截了模型自行写入的正式利率百分比，律师视角复核另发现时效追问主体倒置、策略与情景自相矛盾及未经确认的时效结论。该运行永久保持 `BLOCKED_SINGLE_CALL_NO_RETRY`，不能因结构完整或模型正常结束改记为通过。

基于失败证据，[ADR-0044](decisions/ADR-0044-strict-lawyer-core-and-controlled-projection.md) 已把分析核心升级为 v4：律师包模型迁移到支持严格 JSON Schema 的 `qwen3.7-plus`；模型只负责证据强弱/缺口、攻防理由、策略执行条件/风险和登记决定理由。争点标题、举证责任、角色安全追问、主体与“已提出/仅预判”状态、策略情景、正式利率、金额、期限、行动和文书章节均由代码控制。旧 v3 输出明确拒收且原始失败证据不可覆盖。

v4 当前是 `IMPLEMENTED_OFFLINE_NOT_LIVE_ACCEPTED`：35 项聚焦测试通过；基于同一真实输入快照的不发网预检确认请求使用 `qwen3.7-plus`、`json_schema`、`strict=true` 且不发送 `max_tokens`，按官方最大输出和北京公开原价计算的最坏费用为 1.093024 元，低于固定 1.20 元上限。该结果只是调用合同与硬控制器证据，不是新鲜模型成功。

文书编译器已生成并逐页检查 8 页 A4 DOCX 与 8 页原生 PDF，中文字体、分页、候选标记、长日期/金额/ID 换行和律师决定中文标签均通过；该候选来自离线合同测试，不是 v4 真实 Agent 交付件。新的 v4 响应必须重新编译并重新验收。完整证据见 `docs/LAWYER_AGENT_ACCEPTANCE_2026-08-28.md`。

下一单一产品目标不是继续登录、调参或机械复跑，而是先闭合 v4 失败样本回归、受管 Worker 接线和文书渲染，再只执行一次新的独立真实调用。只有严格响应、语义硬门、零非法引用、确定性计算一致和 DOCX/PDF 逐页验收同时通过，才把合成案律师决策包能力定级为 `PROVEN`；之后仍需浏览器律师旅程、匿名真实案律师认可、跨租户、备份恢复和供应商治理才能进入商用门。

### 2026-09-05 当前项目事实更新

上述“v4 尚未新鲜调用”的历史判断已由 M2 的独立、单次受控运行部分更新：M2 从完整合成案件、
时效受限同律所法源恢复和一次 `qwen3.7-plus` 外发获得了正常结束、可认证的 11,506-byte 原始响应
与用量回执。历史 M2 状态机在旧解释器下仍为 `WAITING_INPUT`，因为模型自由文本中的四处差额字面量
不是直接来源事实；该历史状态、唯一外发和运行版本保持不变。

当前解释器仅在服务器可独立证明同字段、同币种、唯一来源差额时删除这些字面量，而不接受或补写任何
金额。它对 M2 封存响应形成了 32,570-byte 的 `NEEDS_LAWYER_REVIEW` 决策包候选。M2 的完整民法典
可见正文为 111,098 字符，文书阶段通过注册来源 `CN-CIVIL-CODE-680` 的确定性解析器重新验证标题、
通过日期和第六百七十九条/第六百八十条边界，产生 211 字符、带独立 SHA-256 的法条投影；不能对未知
来源截断或扩容。

一次只读 M2 文书回放已实际生成 3 页 A4 DOCX/PDF（DOCX
`39d4562c43e1b5829e9460bd0bec56024bec251a6db260db511079944d7a3a79`，PDF
`73b5cbaca55c00c28d07f7a983c8b9441edc9ad6f7aaf8cad418293fefc0a1b3`），并确认 0 新 Agent run、
0 模型调用、0 案件数据库/对象存储写入、1 次隔离渲染和封存状态未变。它证明真实模型到律师复核候选
的受控交付链，不证明持久化正常任务已 `SUCCEEDED`、律师已复核、用户已验收或系统已经商用放行。

同日恢复与权限审计进一步确认：M2 仍为 `WAITING_INPUT`，其持久化文书包、修订请求和当前可交付文书
均为 0；同律所但没有目标案件角色的临时活动主体对已独立核验候选的 Web 文书授权查询返回 0 行，完整
事务回滚后没有留下测试主体。Docker 磁盘异常后的 PostgreSQL 数据卷控制信息为干净关闭，恢复仅清除了
全零的数据目录/套接字运行锁，未清空或修改案件、对象或身份数据。

随后 M3 独立合成验收完成了唯一的一笔当前运行时 `qwen3.7-plus` 调用：响应在 71 秒内正常返回、私有归档
及供应商用量回执可认证，但模型在攻防残余风险中把两项来源金额相减后写成“可能的现金交付或其他形式
借款”。`5,000.00 CNY` 不是该字段内任一来源的原样事实，且这句话是实质性事实推断，不是 ADR-0077
允许删除的紧邻“差额/差异”展示字面量。严格解释器正确以未绑定数值拒收；M3 仍为 `WAITING_INPUT`，
没有决策包、文书或提交物，且不得创建 M4、重发或改写 M3。

[ADR-0080](decisions/ADR-0080-qualitative-model-projection-and-chinese-quantity-gate.md) 据此规定未来模型只读取
以来源编号绑定的定性量化标签，不接收原始金额、日期或其他可计算数值；全部自由文本只能作定性分析，
中文金额、日期、比例和期限也会在本地拒收。原始来源、金额台账、确定性计算和律师候选来源目录仍由服务器
保存与展示。修复后 70 项聚焦测试、M2 零外发封存回放和 API 健康检查均通过；Worker 仍停用，未产生第二次
模型调用。磁盘随后因 Docker 构建与恢复下降至约 358MB；在先将 ClamAV 切换到当前 API 镜像并实测健康后，
只移除了已停止、可重建的 Worker 容器和没有任何容器引用的旧 `local-v5` API 镜像，未触及命名卷或案件数据。
可用空间回升至约 864MB。随后核验 BuildKit 元数据，仅清理描述为本项目旧 API Dockerfile 层的 943.6MB
可重建私有缓存，保留仍被当前镜像共享的缓存；可用空间一度回升至约 1.5GiB，但仍低于稳态运行所需的 5 GiB 余量门。

完整状态以 `docs/LAWYER_AGENT_ACCEPTANCE_2026-08-28.md`、ADR-0077、ADR-0078、ADR-0080、ADR-0081 与 ADR-0082 为准。

### 2026-09-05 恢复候选与运行环境补充

ADR-0081 的 `0077` 迁移已应用。固定 M2 封存响应只写入过一次独立恢复候选
`fc351603-2f2f-5bc5-80ce-e8f0db43f773`：它是 `NEEDS_LAWYER_REVIEW`、`court_ready=false`，且与正常
`case_agent_artifacts`、Agent 事件和验证回执分离。只读核验显示 M2 仍为 `WAITING_INPUT`、事件版本 8、分析任务
仍为 `FAILED`、失败回执仍为 `LAWYER_ANALYSIS_OUTPUT_REJECTED`、外发数仍为 1；没有第二次模型调用或任何重试。

当前受管边缘 API 只有在客户端明确声明 `sealed-recovery-v1` 时才实际返回恢复候选及其 9 个律师审阅区块，并明确告知
“原 Agent 任务仍处于受控阻断状态；它不是任务成功、正式事实、法律结论、文书、终审成果或可提交法院的材料”。未声明
该能力的旧客户端实际只得到 1 份普通候选，避免旧界面把恢复件误标为正式成果；该声明不替代既有身份、案件角色或来源页
授权。这证明合成 loopback 身份下的安全读取路径，而不是真实 OIDC/MFA 浏览器旅程、律师认可或商用放行。最新前端恢复
提示源码已通过 ESLint，但可用磁盘仍远低于完整重建余量，无法安全进行前端镜像重建；两处 API 小型安全修复暂通过本地验收
Compose 的只读源码挂载生效，不是可发布镜像。ADR-0082 已让本机与正式 Web 管理员入口在任何 Compose 静态校验、停止消费者
或镜像构建前都先核验 5 GiB 宿主余量；本次审计时实时预检约为 2.3GiB，完整重建被硬阻断，且入口每次都会重新读取，不会触碰现有服务、案件数据或触发模型调用。
ADR-0083 随后将这类本机封存恢复的 Python 源码覆盖从规范 Compose 分离为显式、非发布覆盖层；管理入口既不会自动加载该层，
也会在预检/运行核验中拒绝或报告宿主源码覆盖。已有运行中的 API 在完成干净重建前仍只是局部验收环境。
清理可重建前端缓存后，Docker Desktop 后端进程仍在，但 Docker Unix socket 的两次 5 秒只读 ping 和本机管理 `verify` 的
30 秒状态读取均超时。当前只能报告 Docker 运行状态未知，不能沿用此前服务健康快照；本轮没有停止、重启或重建任何容器。
浏览器验证还须在用户明确选择的浏览器中完成。

Docker Desktop 异常恢复时，数据库数据卷和套接字卷只出现全零的瞬态锁文件。确认 PostgreSQL 已停止后，这些锁文件被
移动为 `.stale-20260905` 副本；数据库、对象存储和身份数据均未清空、重建或改写。此前 API、PostgreSQL、对象存储、
身份、前端和网关曾被验证健康；当前 Docker 控制面不可读，不能把该历史快照表述为当前状态，Agent Worker 仍按设计停用。

## 7. 产品设计需要解决的情绪问题

律师在临近期限时承受的是责任压力，而不仅是操作成本。界面需要让用户随时知道：

- 现在做到哪一步；
- 哪些信息已经确认；
- 哪些结论只是 AI 候选；
- 哪个问题正在阻止提交；
- 修改某个事实会影响哪些文书；
- 接下来唯一需要自己决定的是什么。

因此，视觉上应当专业、平静、克制、证据密集但不混乱，不能使用泛 AI 产品常见的紫色渐变、大面积聊天气泡和炫技动画。
