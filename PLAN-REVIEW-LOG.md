# 计划对抗审查记录

审查范围：`PLAN.md` 及其引用的项目基线文档。

开始时间：2026-08-03（Asia/Shanghai）。

最大轮数：5。

审查原则：只处理会影响产品可执行性、安全、法律工作流正确性、UI决策、阶段依赖和验收可信度的实质问题；审查会话只读，不允许修改项目文件。

## 工具状态

最初两次调用在 WebSocket 超时后、HTTPS 降级完成前被提前终止，因此未生成可读取结论。独立只读烟雾测试确认身份验证和模型调用有效，CLI 在 WebSocket 连续超时后会自动降级到 HTTPS 并成功返回。现已延长等待并恢复完整只读审查。

## 本地预审发现与处理

1. **里程碑口径冲突**：README 将 10—12 周称为内部 Alpha，但质量计划要求 Alpha 必须端到端通过。已改为“技术预览版”，端到端 Alpha 调整为 18—20 周。
2. **Skill 数量冲突**：PLAN Phase 0 写首批9个，知识库计划实际列出16个。已统一为16个。
3. **UI 门可能错误阻塞后端**：原文把5名律师测试放在任何 UI 编码之前。已明确三方向选择后才开始业务页面编码，5名律师测试作为 Phase 1 出口门。
4. **法律数据许可边界不足**：原文只写“接入”官方数据源。已补充只能使用官方允许 API、下载、人工导入或授权数据库，不得绕过访问控制和服务条款。
5. **法律知识责任人缺失**：团队配置未明确谁审核法源效力和规则更新。已增加法律知识编辑/执业律师角色。
6. **Git 忽略规则误伤合法资产**：全局忽略 PDF、DOCX、XLSX 会导致脱敏测试样本和文书模板无法版本化。已改为排除私密数据目录和生成目录，合法合成资产允许进入显式目录。

## Round 1 — Codex

审查线程：`019fc765-b568-71d2-91a2-6add14b61c62`

1. **阶段依赖互相矛盾。** `PLAN.md §3.4`允许技术基础并行，但 `PLAN.md §20`又要求 Phase 1 后才初始化技术基础；`DESIGN_BRIEF.md §7`还禁止提前初始化前端。
   修复：拆分“前置契约/安全研究”和“选定视觉方向后的实现”，明确并行边界与出口条件。

2. **租户隔离没有覆盖完整数据面。** `ARCHITECTURE.md §6–8`和 `SECURITY_AND_COMPLIANCE.md §4–5`列出层级，但没有定义 Firm/Workspace/Team 的实际边界、RLS 上下文、对象存储 URL、搜索索引、任务队列、缓存、审计和导出的隔离规则。
   修复：Phase 0 固化租户模型与授权矩阵，并对所有存储、后台任务、索引和导出路径实施负向测试。

3. **审批权限和审批证据不足以证明“律师决定”。** `CONTEXT.md §2`、`PLAN.md Phase 0/2`只要求有批准人，没有规定承办律师/协办律师/助理的具体审批矩阵、自审批、委托、离职回收、二次认证和锁定/导出双人控制。
   修复：建立由 Command 层强制执行的角色—动作矩阵，并将审批绑定到 actor、matter 版本、对象哈希和最终文本哈希。

4. **提交版状态机无法证明唯一、可撤销且不可继续导出。** `ARCHITECTURE.md §4`没有 `LOCKED/REVOKED/STALE/CURRENT` 等提交生命周期；`PLAN.md Phase 8`的“唯一指针”和“撤销锁定”只是文字要求。
   修复：定义不可变提交包生命周期，并用数据库部分唯一约束和原子事务保证每个案件只有一个 current bundle，失效/撤销包一律不可导出。

5. **异步任务存在旧版本结果回写风险。** `ARCHITECTURE.md §3.1/7`提到 `expected_version`、Outbox 和幂等，但没有规定任务完成时如何原子校验输入版本、幂等键作用域及外部请求未知状态。
   修复：每个 Run 保存输入快照和版本指纹，结果只能通过 CAS 应用；为所有外部请求建立持久请求账本，未知状态禁止自动重试。

6. **律师直接改文书会破坏来源链。** `PLAN.md §13`和 `UI_UX_DESIGN.md §5.11`允许自由编辑，但没有对修改后的事实、金额、法条和证明目的重新解析、校验和审批。
   修复：将编辑保存为版本化差异，重新运行来源、金额、一致性和批准校验，只有最终文本哈希重新批准后才能锁定。

7. **法律时态适用模型不够确定。** `KNOWLEDGE_AND_SKILLS.md §7–9`只有生效期间和过渡规则 ID，没有明确合同、付款、违约、起诉等不同事件日期的适用锚点、优先级和冲突处理。
   修复：建立类型化 `ApplicabilityRule` 和案件专属不可变 legal bundle，并用版本化案例覆盖多日期、地域和过渡规则。

8. **法源许可和快照保存缺少硬门禁。** `PLAN.md Phase 5`称“已授权”即可接入，但没有逐来源记录 API/服务条款、存储和再分发权限、商业数据库许可、地域限制及撤销处理。
   修复：建立法源许可登记表；缺少许可、授权范围或合规访问证据时禁止抓取、保存和进入知识库。

9. **外部模型/OCR/MCP 调用没有完整的事前授权状态。** `PLAN.md Phase 2/18`只有白名单、成本上限和预算要求；`SECURITY_AND_COMPLIANCE.md §8`也未把调用字段、地域、供应商、保留期和用户确认绑定到一次调用。
   修复：所有外部调用统一经过持久化 preflight，显示数据范围、供应商、地域、成本上限并取得明确确认，否则 fail closed。

10. **程序期限被当作抽取结果，而非确定性法律计算。** `PLAN.md Phase 4`和 `ARCHITECTURE.md §3.4`要求显示期限，但没有法院/地域、送达方式、节假日、起算终止、顺延和不确定性规则。
    修复：增加版本化期限规则引擎和来源，未完成律师确认前不得显示可依赖的剩余时间。

11. **“确定性计算”仍缺少关键规范。** `PLAN.md Phase 4/6`支持外币和复杂冲抵，但没有汇率来源与估值日、时区、日期包含性、同日证据精度、多借款分配和舍入阶段的规范。
    修复：Phase 0 要么明确将外币排除出 v1，要么先冻结完整公式/汇率/事件排序/舍入规范并建立金标准测试。

12. **证据召回率 99% 仍允许决定性页面静默漏掉。** `PLAN.md Phase 3`和 `QUALITY_AND_RELEASE.md §3`只要求低置信结果人工复核，没有要求所有排除页进入律师可审阅的完整清单。
    修复：模型只能做候选排序，必须生成全量页范围清单并由律师确认排除；关键证据采用零漏页门槛或双人盲审。

13. **审计日志与观测设计存在隐私及失效盲区。** `SECURITY_AND_COMPLIANCE.md §10`要求记录 prompt/output，同时又要求避免保存原始隐私内容；没有脱敏、隔离、WORM/篡改证明或审计写入失败时的 fail-closed 规则。
    修复：只记录结构化哈希和最小字段，原始证据单独加密隔离，并对审计不可写、越权和异常成本设置阻断与告警。

14. **恢复目标不可验收。** `QUALITY_AND_RELEASE.md §8`只说能恢复，未定义 RPO/RTO、数据库与对象存储一致性、任务恢复、孤儿文件和索引重建后的核对。
    修复：为原件、派生件、状态、审批和审计分别定义 RPO/RTO，并进行带 Manifest 对账和任务重放的完整恢复演练。

15. **Beta 里程碑和指标未操作化且时间不可信。** `PLAN.md §2/5/Phase 8`要求 2–4 周内完成 10–30 个真实案件、3 家律所复购、50% 节时和 2 小时人工，但没有样本招募、基线、分母、案件难度分层或安全错误统计。
    修复：把安全发布门与商业验证分开，预先定义试点 cohort、基线和最小样本，商业指标不得替代法律正确性和恢复门禁。

16. **移动审批范围直接违反当前非目标。** `CONTEXT.md §3`排除移动端完整办案，但 `UI_UX_DESIGN.md §4`允许第一版移动审批，`PLAN.md §15`又将其列为 P2。
    修复：v1 彻底排除移动审批，或在 Phase 0 明确扩大范围并增加设备绑定、阶梯认证、离线禁止和专项验收。

`VERDICT: REVISE`

### Builder response

接受 16 项意见并完成基线修订：

- 明确 Phase 0/1 只并行契约、安全研究和可丢弃验证，生产应用实现必须通过 Phase 1 出口；
- 将 `Firm` 固化为安全租户，补齐 RLS、对象 URL、索引、队列、缓存、审计、导出和恢复的全数据面隔离与负向测试；
- 增加角色—动作矩阵、委托/离职回收、再认证、职责分离，以及绑定 actor、权限快照、对象/文本哈希的审批证据；
- 增加不可变提交包生命周期、部分唯一约束、原子 current 指针和失效/撤销禁导出规则；
- 增加异步 Run 输入快照、版本指纹、CAS、幂等作用域与外部请求账本；
- 将律师文书编辑改为不可变差异、重新校验和最终文本哈希重新批准；
- 增加 `ApplicabilityRule`、案件 `CaseLegalBundle` 和合同/出借/付款/违约/起诉等适用锚点；
- 增加 `SourceLicense` 硬门、逐次外部调用 preflight 和未知提交恢复；
- 将程序期限纳入版本化确定性规则引擎，未确认时不展示可依赖倒计时；
- v1 正式计算仅支持人民币，并在 Phase 0 冻结时区、日期包含性、同日事件、多债务分配与舍入规范；
- 将 99% 召回率降为排序指标，强制全量页面处置 Manifest、零未审页和关键证据复核；
- 审计改为最小结构化哈希、受限原文存储、防篡改、fail closed 与异常告警；
- 为原件、状态、派生件和索引分别定义 RPO/RTO，并要求 Manifest 对账、任务重放和孤儿文件检查；
- 将 Phase 8 调整为 6—10 周，总周期调整为 28—32 周；安全门和 cohort/基线/错误分母与复购商业指标分离；
- v1 完全移除移动端查看和审批。

请在同一只读线程中复审修订后的 `PLAN.md` 及全部链接基线文件。

## Round 2 — Codex

上一轮 16 项状态：1、3、5–12、14、16 已解决；2、4、13、15 部分解决。仍有以下阻断项：

1. **租户作用域仍有 schema 矛盾。** `ARCHITECTURE.md §6.1`要求所有业务对象同时带 `firm_id` 和 `matter_id`，但 `Firm`、`User`、`AuthorityDocument`、`SourceLicense`、模板和知识库存在全局/律所级对象；没有定义三种作用域及其 RLS 规则。
   修复：明确 `global/firm/matter` 作用域、所有权和可见性，允许 `matter_id` 为空的对象必须有独立策略和负向测试。

2. **已导出提交包仍未定义失效路径。** `ARCHITECTURE.md §4`只定义 `LOCKED_CURRENT → STALE`，没有 `EXPORTED → STALE/REVOKED`；唯一约束也只限制 `LOCKED_CURRENT`，不能证明导出后仍只有一个 current bundle。
   修复：把 current/valid 指针独立于生命周期状态，补充导出后失效事务、`EXPORTED → STALE/REVOKED` 和对应回归测试。

3. **审计字段在架构与安全基线中冲突。** `ARCHITECTURE.md §10`要求保存完整“提示词”，而 `SECURITY_AND_COMPLIANCE.md §10`又规定默认不保存完整 prompt、OCR 或证据正文。
   修复：统一审计 Schema，默认只保存脱敏结构化字段和哈希，原始 prompt 仅在独立加密存储、审批和短保留策略下保存。

4. **Phase 0 工作量与时间仍不可信，且总周期算术不一致。** `PLAN.md §5–6`把 10 次访谈、3 次观察、3–5 个金标准人工标注、租户/审批/计算/期限/法源/恢复契约全部压进 1–2 周；各阶段合计约 24–30 周，却声明 28–32 周，未说明缓冲。
   修复：将 Phase 0 延长或拆分为硬门与后续研究，并显式加入可计算的缓冲周，使阶段总和与 README/PLAN 周期一致。

5. **Phase 1 律师测试门仍不可复现。** `PLAN.md §7`只规定“5 名律师、4 名通过、无需指导”，没有目标角色、统一合成案件、任务脚本、时间上限、独立完成定义、观察记录或失败后的处理。
   修复：Phase 0 冻结 Phase 1 可用性测试协议、合成数据、任务分母和判定规则，再执行 5 人测试。

6. **UI 状态枚举仍与架构状态不一致。** `UI_UX_DESIGN.md §2`称全系统统一使用七种状态，但架构还定义 `BLOCKED`、`FAILED_RETRYABLE`、`FAILED_MANUAL`、`STALE_RESULT`、`UNKNOWN_SUBMISSION`、`REVOKED` 和 `EXPORTED`；这会使原型把未知提交、阻断和撤销错误地压成“失败”或“失效”。
   修复：在 Phase 1 前定义并冻结 workflow、run、approval、artifact validity、external request 五套枚举及其 UI 映射。

`VERDICT: REVISE`

### Builder response

接受 6 项意见并完成第二轮修订：

- 领域对象新增 `GLOBAL/FIRM/MATTER` 三种作用域，定义所有权、`matter_id` 可空白名单、数据库约束、RLS 和负向测试；
- 提交包改为 lifecycle、validity、`Matter.current_submission_bundle_id` 三层模型，补充 `EXPORTED → STALE/REVOKED`、current 指针原子清空和端到端测试；
- 架构审计字段统一为 `prompt_id/version/hash`，普通审计不保存完整 prompt 或案件正文；
- Phase 0 调整为 4—6 周、Phase 8 调整为 6—8 周，各阶段合计 27—32 周，另列 2 周招募/专项复核/返工缓冲，总周期 29—34 周；
- Phase 0 新增可复现可用性测试协议，明确参与者、统一合成案件、9 项任务、45 分钟、6 项高风险任务、8/9 总任务、观察记录和失败重测；
- UI 状态拆分为案件流程、运行、审批、派生件有效性、提交包生命周期和外部请求六个状态域，并建立机器状态到文案/颜色/动作/阻断的一一映射。

请在同一只读线程进行第三轮复审。

## Round 3 — Codex

六项 Round 2 修订中，租户作用域、导出后失效/current 指针、总周期算术和六域 UI 状态表已解决；剩余三个阻断项：

1. **Prompt 审计字段仍有跨文档歧义。** `KNOWLEDGE_AND_SKILLS.md §2`仍要求“保存 Prompt”，而架构和安全基线只允许 `prompt_id/version/hash`，未明确前者不是完整提示词。
   修复：将 Skill 规范统一改为保存 `prompt_id/version/hash`，完整 prompt 只能进入受限、加密、短保留诊断存储。

2. **阶段中间里程碑仍与新阶段算术不一致。** `PLAN.md §5`按顺序计算，技术预览约为 13–16 周、端到端 Alpha 约为 21–24 周；README 仍写 2–4、10–12、18–20 周。
   修复：统一 README 与 PLAN 的累计周数，或显式给出允许的重叠阶段、依赖和日历计划。

3. **Phase 1 的高风险任务集合仍不可复现。** `PLAN.md §7`要求 6 项高风险任务全部正确，但 `DESIGN_BRIEF.md §原型任务`只列出 9 项任务，未标明哪 6 项、每项“正确”的观察证据，以及可点击原型如何证明无越权/误批/误导出。
   修复：在 Phase 0 协议中逐项标记六个高风险任务，定义可观察的通过证据，并使用带权限/状态模拟的可记录原型测试。

`VERDICT: REVISE`

### Builder response

接受三项意见并完成第三轮修订：

- Skill 规范统一为 `prompt_id/version/hash`，完整 prompt 仅允许进入独立加密、审批访问和短保留诊断存储；
- README 里程碑按 PLAN 累计周数改为：视觉方向 4—6 周、可点击原型 6—8 周、技术预览 13—16 周、内部 Alpha 21—24 周、Beta 29—34 周；
- `DESIGN_BRIEF.md` 将原型任务 4—9 固定为 HR-1 至 HR-6，逐项定义可观察通过证据、权限/状态模拟、日志要求和一票失败条件，并明确 Phase 1 只验证交互，运行时权限与事务保证留给后续技术验收；
- 同时将设计 Token 从旧的笼统 `status.*` 改为 run/approval/artifact/submission/request 分域命名，消除残余状态混用。

请在同一只读线程进行第四轮复审。

## Round 4 — Codex

第四轮只读复审逐项确认：

- Prompt 审计已在知识、架构和安全基线中统一为 ID/版本/哈希，原始 Prompt 仅限受控短期诊断存储；
- 阶段工作 27—32 周加 2 周缓冲等于 29—34 周，README 的 13—16、21—24、29—34 周累计节点一致；
- HR-1 至 HR-6 已固定并定义权限/状态模拟、日志、最终状态与一票失败条件，参与者、样本、时限、分母和重测规则跨文件一致；
- UI Token 已按运行、审批、派生件、提交包和外部请求分域，无残留 `status.*` 通用状态令牌；
- 未发现仍足以阻断进入 Phase 0/Phase 1 的材料问题。

`VERDICT: APPROVED`

审查在第 4 轮收敛，未使用第 5 轮。审查线程全程以 read-only sandbox 运行，未修改项目文件。
