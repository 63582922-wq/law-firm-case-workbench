# ADR-0103：未决事项与只读研判的调度边界

状态：Accepted（0092/0093已部署，实际研判运行中）

实际任务4af42bf5-11eb-55dd-9779-5ec650ffabf0已在14版合成案件创建，零调用。
0050把存在ACTIVE后续事项时的所有非控制Run停放，导致律师必须先处理争议才能分析争议。

保留健康控制游标、隔离、恢复意图和转移锁；仅对受控被告初步研判开放只读分析范围。
范围要求同案同律所、非失效/取消、无激活文书计划、结构化答辩目标、最多一次调用及120分。
没有图时仅可进入本地受控规划；有图时只允许context、analysis及编译器补充的本地
法源检索计划工具（plan_authoritative_rule_research，DENY网络，非外部检索）。
异常处置、重抽取、原件读取、文书执行不借此获得运行权限，异常控制归属不变。

数据库wake与运行时双次claim复核同一谓词，健康状态检查不能被该例外覆盖。
迁移拒绝修改未知形态的旧wake函数；不改变旧迁移文件，不直接把QUIET改READY。
已有停放任务应在规范事件触发或显式受控唤醒后继续，不新建/清零预算。

0092实施后须验证实际角色可见性、限定图拒绝、控制不健康拒绝以及已有任务继续。
尚不表示完整交付或商用验收。

真实Worker角色谓词检查：当前初步研判true、既有文书执行false。原任务通过正常Web暂停/
恢复记录唤醒，未直接写队列；2项本地任务完成后进入模型任务审批。固定合成审批已保存，
run到EXECUTING/event13，模型任务max_attempts=1/max_external_calls=1/max_cost=120。
原异常控制归属未改变，两项暂缓未撤回；模型结果仍需独立核对。
