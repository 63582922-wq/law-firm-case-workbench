# 完整全流程开发计划

> 当前唯一执行基线（2026-08-15）：[律师办案 Agent 商用 Web 执行计划](docs/COMMERCIAL_WEB_EXECUTION_PLAN.md)。
> 产品能力与安全总纲：[律师办案 Agent OS 产品架构](docs/LAWYER_AGENT_OS_ARCHITECTURE.md)。本文件下方的大量阶段记录用于追溯；凡与当前基线冲突的桌面、本机简化流程、固定角色模板或旧状态均不再驱动开发。

## 2026-09-10 当前交付阶段（优先于下方历史记录）

### 2026-09-11 用户办案界面收敛（进行中）

- 已在真实浏览器收敛三处用户可见断点：成果页将“内部审阅稿”与“法院提交候选”分开；文书正文按章节折叠、来源按段落展开；原始材料案的材料记录、材料提示和办案研判默认只呈现行动摘要，完整判断与来源按需展开。已用浏览器核对原始材料案与 11 份材料案的实际状态，不再显示模型、运行参数或研发说明作为默认工作内容。
- 浏览器实际下载复核已完成：现有受控合成案的 3 页 PDF 审阅稿和可编辑 Word 均经服务端版本核验后发起下载并在浏览器确认完成；它证明“已确认输入→审阅稿→下载”，不替代无预置事实案的完整链。
- 原始材料案已实际显示来源绑定的风险、补证与下一步候选。其尚未形成文书的原因是正式诉请、争点、法源和律师回应仍未确认；这不是模型失败，也不能由 Agent 自动越过。下一目标是新建一条独立、固定预算的合成律师决策验收，将已验证的原始材料链接续到文书候选、证据目录和下载，并明确标记为测试律师决策，绝不模拟真实案件批准或法院提交。
- 案件首页改为“办案进度 + 当前唯一行动”。只有在代理情境和材料均已确认后才展开办案助手，避免在尚未具备输入时向律师展示内部处理状态或目标编排。
- “依据与测算”已接入官方来源核对，并新增“从已入卷材料页确认合同、付款、起诉、受理等关键日期”的受控动作；服务端生成审批绑定，不接受浏览器提交哈希、对象地址、规则参数或利率。
- 该阶段尚未完成：受控规则卡、连续规则包和正式金额核对仍需继续接入同一网页旅程；不能因为可确认日期或可显示进度而称全流程已交付。
- 已识别并修复原始材料主线的一处实质断点：材料经律师确认后若尚无正式争点，旧研判只生成笼统事实锚点，无法给律师下一步。ADR-0124 改为输出来源绑定的“待确认问题、补证缺口和下一步”，仍不自动写入正式诉请、争点、规则或金额。后续必须以一次有上限的真实研判验证该候选能被律师界面审阅并进入案件范围确认。
- 已完成该项真实合成验证：从一份未预置案件事实的原始 PDF 开始，系统完成本地读卷、一次受控候选提取、律师确认两项事实、一次受控研判和独立复核。最终候选在律师审阅服务中呈为“需要处理的问题 / 下一步工作 / 待补证据”，并绑定到已确认材料；研判运行实际消耗一次外部调用、无自动重试。它证明首轮材料到可审阅办案建议的链路，不证明正式答辩状、证据目录、计算或法院提交文件已交付。
- 过程中发现并修复两项实际运行断点：已确认的提取批次在旧整案运行取消后不应迫使同页重新外发（ADR-0125）；来源绑定问题发现候选必须进入与传统律师决策包同等严格的独立复核注册表（ADR-0126）。两项修复均有聚焦回归测试，避免把本次模型返回当作“已成功”而绕过复核。
- 前台成果面板已优先展示问题、建议动作和待补证据，隐藏模型、任务图、调用参数等研发信息；案件要点页改为“确认本案要回应什么”。前端生产构建、类型检查和线上首页健康检查已通过。当前浏览器自动化会话超时，尚未取得这一状态的可视化浏览器验收记录，不能把构建通过描述为视觉验收。
- 案件范围确认页现在会读取当前已独立复核的材料整理结果：把“需要处理的问题、建议先做、待补证据”放在填写表单之前，律师可明确地将建议问题带入可编辑草稿；仍须自行确认对方诉请并选择已确认事实后才能保存为争点候选。建议内容只作为可编辑草稿，保存、确认和后续法律结论仍受现有服务端版本/权限/来源校验约束。该项已通过前端类型检查、生产构建、线上健康检查；当前自动化浏览器连续超时，仍待可视化验收。
- 本轮独立合成验收已真实完成“当前输入 → 三份候选成果 → 服务端二次核验 → 下载接口”闭环：案件审阅意见（Word/PDF）、民事答辩状（Word/PDF）、证据目录（Excel/PDF）均返回当前审阅版本与正确文件类型，仍统一标为“待律师审阅”。成果页已改为三项可切换清单，不能再只显示第一份文书；主界面不再以内部审阅、Worker 或运行说明作为成果内容。
- 本轮发现并修正一次受管 API 热加载遗漏：证据目录使用普通 PDF 原件页作为目录来源时，旧进程仍按已废弃的渲染页哈希路径拒绝读取。重启加载当前来源核验代码后，证据目录的审阅、PDF 与 Excel 下载接口均按当前版本通过。没有新增模型调用、没有批准法律立场、没有锁定或提交法院材料。
- 已完成代码与接口层验收：前端 lint、生产构建、144 项聚焦后端测试和三份成果的六个下载响应均通过。浏览器自动化读取当前仍超时，因此不得把本轮接口验证表述为新的视觉或交互验收；下一步只在可用的用户浏览器中核对成果清单、切换、正文查看和下载触发，不再新增模型运行或重复生成候选。
- 已完成一次受控的正文修改链：在脱敏合成民事答辩状中保存一处来源绑定、非实质性的复核标记，律师生成授权后由零网络修订 Worker 生成第 2 版候选；新版本 Word（39,018 bytes）和 PDF（156,376 bytes）均通过当前审阅版本核验并返回 200。修复了一个实际运行断点：内容修订不应因前序只读任务的 `writes_managed_derivatives` / `approval_gate` 历史值而被拒绝；新版本仍须同时满足当前计划、来源、系统 Worker 和独立律师生成授权。它证明“修改→授权→新候选→下载”服务链，不代表律师批准或法院提交。浏览器自动化仍连续超时，故页面点击和视觉状态仍待补验。

### 2026-09-10 当前真实材料闭环验收（进行中）

#### 当前事实与唯一下一步（优先于下方所有历史叙述）

原始材料答辩目标的两阶段预算已经部署并在独立的干净合成案卷中完成零模型预检：没有已确认
事实时，运行实际持久化为累计 2 次/240 分、已用 0；已有确认事实时仍为一次/120 分。该行为由
服务器快照决定，浏览器不能加大预算，也不会退回通用 50 次/100000 分路线。

两次受控候选提取已真实返回并形成来源绑定候选；一次低风险候选确认已实际进入事实台账。第三次
提取在 5 秒内发生 TLS 握手被对端关闭，未产生私有响应对象。它保持一次 `UNKNOWN_SUBMISSION`，
不重发、不改写。ADR-0119 已将未来 DNS/TLS 连接前失败分类为零调用、零费用；真正未知结果仅作
一次私有对象查找后静默暂停，不再因提示词更新或后台轮询卡住案件。

当前不能称为完整 Agent 验收通过：无预置事实的合成案已完成“候选提取→律师确认→来源绑定研判
→风险与补证”，但尚未在同一案卷连续完成“答辩状、证据目录、补证清单→修订下载”，也还缺浏览器律师旅程的可见验收。
当前律师前台不再把内部运行说明当成操作指引。独立证据目录已接入受控成果目录：只投影律师已确认
纳入范围的证据页，输出 Excel/PDF 审阅稿，证明目的与证据三性保持待律师核对；它尚缺完整案卷的
生成/下载和浏览器可见验收，不能提前写作已交付。下一单一目标是先完成这条用户路径的可见验收，
再在模型连接恢复、输入固定时安排一次新的独立、
明确两次/240分上限的全链验收；历史未知调用不作为重试对象。

#### 原始材料答辩目标的两阶段预算（已部署并完成零模型预检）

已识别并修复一个阻止主线闭合的架构错误，而非模型能力问题：原始材料案件请求答辩状时，
材料候选提取与律师研判原先共用一次外部调用、120分的预算；第一阶段正常消耗额度后，第二
阶段必然无法进行，取消答辩目标又会错误落到50次、100000分的通用预算。ADR-0116现在将
预算限定为服务器快照驱动：没有确认事实的答辩目标只有累计2次/240分，确认事实后发起的
答辩目标仍是1次/120分，浏览器不能选择或放大预算。

控制层、专用规划器和 Worker 的聚焦检查通过，受管 API/Worker 已加载；独立干净案卷的持久运行
事件已经核对 2 次/240 分累计上限和零模型调用。该部署预检只解除预算阻断，不能替代后续一次
提取、律师确认和研判的全链验收。

本轮以现有无真实客户数据的合成案卷复跑统一办案主线。发现并修复两项会让律师卡住的
状态问题：被取消/过期运行的读卷结果不再错误计入当前材料覆盖；律师已点击启动办案后，
同一案件中本地、无网络的 PDF/Office 读取不再要求第二次逐任务批准。外部模型、OCR、
事实确认、法律立场、文书和对外提交的审阅门保持不变。

真实受管运行 `8f5b8053-3d39-5883-a6a6-4b2c4d45d3e7` 已完成本地读卷与一次受控候选
提取：外部调用 `1/1`、费用 `105/120` 分、无自动重试，随后进入 `READY_FOR_REVIEW`。
它产出 44 条可回到原件的候选，全部按 3 个异常原因组分流（来源文本可信边界不足或收付款
必要字段不完整），没有提升为事实或法律结论。这证明“材料→本地读取→一次受控提取→
来源绑定候选→待补证/重新提取分流”可真实运行；不证明风险研判、文书或最终应诉文件已完成。

前端 ESLint、TypeScript、生产构建及 185 项相关后端测试均通过，服务已重启且健康。浏览器
自动化读取在本机连续超时，未取得本轮视觉/交互截图；这是一项独立的验收证据缺口，不能
替代或否定上述服务端真实运行。下一单一目标：在律师界面确认 3 个材料异常组的可见动作和
来源回看，再验证系统将“需要补证/重新提取”明确呈为风险与证据缺口，而非要求律师逐笔猜测。

律师端已进一步收敛为可执行的下一步：材料整理尚待核对时，案件首页不再先展示任务计数、
成果计数或内部处理摘要，而是直接引导至“核对整理结果”。案件要点页也把材料整理移到最前，
在没有已确认事实时不展示空的诉请/争点填写表，避免律师在材料尚未可用时被迫猜测下一步。
概览页原有的四步流程说明已移除，统一办案 Agent 现在是页面中唯一的“下一步”入口，避免
相互矛盾的进度提示和面向研发的流程解释。
这项改动已通过前端构建并已部署到本地受管前端；仍缺本机浏览器自动化恢复后的视觉与交互
验收，不能据此声称用户路径已浏览器实测。

另对既有“已确认合成输入→可编辑答辩状”链做了只读复核：运行
`ca6ccf3a-0b8f-5cb3-8ced-2b1bc0fb83ce` 的第 3 版答辩状仍为 `CURRENT`、可下载，包含
7 个审阅章节；DOCX 39,100 bytes 与 3 页 PDF 161,694 bytes 均可重新读取，且两者都有
“律师复核候选”标记。该证据证明已确认输入后的文书候选链仍可用；它不替代本轮
“原始材料→异常分流”运行，也不等同于律师已批准、签署或向法院提交。

### 2026-09-10 单一律师办案主线与读卷后续阶段（当前执行）

已确认用户侧的实质断点：统一办案 Agent 可以完成“先读卷”的准备阶段，后端也已有
同一运行的来源绑定分析续阶段，但律师网页没有继续入口；材料页还保留一条历史材料
Agent，造成两个“开始分析”的平行路径。该问题会让用户误以为系统没有 Agent 能力。

本轮已采用 ADR-0113：材料页不再发起历史平行 Agent，只保留接收、原件审阅和回案件
概览的单一动作；案件概览中的统一 Agent 在读卷完成后只显示“核对整理结果”。律师确认
可用的事实与收付款、处理需要单独核对的内容后，系统才以当前版本重新规划风险、补证和
文书工作。浏览器不能传入候选、来源、模型、提示词或预算；未确认材料仍不会成为事实、
文书或对外提交内容。

已发现并修复读卷后续阶段在只读事务错误使用写锁的问题；真实预检确认无预置事实合成案
仍有一个已验证读卷任务、56 条待核候选和一次受控研判额度。随后真实追加被当前案卷版本
保护拒绝：读卷任务快照为版本 7，而案卷已更新至版本 14。该拒绝正确阻止旧案卷研判，不能
绕过。下一单一目标：通过律师端材料整理结果完成受控确认/分流，触发已有 snapshot refresh
与重规划主线，并以一个新的无预置事实合成案件验证“材料→核对→风险与补证→审批→结果”。

### 2026-09-10 首轮研判输入收敛（当前执行阶段）

真实合成案件的已发送研判任务暴露出一项产品链路问题：首次严格结构化模型请求包含 67 条输入，其中大量为尚未确认的交易/事实提取候选。该设计既让待核材料过早进入模型前提，也使首轮请求在服务端响应前中断，不能作为可用办案体验。

已采纳 ADR-0111：原件、候选及其原页绑定继续保留在材料核对链路；首次律师决策包只接收已治理的代理边界、确认/争议事实与诉请、法源/规则和待核事项。受控规划器及回归测试已更新。传输层同时只记录固定的响应头失败类别和请求哈希，绝不记录模型可能回显的案卷内容或密钥。此阶段的单一下一目标：部署该输入边界，重新建立一个新的、一次调用上限的合成案件研判任务，核验它能产生可审阅的来源绑定决策包；旧 `d7943197-470f-56ba-a75b-3d672533b0fe` 保持 UNKNOWN，只做查档，不重传。

### 2026-09-10 暂缓材料核对与研判执行解耦（当前执行）

已确认两项 `DEFERRED_REVIEW` 仅表示未确认候选须由律师在材料区复核；它们不构成重提取或必要补证。此前调度器将所有活动后续事项作为案件级锁，导致新的受控研判运行在 `RUN_CREATED` 后被静默，不是模型、额度或浏览器问题。

本阶段保留暂缓候选和审计链，但只让 `REEXTRACTION` 与 `MORE_EVIDENCE` 阻止运行；仅重新唤醒尚未开始、非恢复、非控制运行。下一步：部署迁移，观察当前新运行进入本地规划和律师可见审批；旧未知响应不重发。

### 2026-09-10 首次真实来源受控研判（已完成）

当前原始材料合成案件已从浏览器发起并完成一次新的受控模型调用：本地规划、律师界面批准、一次外部分析、独立校验均已完成，运行进入 `READY_FOR_REVIEW`。该决策包关联 11 项受控输入、没有任何未确认事实/交易候选；生成案件上下文核对、法源检索计划及律师决策包三项候选。旧未知响应保持历史且未重发。

浏览器实测显示：在材料和法源不足时，Agent 输出可追溯的风险、补证、反制与律师决定，而不伪造答辩状。结果页默认改为五组律师优先信息，并将其余明细、来源清单和运行依据折叠。完整合成案件已保留可编辑 Word、PDF 预览和来源绑定候选，尚待以浏览器完成文书审阅、版本更新与下载验收。下一单一目标：闭合该有限文书链，并将当前“材料不足→不生成正式文书”的结果作为商用安全验收记录。

### 2026-09-10 客户工作台收敛（当前完成阶段）

已把客户可见首页与材料页从研发控制面收敛为律师办案路径：案件概览只显示下一步、待处理事项、可审阅成果和材料入口；材料页只保留添加、确认接收、页面审阅和异常处理。模型、任务图、Worker、调用参数、服务装配和开发说明不再出现在默认客户界面；重复的五步说明条已移除。真实受管浏览器已检查案件首页与材料页，前端 ESLint、TypeScript 与生产构建通过，已重启前端并恢复健康。

这只证明客户界面与当前运行状态一致，不证明本案分析或应诉文件已完成。下一单一目标：只读确认 `d7943197-470f-56ba-a75b-3d672533b0fe` 的不确定结果边界，修复可证明的传输问题后仅安排一次有上限的真实分析验收；不重传既有不确定请求、不重复上传材料、不把恢复候选冒充正式成果。

### 2026-09-10 主链路重整（最新执行依据）

HTTP400修复已进入真实执行：0099应用，event56记录CASE_ANALYSIS_REQUEST_REPAIR_REVIEWED，
绑定原attemptce2e7015-1704-5904-a1e8-443f4830369f及原/新请求哈希，发送前再次比对。
本地编译和6项阶段合同检查通过；原任务完成第五图本地步骤，event63批准既有用户授权
范围内的一次分析，event64 actual RUNNING/SUBMITTED，attemptd7943197-470f-56ba-a75b-3d672533b0fe。
没有重复上传或抽取，累计上限5调用/239分；当前应观察这一个明确句柄，不因等待重发。
实际新响应及质量尚未知，下一单一目标：读取并审查本次结果，接续用户可用成果。

HTTP拒绝诊断已补齐：非200读取最多8193字节，只记录固定错误类别、截断标记和摘要，
不将供应商可能回显的案件内容/密钥/指令写入日志；正文读取失败仍保留已知HTTP失败。
同时在请求准备阶段拒绝任何嵌套空enum和倒置范围，避免同类格式错误再次外发。
诊断与传输回归通过；此为代码修复，不追溯猜造此前400正文，不新增模型调用。
下一步仍是本次已知格式失败的显式恢复，保留event55与此前累计调用，不自动重试。

新版修订实际执行记录：0098已正常应用，原Run追加CASE_ANALYSIS_REVISION_REVIEWED/event46，
第四图完成本地计划/整理，正常执行批准event53。task2af6fa13-7c74-52fb-8305-855691ee84d5，
attemptce2e7015-1704-5904-a1e8-443f4830369f已终止FAILED/SUBMITTED，event55 WAITING_INPUT，
错误LAWYER_ANALYSIS_HTTP_400；本次模型响应未生成，记录费用0，不能继续当作等待或成功。
累计上限4调用/239分，没有重发；旧分析及质量问题保留。
查到确定的请求缺陷：无已核验法源时新版schema使用enum=[]，现改为不可选哨兵且数组
maxItems=0，4项请求检查通过。现传输层在非200时丢弃错误正文，故只能判为400的高可信
候选原因，不能断言供应商已证实或修复后实际调用通过。下一单一目标为保留有界错误诊断，
让已知请求格式修复按显式费用记录恢复，不清零历史/不冒充未发出/不自动反复试模型。

新版文书消费已接线：内部分析工作稿直接读取新版问题、双方立场、反制、具体行动和
补证，不显示旧技术运行说明；引用保留分析成果版本和当前文书已授权的底层来源，
不把未确认候选变为正式事实。答辩状的风险读取兼容新版问题，但原事实/法源审批门保留，
不能据此称为完整可交付答辩状。5项新候选/适配器/文书来源检查和34项文书检查通过。
实际Worker只读重建event45当前分析投影：67项来源，新合同逐项处置67项，输入估计45481，
没有模型外发、没有新文书。当前单一下一目标仍为：在明确保留旧质量失败及费用情况下
运行一次新版修订，检验实际问题覆盖和立场，再推进真实可编辑成果；避免再新增外围检查。

新版候选与实际适配器源码已接通：agent-discovered-lawyer-analysis-candidate-v1保存输入
版本、原始来源投影、来源处置、自主问题和真实调用回执；独立读取重新校验来源绑定、
问题摘要、候选标记及用量计费。现有公开成果解析入口兼容新版，旧成果仍走原解析器。
网页投影显示问题/双方立场/反制/下一步/补证，原来源链接不替换为虚构正式争点。
适配器在存在FACT_CANDIDATE或TRANSACTION_CANDIDATE时选择新版同次发现+分析请求，
已确认台账的历史路径暂保持旧合同。4项新版候选（含实际适配器模拟响应到暂存路径）、
32项原分析和15项网页检查通过；这些是本地证据，没有真实新版模型调用或浏览器新成果。
尚未重启Worker启用源码；当前Run仍event45旧质量不合格候选。下一单一目标：核实新版
成果与文书消费边界、在保留失败质量记录和累计费用下安排明确一次修订运行，再读实质
输出及浏览器成果，不回退到重复上传/抽取，也不把模拟供应商成功称为真实模型能力。

动态分析请求/响应接线增量：新增discovered_analysis同次请求，包含自主问题标题、
双方立场、优劣证据、补证、反制、残余风险和下一动作，并逐项处置全部来源。
复用现有受控供应商请求准备、预算估计、请求哈希、用量解析与定性金额边界，
没有另起发现调用。旧32项分析合同保持通过。新响应按交叉引用与逐来源覆盖拒绝遗漏。
尚未切换Worker：还缺新版候选成果编译/解析和网页投影；当前旧成果及运行记录不变，
没有新增外发。下一单一目标：把新响应落成带版本、来源和审阅状态的成果，接通页面，
再一次性切换调用链，不能提前让Worker发送无法读取的新版结果。

动态争点合同已开始替换设计：新增case_agent_issue_discovery，模型可将多条来源关联到
同一个实质问题，案号/日期可保留背景；每个输入必须有明确处置，不因不成为争点而消失。
服务器生成独立proposed-issue标识，绑定来源版本，禁止伪装为正式争点或审批。
本模块尚未接入现行请求/成果编译，不能声称分析能力已经改善；没有新增模型调用。
下一步集中切换分析合同及候选编译，保留旧成果可读，避免另加一轮只做发现的付费调用。

第三图实际返回：READY_FOR_REVIEW/event45，成果0185291a-4292-5465-96b6-b99d23f416a5。
qwen3.7-plus输入25861/output4604/合计30465tokens，本次计费9分、重试0；已终止运行，
不是仍在等待。用户可见成果读回：引用交易候选仅2处，正文没有重复、退款、港币、手续费；
把被告利息冲抵作为对方攻防并把减少计息基数写作风险，仍有主体立场倒置。
虽然独立格式验证通过，产品质量判为未通过，不批准法律口径、不据此生成提交件。
已定位结构问题：_analysis_anchor_sources优先取原有CASE_FACT且截20项，合同强制每项
输出风险/攻防/行动，案号、受理日期被包装为争点；加入材料本身不足以纠正此设计。
下一单一目标：让模型提出来源绑定的实质争点与跨材料问题，取消逐事实造风险的合同，
保留可核验引用、候选状态、立场归属与正式金额边界；先改分析结构，不重复付费碰结果。

同Run分析续阶段已实际启动：0097正常应用，原Run追加CASE_ANALYSIS_STAGE_REVIEWED/event34，
第三图包含本地法源计划、case_context_review、lawyer_decision_package；前两项实际成功。
按既有用户授权通过正常任务执行入口追加批准event41，分析任务2110b5f0-df9f-58da-ae71-d1f9eabcde31
在event42进入EXECUTING；attempt f0ae1bcd-db7b-54b2-a7d1-d67d155cd54c已RUNNING/SUBMITTED。
禁止因观察超时重发。累计上限3调用/230分，本阶段一次分析最多120分；旧用量和旧成果保留。
4项续阶段合同、21项监督器、27项持久层检查通过；实际Worker编译56项候选范围通过。
下一单一目标：读回这次模型结果并检查重复、退款、双方立场、主体身份与证据缺口，
随后推进律师可编辑的应诉成果。不把已提交调用当作分析质量通过或全案完成。

未确认身份/材料记载接线已补齐：FACT_CANDIDATE 与交易候选分型，均只供初步分析，
不成为已确认事实或文书前提。读取排除已推广记录及已提交纠正的旧候选；私人未提交
草稿不进入Worker。实际Worker只读装配得到67项来源，包括48项交易候选和8项事实
候选，两项分析任务均完整选入56项候选。0096已通过回滚预检并正常应用。
本轮6项来源合同、10项context、15项网页来源、12项规划路由检查通过；没有模型外发，
没有新成果或律师接受。当前下一单一目标：实现同Run从已完成抽取转入分析的受控续阶段，
保留原事件、成果及累计2次外发/110分预算占用，限定新增一次分析；不要求用户再次授权，
不伪造案件版本更新、不清零预算、不重复读取/抽取。旧material_stage仍仅允许抽取图，
这是需要修复的阶段衔接能力，不是账号充值或用户权限问题。

未确认交易分析接线：新增TRANSACTION_CANDIDATE，逐项复核已验证抽取台账的候选哈希、
来源页、材料名称与既有律师决定，不提升为正式交易、不计算官方余额。当前权威读得48条
（新流水43+法院材料5），加旧事实/代理情境/待核事项形成59个分析来源，待抽取页为0。
真实Worker装配只读预检证明context及lawyer_decision_package都选入全部48条，并且没有
读取/OCR任务；此为输入选取证明，不是新分析已经执行。新增来源只允许context/analysis，
文书入口拒绝作为确认前提。网页来源回链和计划引用类型已接线；0095已APPLIED。
4项新来源边界、12项规划路由、18项权威投影、10项context、18项装配、32项分析合同、
15项成果网页、11项动态计划检查通过；均不构成模型分析质量或用户接受。
还不能直接再次调用：原Run保留的补充材料阶段只授权读取/抽取，累计调用2次已使用。
下一单一目标为完整候选输入后的同Run分析续阶段：还须纳入新抽取4项主体/户名记载的
未确认事实来源，避免分析只看到简称而缺少材料内身份说明；保留已有纠正/暂缓，
然后用明确累计上限授权分析，不清零预算、不再重复抽取。新研判/答辩状/独立证据目录
及补证清单仍未交付，不能把本轮59项输入接线当作这些成果已完成。

用户可见闭合：抽取成果原先被通用阅读接口拒绝，现新增结构校验后的“材料与收付款核对”
视图：4条材料记载、43条逐笔记录、原文摘录及原页按钮；未改候选原文或审批状态。
只读匹配同日/同额/同渠道/同付款收款标签，显示两组共4条“可能重复，待核对”，
不合并、不删除、不自动计入金额。网页投影15项检查通过。
真实Chrome无需登录，首页打开最新成果成功看到47条；点击一条可能重复记录的原页按钮，
实际进入微信账单第3页/evidence focus=62fe1d53-c8d8-4944-b3bd-4c19f6115571，单页图像已显示。
此为真实候选查看/来源回查证据，不代表43条交易已核实或整个案件完成。页面仍有重复说明
和通用成果标题，后续须整理；当前优先让新候选真正进入风险/证据分析，不再重复模型抽取。

补充22页实际运行已完成：原Run第二图READY_FOR_REVIEW/event33，抽取批次
cd536f3f-d74d-5079-8eec-0aabbca19a17，成果9f41fbae-ecc2-5455-aa9e-f4640578ad20。
形成4条事实候选+43条交易候选，未确认任何新事实/交易；正式交易表仍0行。
按PDF技能读取两份原件全部22页：包含43条交易，抽取摘录逐编号覆盖，无缺号；两页为空页，
20页存在候选来源正常。港币8000、人民币周转款50000、第二笔还款说明10000、银行退款
20000、两笔手续费及可能重复的#9/#45、#21/#46均原样保留，不合并原件或自动抵扣。
这不代表实体归属正确：模型未主动标出两组可能重复，43条交易仍须按来源归属/冲正/费用
研判。初步分析尚未消费本次新候选，不能宣称风险分析/答辩状已完成。
累计外发2次；账本占用110分，其中本次105分为保守预算预留，并非供应商实际账单。
本阶段没有重发；下一单一目标为把未确认候选作为带来源、带状态的分析输入，生成律师
可复核的风险与证据问题，而不是要求先将43条记录人工确认为实体事实才能分析。

同Run补充处理现已实际进入执行：0094部署，原run4af42bf5-11eb-55dd-9779-5ec650ffabf0
追加SUPPLEMENTARY_MATERIAL_STAGE_REVIEWED/event17；目标/原始预算/旧成果和用量不清零，
有效累计额度2调用/125分（原已用1调用/5分，本阶段新增最多1调用/120分）。
原生PDF与OCR同时可用曾被规划器误判歧义（本地失败event19，无外发）；修正为优先本地
PDF原文，保留真实Office格式歧义拒绝。正常暂停/恢复20/21后生成第二图，包含本地法源
计划、22页PDF读取、一次候选抽取。前两项实际成功；抽取attempt32c1189f-a92e-58f2-a6e7-c8549da742f0
已RUNNING/SUBMITTED，尚待实际结果和来源质量复核，不得重复发送。原两项暂缓不撤回。
新阶段合同9项、原生阅读路由11项、监督器21项、持久层27项检查通过，非整案验收。
0094首次字段歧义导致事务整体回滚，APPLYING留存；专用恢复脚本核验旧摘要和对象不存在，
在单事务完成修正DDL及APPLIED，失败摘要保存在脚本；两个新增函数归属恢复为schema_owner。
下一单一目标：读回本次抽取及原页来源，再接入有双方立场区分的风险分析和文书成果。

当前增量复核：未知抽取覆盖不再被当成“已完成”静默排除；只有权威覆盖true才跳过。
当前真实Worker事务再次读得14版、22待抽取原页及2待核事项。新增同Run补充阶段提案，
绑定原页覆盖/旧图/事件版本，保留费用累计，限下一阶段1调用/120分；6项检查通过。
此提案尚未接入持久命令和队列，不是已授权或已抽取。下一单一目标：完成同Run补充
阶段的事务入口及限定调度，再实际读取22页；不重跑旧8页，不重新建案或清零用量。

连续执行校正：实际核对unassisted-court-g763df86的F1字节哈希与已入卷案件
767fda38-e3de-5a15-816f-510a686c7600完全相同（1e093375…ac1b）。下方“未入库”
为早期历史状态，不能据此重新创建案件/上传/抽取；沿用既有7项确认事实与待复核候选。
本阶段计划为补入争议还款材料并关联原补证事项，不重复已完成读卷。
按PDF技能读取合成微信账单12页及渲染第12页，中文可读；材料仍为简化文本，
不证明真实微信账单版式/视觉识别。正常上传服务完成slot95560cbd-af11-4353-8acf-3f48a92c6ebe，
原件ceafb948-c56b-47b3-83bc-066698e395ca，12页，SHA-256
1e160b9265b85d23a03421471885dd951ae6a06258f1a6f3fa85e9e762103a42，案件12→13。
意图/slot/回执已归档artifacts/lawcase-wechat-supplement-20260910；新增固定范围入卷脚本
拒绝哈希差异、旧检查点重放及案件版本漂移。没有新事实确认或模型调用。
真实Web后续服务将该原件及已入卷银行流水绑定8c206843-ac6e-4354-b358-b3dd7aeac34e，
固定键unassisted-repayment-supplement-20260910-v1，回执SATISFIED、案件14。
只模拟合成补证材料关联，不批准还本/冲抵/交易归属/余额，亦非真实律师认可或用户验收。
复核：本阶段入卷和补证关联已完成；API/Worker运行正常。仍有两项待研判、候选纠正及
同目标阶段推进要闭合，尚未形成此原始材料案件的风险分析/独立证据目录/补证清单。
下一单一目标：保留未决事项和来源，接通初步研判，不以撤回争议或批准未知事实解除阻断。

初步研判状态门已修正：已存在至少一项确认来源事实时，候选/争议/否认事实及未决诉请
可保留原状态、哈希和引用进入既有context/analysis合同；不再要求其全部CONFIRMED。
正式文书来源门仍严格确认，法源/规则仍LOCKED，不把未确认事实提升为已批准。
路由11项、context10项、分析合同31项检查通过；新增检查覆盖不确定来源确实保留、
仅候选不能冒充确认基础及同样输入不能直接进入文书。无队列任务时更新本地Worker。
本阶段尚未新增模型调用；两项暂缓事项仍ACTIVE，其页级来源信号与窄分析投影的
衔接仍需闭合，不能删除这些信号来得到“通过”。下一单一目标为该来源绑定衔接与实际研判。

进一步定位：暂缓信号绑定evidence-page，窄分析投影删除原页后必然拒绝；不能简单忽略信号，
也不能把整页原件直接交给窄分析供应商。新增独立BoundReviewObligation合同，保留当前决定、
原状态、原因、版本、页级引用，原因变化也改变内容哈希；不伪装成已确认事实。
4项检查覆盖原样保留、原因变更失效、来源不可见/状态提升拒绝及重抽取不可冒充完成。
尚未接入运行时，不产生新模型调用；下一单一目标为权威投影→context→分析及回查的完整接线。

上述合同现已接入源码：权威投影在核验当前复核生命周期及原页之后构造REVIEW_OBLIGATION；
窄分析只将已存在的同ID未决事项信号重绑定至该结构化来源，原始完整投影仍保留页级引用。
context在同事务重读权威投影及原因，不接受浏览器正文；分析保留未决锚点并同时保留事实锚点，
结果锚点解析使用相同次序，标题不称“已确认”。运行装配仅context/analysis注册此类型，
文书来源门拒绝，计划提升遇到尚无映射类型明确拒绝而非未捕获KeyError。
规划持久层18项、分析32项（含未决+事实锚点一致）、路由11项、装配18项通过。
未重启消费者或新发模型请求；计划来源SQL白名单/网页来源定位及实际组合回读尚未完成，
不能称运行闭环。下一单一目标为新类型的计划/网页来源绑定与真实当前案件预检后研判。

真实预检通过：Worker真实权限读取案件14版，得到8项事实、代理档案及2项REVIEW_OBLIGATION，
暂缓信号分别指向同ID复核来源，未丢失。0091已通过正常postgres-migrate应用，仅扩展
计划输入类型且限定WORK_PLAN用途/未决状态/同ID前缀，未放宽正式文书来源。
计划提升映射及Web中文类型标签已接线，计划16项/Web计划11项通过；原页点击定位仍待浏览器验证。
当前真实任务4af42bf5-11eb-55dd-9779-5ec650ffabf0已创建，固定键
unassisted-source-bound-analysis-20260910-v1，案件14版，最多1次调用/120分，未预置事实。
实际状态CREATED/event1，队列QUIET，没有发出模型请求；不能把已创建当已运行。
原因已定位0050 wake及runtime claim共同围栏：同案存在ACTIVE后续事项时，仅旧控制run可领取。
两项暂缓仍由旧run控制，因此新初步研判被静默停放。这不是模型故障或缺少用户充值授权。
下一单一目标：以明确的只读分析阶段范围接通队列，保留异常控制归属和重抽取/写入围栏；
不得直接改QUIET为READY绕过双重检查，不重建本任务，不撤回暂缓冒充解决。

队列修复已实施ADR-0103/0092及0093：健康控制归属保持，只对无激活文书计划、1次/120分
受控研判及其本地法源检索计划开放并行读取；wake/claim双检查共享同一限定谓词。
真实Worker角色初步研判true、原文书执行false；15项队列检查通过，正常迁移记账。
本任务经Web正常暂停/恢复唤醒，已持久规划3任务；context和本地法源检索计划完成，
进入WAITING_APPROVAL/event11。合成验收审批仅针对27308d7c-061a-5dcb-a5aa-7bf4e6c0f75f
单次内部分析，固定键unassisted-analysis-model-approval-v1；当前EXECUTING/event13。
没有重建案件/目标或增加预算，没有撤回暂缓或批准法律立场。下一单一目标读取本次结果并
核对风险/未决事项来源与页面展示，不重复发送模型请求。

本次真实模型已返回：原任务4af42bf5-11eb-55dd-9779-5ec650ffabf0到READY_FOR_REVIEW/event16，
3项任务SUCCEEDED，其中仅分析任务SUBMITTED（一次），本地两项NOT_APPLICABLE。
律师决策包d5bff12e-d94b-5423-af7d-e0c0bb08ee1f；context为dbb568f8-7799-552a-aa67-71b64268359b，
法源检索计划baced2f0-5b1f-5ce9-9b69-0824d2849c74。未重复调用或改变原异常控制。
正常Web成果读取实际发现review-obligation前缀未加入来源解析器，已补专属待核标签、
不伪装直接原页；14项网页成果测试通过。相同持久成果再读取成功，包含10项争点/证据风险、
4项对方主张与反制、2项策略、5项当事人补充、10项行动、3项律师决定及模型运行证据。
此处仅证明真实生成和服务可读，未据计数判断法律质量；新来源到原页的浏览器定位、正文
逐项审阅及应诉文书/证据目录/补证清单仍需完成。下一目标审阅本成果并推进必要决定和交付。

实际正文质量复核未通过，不将READY_FOR_REVIEW称可交付：分析把我方利息冲抵放进对方主张，
把“缺少直接纠正候选入口”旧开发说明当诉讼风险，将暂缓标记当实体重大疑点；请求存在
被描述为证据支持较强，法律研究又被确定性编译器全部归入当事人补充。补入流水尚未抽取
进本次窄分析，它不能证明完整材料理解。模型实用量5278输入/4428输出，共9706token，5分。
修复：系统提示明确角色归属、请求不等于权利成立、流程状态不证明实体风险、开发说明非案情，
法源不列当事人补证、未覆盖不等于材料未提交。历史候选不改写，Web显示将法源类追问
分流“由律师与Agent核验”，实体材料仍留当事人补充；14项网页测试含分流及原件不变，
32项分析合同通过。尚未重发模型；提示修改不是已证明质量修复。
下一单一目标：把已入卷银行/微信材料的来源抽取与案情主张区别接入工作流，完成原页定位，
避免只拿法院程序事实与旧暂缓原因反复分析；不批准当前低质量候选或用它直接生成提交文书。

待核事项原页定位已实际接通：Web以当前律师同案权限，从活动后续事项→原抽取组→候选页
重新关联，不使用浏览器页号或伪造事实。按原signal分片ID仅选对应分片首个原页；不是完整
多页浏览器清单，重复拒绝类暂未接此路径。两项分别回链1c5d25e7…与66c1081e…。
14项网页成果检查通过；真实浏览器曾暴露HTTP投影页绑定白名单漏新类型，现补齐。
Chrome无需重新登录打开本次真实决策包，看到“由律师与Agent核验”；点击首项待核事项
“查看原始页”实际进入/evidence?case=767fda38…&focus=1c5d25e7…，页面显示法院材料第5页
及其单页图像。材料列表同步显示3份原件。没有新模型调用或事实/证据批准。
本阶段来源定位完成，正文质量仍未通过；下一单一目标为新增流水的实际抽取与材料覆盖
状态接入，防止已有少量事实时窄投影静默跳过新材料。

材料覆盖已按真实执行台账查明：新增只读read_material_extraction_coverage，按原件/页身份
比对成功extract_case_ledger任务输入与同图PASSED独立复核；不按候选数量推断，空事实页
亦可已处理，重复内容不同原件仍各自保留。当前法院8页全部已抽取，银行10页、微信12页
均0页已抽取，合计22页未进入本次分析。真实Worker角色查询成功，无新模型调用。
这证实少量法院事实触发窄分析后漏掉新增原件；上次结果不能当完整案卷分析。
当前覆盖读取尚未接入自动规划/网页状态；下一单一目标为按这22页生成受控补充抽取，
并保持原异常控制、累计预算和当前来源版本，不通过重抽旧8页或再次分析旧事实代替。

覆盖状态已接入权威规划事务：原页extraction_complete作为处理元数据进入规划摘要，
不更改原件内容哈希/证据决定。窄投影保留未处理页，规划优先材料准备，不再因已有事实跳过。
路由11项（含已处理排除、未处理必须有读取器）、规划快照16项、持久投影18项通过。
真实Worker权限当前14版窄投影读得22个未处理页并保留2项待核事项，旧8页已排除。
未运行新模型或重启常驻Worker；现有只读分析队列例外不包含补充抽取，须继续接合法材料授权、
补充抽取范围与异常控制并存，不能直接放宽为任意工具。下一目标为该22页真实抽取执行。

阶段推进代码（ADR-0102）：材料准备从逐页任务改为按读取器批处理，保留逐页来源；
可授权原生PDF接一个候选抽取任务，混合OCR作为直接依赖且保持OCR风险，不伪装原生。
初步研判只要求已确认代理与来源事实，不再强迫先有诉请/批准法源；已激活文书计划的
完整来源门仍保留。84页批处理/抽取依赖、缺法源初步研判、禁止OCR伪装等9项路由测试通过。
普通及恢复规划均按剩余预算编译，保留原目标与累计用量，额度不足留明确下一阶段检查点。
发现事实抽取过去以0费用记录，现按官方高峰价设保守费用预留、120分上限和外发前拒绝门，
历史0费用仅查询恢复，不重新发送。预留不是实际账单。抽取+路由13项、Worker/Web控制55项、
运行装配/抽取持久化/分析63项聚焦检查通过；实际生产组合根预检通过，无队列待执行时
更新API/Worker。尚无新原始材料整案运行；预算调整入口及正式诉请/文书所需决定仍待闭合。
下一单一目标：无预置事实的合成原件入卷与候选抽取实跑；不得将本段代码测试当Agent能力验收。

当前已实跑到：分析 → 激活合成计划 → 文书第1版 → 正文修订第2版。文书运行
ca6ccf3a-0b8f-5cb3-8ced-2b1bc0fb83ce为READY_FOR_REVIEW；第1版Word/PDF已在
Chrome实际下载并对照服务端摘要，PDF共3页、中文逐页正常。本机另行渲染Word的字体
环境缺字，不能据此宣称Word视觉验收通过。第1版保留在outputs/synthetic-defence-review。
正文修订实际暴露绑定事务只设firm而漏actor，RLS合法隐藏认领任务；现补真实Worker
上下文，不放宽策略，7项绑定测试通过。实测授权/请求/任务/审计事务在入队后注入异常
完整回滚、同键保存/授权无重复；第三项显式合成修订7d16efb0-13ce-5917-a3e3-0d6a0545d8ca
SUCCEEDED，新包a2890b80-6653-5d44-987b-d9e23e0b1554为第2版，前两项FAILED保留。
修改仅将已有诉请争议改为直接答辩表达，不新增事实/法律批准。该修订零模型调用，
服务独立读取文件通过；本地overlay消费者及页面入口已接通。随后Chrome实际修改第2版
诉请段落、保存并模拟授权生成，第3版包7d0bf5dc-a7de-5059-9297-3703a8b1a70b获PASSED。
真实浏览器下载到outputs/synthetic-defence-review/网页修改验收稿.docx及同名PDF；
Word SHA-256 cf7a29417a703021ca9eab16d0a3c9c120c1009b37085fa1be74f224766a1dec，
PDF 74849849b760ce1ff9c140eb4efca6483861d126678b6b7e6e79cb73aef1bc8a，与服务下载一致。
修改文字在两文件中均核对存在，3页PDF逐页中文正常；旧第1版审阅标识下载被服务拒绝。
保存后的生成入口原来藏在页面底部历史记录，现复用原授权组件直接接在保存结果旁，
并显示真实生成状态，避免新版成功后仍写“尚未生成”。类型/lint及单进程构建部署通过。
修订与绑定21项聚焦测试通过；所有正文修订零模型调用，不代表律师终审或商用验收。
仍未完成：无预置事实的自主读卷、独立证据目录/补证清单、完整浏览器编辑旅程及商用验收。
下一单一目标：闭合未预置事实的原始材料准备→研判阶段推进，不重跑已通过的文书链。

连续执行补充：已修复分析到计划的来源合同断点，0087/0088前向迁移已应用；
APPROVED_LEGAL_RULE及LEGAL_RULE用途与代码一致，API补CASE_CLAIM显示标签。
同一分析生成候选计划64161e75-1bbb-4b49-843e-b9da14de901f，已在全合成夹具中模拟
审阅激活（不代表真实律师认可）。文书运行0259d9c1-7427-5744-b1a5-4cec4b7a3291
在零任务阶段拒收，原因是分析窄投影被误用于文书执行，过滤了ACTIVE计划信号。
文书阶段已改用正常权威投影，真实快照纯编译通过，未调用模型。
ADR-0101与0089/0090增加严格显式本地规划失败恢复：成功保存的本地提案、无图、无任务、
无任何规划/任务外发、当前计划不变，创建有前任关联的新执行而不改旧失败。
本段为文书生成前排障记录；实际文件及修订进度以上方最新状态为准，仍不得报整案完成。
聚焦检查：来源合同2项、网页计划11项、受控规划6项、运行持久化27项通过。

后续真实文书执行继续暴露两处旧合同：事实来源重新核验遗漏evidence_links及
correction_candidate_id；文件入库的来源清单仅支持事实/交易，没有诉请/法源/规则。
现两处已修复，保留已确认回应、当前法源许可、案件批准依据包、规则版本/指纹核验。
新增安全诊断只记录异常类和函数/行号，不记录异常正文、案情、局部变量或供应商响应。
文书绑定6项、交付持久化33项及新增3类来源成功/撤销/待复核分支检查通过。
历次失败均保留，每次新文书执行有前任关联且external_calls=0；模型分析仍只有原来1次。

本轮实际新分析已通过：固定运行3bb15109-a4dc-5d85-9e51-b20b53c57357，
合成案件0eb75f87-ccd3-5ae0-9207-c8df529d3657，起始版本34。通过正常服务新建，
硬预算120分/1次外发；独立读取确认READY_FOR_REVIEW、实际external_calls=1，成果包括
CASE_CONTEXT_REVIEW_CANDIDATE及LAWYER_DECISION_PACKAGE_CANDIDATE
（66ffce4d-36b1-5851-81d7-6fe019577984）。不是旧封存响应回放，不是仅服务就绪。
该案预置过确认事实，故仍不证明自主原始资料理解；未生成/批准文书，未宣称整案交付。

本轮实跑修复两个真正阻断：①已知模型拒收的WAITING_INPUT阻止显式新运行，现于原事务
重放所有任务回执后，只允许全部终结、无UNKNOWN且有指定输出拒收的新命令；旧事件不变。
②台账网页快照含evidence_links/correction_candidate_id，但规划快照遗漏，导致同版本
指纹不一致；现统一字段，18项规划持久化测试（含两端指纹一致）通过。新建守卫27项通过。
确认当时没有EXECUTING/PLANNING/VERIFYING运行后仅重启Worker，沿用同一新运行继续。
观察脚本600秒超时发生在排障之后、任务实际EXECUTING期间；未重新调用，随后只读读取
证实同一任务READY_FOR_REVIEW。临时会话均撤销。Chrome打开目标案时用户切换了应用，
停止进一步UI操作，尚不声称浏览器已验收新分析。下一目标：读取本次分析与来源并推进
当前计划和文书候选，继续修改/下载验证，禁止把分析包就绪当整体完成。

实际代码修复已完成第一段：答辩目标缺少所需来源类别时，规划投影保留原件，受控路由按
服务器授权的唯一PDF/Office/OCR读取Skill安排候选读取；不调用通用规划模型、不扩大预算。
已有激活文书计划仍禁止回退到读取/模型任务。新ADR-0098记录该边界。
8项聚焦测试通过，覆盖缺输入进入读取、读取工具歧义拒绝、原件不静默丢弃、完整来源原路线
及已激活计划不增加模型调用；git diff --check通过。未重启Worker或新建付费运行。
尚待：准备结果回到同一办案目标的阶段推进、预算不足检查点及实际浏览器端到端验收。
不能将仅原件读取候选标为已完成答辩目标，部署前必须继续核对这一完成语义。

用户已明确同意从原始合成资料出发重整主流程并要求“开始吧”。执行清单为
[主流程阻断清单](docs/DELIVERY_BLOCKERS.md)。本轮发现交付类型与固定应诉路线、单次预算
耦合：缺少已确认来源时不能先读卷，不能直接改走默认1000元上限的通用路线。
下一步先定义准备阶段合同与预算守恒，再修改路由；保留原始证据、历史失败和法律审批。
此前“新模型调用尚未得到回复”已被用户后续明确执行指令更新；在既有供应商、合成资料、
单次120分且不自动重发的范围内不再重复询问。但该单次上限不代表整个多阶段旅程的预算，
不得自动扩大为通用运行上限。本轮仅完成阻断定位和实施清单，没有新增付费运行。

首页交互修订（依据用户22:30截图反馈）：保留ADR-0003视觉，移除首页常驻教程与重复流程条，
已确认代理信息压缩为可展开摘要，任务/决定/成果优先；材料保留直接入口，前置清单按需展开。
工程实现说明不作为律师日常主界面文案；外发、法律决定及提交风险仍在对应动作旁保留。
验收：类型/构建检查后检查实际页面布局与展开、选案入口；不得把此UI修订当整案能力验收。

本次落实：首页移除常驻教程和重复流程条；已确认代理信息、材料处理状态、提交前核对、
使用帮助按需展开，任务标题和不可用提示改为简短业务语言。类型检查、变更文件lint、
低内存生产构建通过并更新前端。Chrome实际刷新后已核对选案、紧凑首页及代理摘要展开，
原件材料入口保留case参数；未调用模型、修改代理信息或批准法律立场。
实测发现重复选择同一案件会无条件清空代理确认状态，现增加同案选择无操作保护并重新构建；
该补丁已在Chrome强制刷新后复测：选择合成案件再重复选择同案，代理仍已确认、任务入口仍
显示可交代任务，没有重新进入待确认。此阶段只是首页降噪，不代表整案Agent或商用验收通过。
随后通过真实应诉材料导航检查同案20版：文书候选0、当前材料包无；界面仅有核对摘要和
收付款核对表生成入口，不能据此称已有答辩状交付。未点击生成、律师批准或提交动作。
下一目标：取得当前版本的来源绑定分析与动态文书计划，才能验证正文修改和下载；
继续适用下方独立新模型运行限制，不能用核对摘要替代应诉文书验收。

本轮执行链复核：在实际case-agent-worker容器内读取配置，document_delivery_enabled=true、
document_content_revisions_enabled=false；使用Worker/Verifier原有独立身份执行正文修订
read-only structural preflight通过。仅证明所检查的角色/RLS/任务触发器/读取权限存在，
不证明授权提案入队、事务回滚、后继渲染和律师修改旅程已验收。依ADR-0086不因结构预检
通过而自动打开开关。真实正文链验收仍需当前有效根文书；没有用旧失效包或恢复候选替代。

用户最新要求：按既定计划连续执行至完成。已建立持续目标，不以阶段修复标为目标完成。
当前文书失效路径修复：审阅缓存epoch纳入服务器输入状态；INPUTS_CHANGED即使任务事件号
未变，也撤销浏览器旧审阅/下载凭据的终审资格。计划页不再将旧任务显示为可生成或待终审，
而是明确返回当前案情重新研判；保留历史状态与文件，不绕过后台版本校验。
10项聚焦合同测试、TypeScript、变更文件lint及单进程生产构建通过，frontend更新。
真实数据库复核：12个历史文书包全部属于案件当前40版之前；唯一ACTIVE计划激活于32版，
最新候选计划基于36版，均不能直接作为当前40版交付。M2恢复候选仍只读，不作成果晋升。
后续完整运行受ADR-0079/0080“不创建M4或自动重试”限制；任何独立新模型验收需明确新的
单次授权边界，不凭连续执行指令改写该限制。建议同一既有供应商、仅合成资料、最多一次、
最高1.20元、结果不明不重发；此建议尚未执行，也不保证一次模型调用即可完成整案验收。
后续无付费工作：旧输入文书卡明确显示失效及重新研判路径，打开/下载函数同步拒绝旧输入；
分析读取与文书读取一样核对当前审阅epoch，防止切案或换版后的迟到响应覆盖当前分析。
TypeScript及变更文件lint通过，单进程构建部署复核；未新增模型请求或变更后台权限。

本阶段补充执行项：沿用 ADR-0003 视觉，先把首次交办改为业务任务预设、默认完成标准、
可展开限制及首页使用路径，再验证用户从交案到成果的实际操作。预设不授予外发、事实或提交权限。
任务空待办已按真实状态区分失败、等待输入、对账、暂停、取消、失效、待复核与完成，
不再对失败任务统一宣称“没有阻断运行的事项”；不虚构后台自动修复或律师批准可解决技术故障。
3项状态文案回归、TypeScript检查通过。正文修改链确认已有可选消费者接线，但部署默认未启用；
未因结构预检而直接开启。实际合成身份调用一份历史审阅意见读取被当前授权/成果校验拒绝，
临时会话已撤销；不能把12份历史包当成当前可编辑文书。没有新模型、修改提案或生成任务。
已实现首页四步说明及材料入口、风险/证据/应诉方案三个可编辑预设，替换终审恢复的技术按钮用语；
10项预设/既有终审合同测试、TypeScript 与变更文件 lint 检查通过。单进程生产构建成功，
现有 frontend 已重启读取新构建；这仍是本地验收覆盖层，不是不可变商用镜像。
内置浏览器再次打开仍超时，故不声称首次使用/视觉验收通过；整案模型与交付尚未完成。

需求：暂停参考下载中的真实律师材料，以用户能在 Web 发起合成整案任务、查看来源绑定风险分析、
下载可编辑文书为本阶段验收；不以 OCR 局部成功或测试数量替代整案可用，不扩大到新功能。

执行顺序：①恢复同版本常驻 Agent；②打通实际浏览器入口；③完成合成案任务到风险与文书交付；
④复核下载、来源和失败提示。法律立场及最终提交仍须律师批准，不制造新历史运行或重发封存调用。

已完成①：本地验收 overlay 的 API/Worker 统一只读挂载完整 case_api/case_kernel，替代 Worker
旧的单文件混装；对应装配回归通过。实际完整生产组合根（含文书）启动预检通过。
启动前实查：两条非 QUIET 队列仅为 FAILED 与 REQUEST_FINAL_REVIEW，无待刷新快照，六条
文书修订队列均 QUIET。已启动 restart:unless-stopped 常驻 Worker，真实心跳有效，队列收敛 QUIET，
历史状态仍为 FAILED 3 / WAITING_INPUT 13 / RECONCILIATION_REQUIRED 2，未把失败改为成功。

当前②已进入案件首页：经用户对两域名例外的明确授权，Wi-Fi 原有代理例外完整保留，
仅追加 workbench.127.0.0.1.nip.io 和 identity.127.0.0.1.nip.io；代理端口、TLS及认证配置未改。
Chrome 实际显示案件列表并选入合成计算验收案，看到原FAILED任务与独立恢复候选入口。
此部署实查仍使用既有合成验收身份，页面“已验证登录”不作为真实OIDC/MFA验收证据。
浏览器揭示新的接线错误：常驻Worker报告case-agent-planning-router-v1，Web就绪检查却只认deepseek。
已将Web精确身份引用生产路由器常量，未放宽其他身份、权限、目录、策略和心跳门；
11项聚焦测试通过，包含拒绝旧供应商身份；API已重启，真实Web组合根读取当前心跳、
角色及目录后，agent_ready / ledger_ready / document_ready均为True（修复前agent_ready为False）。
复核时验收标签已不在Chrome标签列表，未重开或改动其他页面，尚无修复后浏览器任务执行证据。
③④未完成，下一单一目标：让现有案件页正确识别可执行服务并完成来源/成果查看，不重复OCR调用。

本轮整案成果读取：通过实际Web成果服务及短期合成验收身份，读取M2既有恢复候选
fc351603-2f2f-5bc5-80ce-e8f0db43f773成功；返回争点与证据风险、对方主张与反制、
策略取舍、补充材料、律师行动与决定等9节30项，业务项保留来源绑定。临时会话已撤销，
没有新模型调用、事实批准、文书生成或历史运行改写。此为审阅候选，不是正常整案完成，
更不是可下载应诉文书。内置浏览器工具超时；独立Chrome窗口操作遇到用户切回其他窗口，
停止争抢前台，不把后台服务读取当作浏览器验收。

## 2026-09-05 历史事实记录

2026-09-07 OCR合同与可读投影修复（ADR-0097）：新增实际适配器字节→独立校验器跨模块测试，
修复前对扫描页/JPEG均复现同一格式拒绝；新生成器撤回误加到v1根节点的费用字段，
六分保守费用仍在执行回执及费用策略中，不是实付账单。未放宽校验器/修改已应用迁移或策略哈希。
Web仅将受限HTML转为纯字符串，未知/活动标签保留字面原文；长文分段而非截尾，系统占位置信度
不再冒充模型概率，并明确全文完整性待核。原始候选、失败事件及源页未修改，没有模型调用。
本轮95项适配器/独立校验/Web投影局部回归通过，未证明既有失败候选可从律师Web入口打开，
也未完成浏览器/律师验收。
后续只读恢复已实跑：解释器对确切费用字段错配分离副本费用、复用严格视觉校验，拒绝其他损坏；
真实数据库当前版本/图/成功OCR回执和私有3020字节原候选核对一致，生成3754字节内存恢复草稿，
SHA-256 01cb917b8ae048ad39d1e540195f15d26edb31a1ba8af8874e3758e058cb5065。
原run仍FAILED/10，纯Web投影已得到识别文字及全文完整性待核提示；无模型/数据库/对象写入，
尚无持久恢复件，不是律师授权入口或浏览器验收。现有封存恢复存储只允许决策包，不冒用其类型。
追加存储代码现已实现：0086独立恢复元数据表、原成果+策略唯一键、角色RLS和不可修改守卫，
PostgresVisualRecoveryStore私有对象读回后再次锁定核对来源/版本，普通事件与验证台账不写。
9项解释/存储局部测试通过。实际数据库worker角色回滚探针证实当前版本可插入、过期版本拒绝；
探针是虚拟对象键且已完整回滚，0086未部署，仍无持久恢复件或真实律师读取证明。
0086随后已规范部署：85项旧迁移摘要一致；停止原API/frontend/gateway后备份
artifacts/lawcase-before-0086.dump，5923722字节/0600/已忽略，摘要
2f4b0ef6d02e497ad4f5252c0d6a15e6be9639d0428037ce3a74a69e09e55570；目录可读，此精确备份未还原。
规范postgres-migrate/分离assert通过86项迁移及TLS/角色/RLS/身份隔离检查，schema_owner为NOLOGIN/NOSUPERUSER。
原API/frontend/gateway启动恢复，前两者healthy，未新建服务栈或镜像。
实际新Worker进程通过存储入口读回原件并持久化恢复件bcffde34-6ab6-5b6a-865e-a22615ea8afc，
3754字节和上述恢复摘要一致；原状态FAILED/10不变，成功OCR回执1/外发数1，无新模型或任务分派。
实际Web数据库角色下，当前主办可见恢复元数据1行，无案件身份可见0行；此不是OIDC浏览器验收。
律师读取和列表入口现已接线：read_review仅在确切恢复ID下走独立读取，重新读原件并推导预期恢复字节，
核对实际恢复对象后再查当前权限/版本；不签发普通验证回执，不开放事实纠正入口。
实际合成身份调用返回“图片识别恢复件（待复核）”、113字符纯正文、完整性提示及确切原证据页；
控制服务实际列表同时保留恢复件READY_FOR_REVIEW/recovery_only和原件CANDIDATE，原任务仍失败。
临时合成会话均已撤销，38项读取/列表/控制局部测试通过，API重启后healthy；非真实OIDC/MFA验收。
本轮浏览器实际打开工作台失败，超时后核对错误页为ERR_CONNECTION_CLOSED，未进入登录或案件页面；
未反复请求登录、未更改系统代理或证书，不能把服务入口成功当成浏览器验收。
本轮连通性对照已定位代理路径：scutil当前HTTP/HTTPS/SOCKS均指向127.0.0.1:7990，
例外列表未包含工作台和身份两个nip.io主机。使用项目CA且不关闭证书校验，工作台直接连接
HTTP200/TLS校验0；身份OIDC发现文档直接连接同为200/校验0。显式经过同一代理，工作台为
HTTP000/SSL_ERROR_SYSCALL，与浏览器ERR_CONNECTION_CLOSED一致。证明直接服务可用及代理路径失败，
不证明代理内部具体规则或远端故障原因。API同时healthy，无需重做账号、证书或模型测试。
下一单一目标：经授权仅为两个本机测试域名增加系统代理直连例外，保留其他代理/认证/TLS设置，
再完成浏览器恢复件与原页查看；该系统网络设置变更超出项目目录，当前尚未执行。

2026-09-07指定借条真实OCR首次成功、独立校验未通过：当次重核百炼官方qwen3.5-ocr北京原价
输入0.5/输出2元每百万tokens及49152/16384上限，6分保守预留仍适用，非实际账单。
真实Web控制服务对唯一指定任务作合成执行授权，run5→6，非事实/法律/文书批准；临时会话撤销。
审批意图/回执归档私有artifacts/lawcase-visible-loan-approval；Worker单次分派意图/结果/验证回执
归档artifacts/lawcase-visible-loan-ocr。未重新规划，任务无自动重试。
任务49ea9106-e3a2-56cd-bde1-bf7ac68b5fc5、attempt193b8693-f6f6-5036-a96f-03a8f3962dba
实际TASK_SUCCEEDED/8，24秒、external_calls=1、cost_minor_units=6，外请求ab1a6ec6-66de-581e-aa2a-679eff911482。
产生3020字节VISUAL_PAGE_REVIEW_CANDIDATE 2ffeeb3b-f456-5a76-93af-f6324a3d3122，
SHA-256 71ef29c0e3021c148359d17c5a8fc27bc8ac3f2401587f27df6dcbe894516f33。
独立验证单独执行一次为VERIFICATION_FAILED/10，原因ARTIFACT_VISUAL_CONTRACT_INVALID；
实查识别端v1候选新增cost_basis/cost_reserve_minor_units，而校验端v1精确字段表未同步，是合同漂移，
不得删原候选字段或覆写失败记录。没有追加OCR请求，候选尚不能通过律师Web入口放行。
开发诊断用实际Web对象读取器读回原候选，重算3020字节及上述摘要；此读取不冒充律师已获准阅览。
与原图目视对照：300,000.00元、周建国/王强、银行转账、月息2分、12个月、2019-06-03至2020-06-02
及落款一致；响应是带代码围栏的HTML，漏掉顶部“全合成测试材料·非真实案件·禁止提交”警示。
因此仅证明核心借条正文识别，不证明完整逐字转录；不得静默补入模型未返回文字。候选confidence=0.5、
各正式标志false，尚无事实入账/规则批准/正式计算/律师认可。
下一单一目标：版本化对齐OCR候选费用合同和独立校验，安全显示HTML来源文本并明确遗漏/待核状态，
通过保留原响应的本地恢复闭合律师可读候选，不再次请求模型或将验证失败改写为历史成功。

2026-09-07原库0084/0085已部署且原运行已恢复：核对83项旧迁移摘要一致，停止API/frontend/gateway后
生成私有备份artifacts/lawcase-before-0084-0085.dump，5902412字节，0600且Git忽略，
SHA-256 f13bd7c0fe8819ab57c585ffeccbdc92ea42258aedc17f7c7c265d102d70ec6e；目录可读，
此精确备份未还原，旧函数缺陷恢复须遵循ADR-0096。Docker cp不能读tmpfs，改为确切文件tar流归档。
规范postgres-migrate应用84/85，postgres-assert报告85项迁移、TLS、角色、RLS和Keycloak隔离PASS，
schema_owner独立确认NOLOGIN/NOSUPERUSER。Compose start遇旧依赖缺trust-bundle-bootstrap，
改为启动原有API/frontend/gateway容器，API/frontend独立检查healthy；不是干净不可变发布镜像验收。
恢复脚本首次因Worker无权读迁移账本在写前退出，改为读取pg_catalog实际守卫/3个固定搜索路径，
未增账号权限；随后实际Web身份只追加material-scope-review-v1事件，同键恢复一致，新Worker成功
PLANNED/5。脚本结果当时标签仍为clone_recovery_result，但实际DSN为原lawcase，独立原库证实；
标签现改为recovery_result+isolated_clone，不改既有记录。
原run 781a290d-7588-5e12-93a0-6e7545cd5ea3现WAITING_APPROVAL/5，事件1/2/3保持，
新增4 PLANNING_MATERIAL_SCOPE_REVIEWED、5 TASK_GRAPH_ACCEPTED，图
aec1c00289fedf1b5e91833be44f9c2bc0a48aadafcb4b746e85f673d8f33736。
规划交换仍唯一SUBMISSION_STARTED及SUCCEEDED，OCR交换0，没有新模型请求、任务分派或事实/法律批准。
下一单一目标：核对唯一借条OCR任务的当前来源/外发批准与费用上限，经合成测试授权后只执行该任务一次，
检查实际返回文字与原图，不把恢复成功当成OCR/全案交付完成。

2026-09-07隔离副本完整应用恢复已实跑：临时HBA仅固定副本、现有Web/Worker/verifier三个账号、
已核实内部data网段172.20.0.0/16、hostssl+SCRAM；其余规则不变。运行结束已删除规则并reload，
pg_hba_file_rules独立确认固定副本规则0。没有关闭TLS/密码认证或开放管理员TCP。
实际Web数据库身份专用review_retained_material_scope成功写事件4，同幂等请求读回一致；新Worker进程
初次恢复暴露记忆读取只认原目标摘要。现仅对明确局部范围从唯一追加复核核对原/有效目标、来源、版本、
容量，保留原goal行及当前owner/worker角色校验；8项记忆持久层测试通过，含缺复核/错误摘要/越界拒绝。
随后独立新进程--clone-resume只消费既有事件4，返回PLANNED/event_version=5/
PLANNER_RESULT_RECOVERED_AFTER_RESTART，没有再次写复核或调用规划器。
独立数据库核对：副本WAITING_APPROVAL/5，任务图1；原库WAITING_INPUT/3，任务图0；
两库规划外交换均SUBMISSION_STARTED=1、SUCCEEDED=1，OCR交换均0。证明恢复链到受控待批准图，
不证明OCR识别/律师审批或全案交付。原库尚未部署0084/0085，隔离副本保留供审计且TCP例外已撤回。
下一单一目标：按备份/旧迁移摘要/停消费者/规范迁移预检部署0084和0085到原库，
为原运行追加同样的范围复核并零新规划恢复，再进入确切页OCR授权/一次执行，不扩展到整案重跑。

2026-09-07隔离恢复副本与真实备份缺陷：原数据库51MB、宿主余量6.5GiB，未启动额外服务，
在既有PostgreSQL创建固定lawcase_scope_review_probe_20260907测试副本。真实pg_restore先后发现
0047 source_policy/risk_policy辅助函数和文书source_refs_hash的digest依赖未固定search_path，
空恢复搜索路径下数据COPY失败。核对全部7个未配置搜索路径的public SQL函数，新增ADR-0096/0085
仅固定上述3个非内置依赖函数为pg_catalog,public，原已应用迁移不改。
两份不完整测试副本经核对无连接后删除重建，仅删除测试拷贝，原库和对象不变、可从原库重建。
最新副本已按pre-data→0085→data→post-data完整恢复并装入0084；仅副本有这两项结构变更，
原库仍83项部署。副本未修改迁移账本以冒充规范部署，也不是全系统/对象存储灾备验收。
原库与副本均读回目标WAITING_INPUT/event_version=3。
diagnose_visible_loan_planning.py新增--clone-recovery严格固定副本DSN，准备执行应用复核幂等和
新组合根留存恢复，规划器设为禁止调用。实际启动在现有pg_hba.conf数据库白名单处被拒绝，
尚未进入复核/模型/任务执行；未更改HBA、关闭TLS认证或放宽权限。副本当前保留供续测。
下一单一目标：为固定隔离副本建立与原库同等级、仅现有三角色的受限测试连接，再跑应用恢复事务；
0084/0085仍须正式备份迁移流程部署到原库，OCR、正式计算及整案交付未完成。

2026-09-07数据库0084实现与局部真实回滚验证：新增复核事件许可及独立触发器，按run→matter→attempt
锁顺序核对Web当前主办/复核人、未接受初始失败状态、当前版本、唯一复核、确切来源集、输出上限，
重算原提案/有效目标/派生提案摘要；任务图接收分支绑定Worker角色/当前版本/复核图摘要并重算图内容。
初次DDL事务因账号名误写失败，按实际lawcase_agent_worker修正；真实事件触发进一步发现两处JSON运算符
优先级错误，均已修正且错误事务自动回滚。没有创建新账号或扩大现有表读写权限。
check_material_scope_migration.py在真实PostgreSQL事务中SET LOCAL ROLE lawcase_web_application，
读取固定合成案真实检查点/成功响应：篡改派生摘要按预期拒绝，正确复核事件插入通过，随后ROLLBACK。
独立查询事件0、迁移函数不存在；当前0084未部署。脚本使用占位事件哈希，仅证明SQL触发合同，
不证明应用事件摘要/检查点/幂等恢复或真正律师批准。新增Worker权限仅是已有纯JSON规范化函数EXECUTE。
本轮无模型调用、OCR、持久业务变更或服务重启。任务图接收分支及完整应用事务未实跑，不能正式放行。
下一单一目标：以真实编译图和应用事件序列完成0084任务图/事务回滚验收，确认不会新增规划交换，
再按备份、迁移、权限预检流程正式部署并恢复原运行。

2026-09-07范围复核后台恢复接线：PostgresCaseAgentStore增加只读唯一复核事件读取与专用
review_retained_material_scope事务入口，复用现有当前权限/版本锁/幂等回执，写前重编核对。
Worker只对原提案目标与当前局部目标不同的恢复路径读取复核记录，绑定请求/原响应/派生摘要，
返回派生提案但不改原响应；首图身份使用复核编译所用run UUID，接收前再核对已批准图摘要。
缺原成功响应或复核不匹配时无lookup/新规划回退，正常原目标恢复路径保持原有逻辑。
117项Worker桥接/Worker/持久层/状态机/规划器测试通过，新增模拟持久Store的留存恢复、原响应保持、
错误请求/派生摘要/缺复核拒绝及UNKNOWN禁止lookup测试；此非实际进程中断或数据库恢复验收。
数据库仍未许可PLANNING_MATERIAL_SCOPE_REVIEWED，SQL独立守卫0084待实现，不开放HTTP复核入口。
本轮没有持久化事件、修改原运行、重启服务或模型请求；OCR仍未执行。
下一单一目标：实现并事务回滚验证0084的复核事件/任务图独立守卫，再按备份迁移流程部署，
以实际原成功响应完成一次追加复核和零新规划恢复；不得跳过数据库层验证直接分派。

2026-09-07追加式范围复核合同实现：新增PLANNING_MATERIAL_SCOPE_REVIEWED强类型事件与严格读写序列化，
绑定原目标/提案、请求、快照、指定来源、原/新输出预算、有效目标、派生提案、任务图及复核人。
重放只接受初始规划失败、无图/无任务/非失效/非取消状态；保留旧事件，重建有效目标与输出预算，其他预算不变。
普通人类/Worker/审批事件写授权均未加入该事件，数据库仍未许可，不可通过普通继续运行启动。
新增只读事务内核对函数_assert_retained_material_scope_review，复用最新成功交换的身份/来源/摘要证明，
重新编译并逐一比较有效目标、派生提案、任务图及确切所需容量；任意过量输出批准亦拒绝。
70项规划器/状态机/持久层单元测试通过，包括事件JSON往返、旧状态不变、重复/篡改/越界拒绝，
派生图守卫测试隔离了既有数据库成功交换核对，不能称真实事务写入或恢复已通过。
没有新数据库迁移、事件持久化、模型请求、OCR分派或用户验收。当前阶段仍内核IMPLEMENTED。
下一单一目标：专用事务写端/数据库0084守卫及Worker留存派生方案消费同时闭合后，才部署并恢复原运行；
不得先开放写入口让PLANNING状态走回新模型请求。整案计算、文书和真实浏览器验收仍未完成。

2026-09-07留存提案局部范围复核候选已真实编译：prepare_retained_material_review保留原目标/
原提案摘要，派生目标关联另记摘要；任务内容及原输入快照不改，不伪造新模型响应。
只有output维度不足时按编译所需增加容量，候选硬上限64MiB，其他预算不变；该对象不是批准或执行权。
23项规划器测试通过，包含原响应保持、来源范围、摘要变更、容量不足及费用不足拒绝。
真实Worker身份读取现存run 781a290d-7588-5e12-93a0-6e7545cd5ea3唯一成功响应及当前版本20快照，
只读--review-candidate正常退出：原提案b7d19118ef5dfdac6991c3ed40bfdbe85e0d9a91297ef7aab8d75ebf5da52cfa，
派生提案4dacf4373dbfc4d960b41188bd29f7c8b9c938ee61addd055d475be58799f043，
图aec1c00289fedf1b5e91833be44f9c2bc0a48aadafcb4b746e85f673d8f33736。
实际只编译1个image_visual_ocr，输入仅0ed71e7c-8f9e-46e2-b3e9-ca382d9f0fc7，任务6分/单次外发/
120秒/32MiB；整运行仅输出容量假设8→32MiB，1元/一次外发/660秒保持。额外法源任务不再出现。
原运行仍WAITING_INPUT/event_version=3，review_approved=false，无业务写入、模型调用或OCR执行。
本阶段复核候选PROVEN仅指此数据库来源的本地编译，不是正常恢复或整案完成；创建/恢复写入口仍未开放。
下一单一目标：为范围复核加入绑定原目标/成功交换/派生摘要的追加事件与数据库守卫，
在当前角色/快照/幂等校验后允许Worker只消费该派生候选，禁止退回新模型调用；随后执行确切页OCR。

2026-09-07局部材料范围内核实现（ADR-0095）：AgentGoal新增非空时启用的material_read_refs，
只接受排序去重、最多200项的证据页/材料对象UUID引用并纳入v3目标摘要；默认空值保持v1/v2序列化。
局部范围不能同时请求文书或活动计划执行；编译门只接受pdf_reading/office_reading/image_visual_ocr，
要求准确覆盖指定来源，拒绝越界来源/研究技能/整案重提取义务，仍沿用来源与外发审批检查。
仅该明确范围不插入法源研究任务，整案法律缺口保持原约束，不从目标关键词猜测。
规划器/状态机/Worker的60项测试通过，包括局部读取完整编译、越界拒绝、摘要篡改拒绝、旧目标读回、
文书混用拒绝及同样文字的普通整案仍须研究义务；另26项持久层单元测试通过，非真实数据库迁移验收。
测试中两处夹具错误已修正，非运行时放宽。
本阶段为内核IMPLEMENTED，不是Web可用或实际恢复：创建服务/API、规划精确快照和旧运行追加式复核仍待接线，
没有数据库迁移、运行/目标变更、模型请求、OCR分派或律师验收；原失败运行不得原地套用新范围。
下一单一目标：闭合服务器授权的范围确认与留存提案恢复合同，保留原目标/原提案摘要及历史，
再使既有提案在明确局部范围和确切容量预算下进入受控OCR，不重发规划。

2026-09-07留存规划真实只读诊断：新增diagnose_visible_loan_planning.py，通过实际Worker身份读取
run 781a290d-7588-5e12-93a0-6e7545cd5ea3的唯一SUCCEEDED提案及当前版本20快照，
实际重放确认WAITING_INPUT/event_version=3。当前编译器抛出output预算不足，required=37748736
（36MiB）、available=8388608（8MiB）、task_count=2，并非此前静态估计的单任务32MiB。
仅在内存中将输出预算改为36MiB，实际编译通过：OCR绑定唯一指定证据页、预算6分/单次外发/禁止自动重试；
但编译器还强制插入4MiB的legal_rule_research_planning，读取代理情境及三笔交易，network=DENY。
这揭示局部“只OCR、不研究”目标与整案法源义务自动插入的范围冲突；不能只扩预算后分派。
两次诊断进程均已退出，无process_run_once、无模型调用、无业务写入或任务分派；首次因预期32MiB断言退出，
第二次输出实际诊断和假设任务图。未修改原始预算、失败事件或供应商回执。
下一单一目标：明确并实现局部材料读取与整案法源义务的强类型执行范围，保留整案必要法源约束，
然后沿只追加的预算复核恢复已有提案；不能用自然语言关键词绕过义务，不能重新请求模型掩盖配置问题。
源图实际OCR、正式计算、完整用户旅程与商用交付仍未完成。

2026-09-07首次可见借条规划真实执行：经实际Web控制服务创建run
781a290d-7588-5e12-93a0-6e7545cd5ea3，绑定案件ad70242f-0a41-540b-a125-d4f809f0309e版本20，
目标仅识别页0ed71e7c-8f9e-46e2-b3e9-ca382d9f0fc7，不研究/计算/起草。固定单次规划脚本
run_visible_loan_planning.py限制4096输出tokens、60秒、请求哈希不变且只允许一次plan调用，未循环队列。
预检请求17401字节，按两倍字节加4096输入余量、官方DeepSeek高峰9/27元每百万tokens及OCR6分预留，
保守估算0.520674元，低于本次1元预算；此非实际token数或供应商账单。
数据库显示唯一SUBMISSION_STARTED及SUCCEEDED规划交换，封存提案只选image_visual_ocr和唯一正确页，
但本地编译为PLANNER_PROPOSAL_REJECTED，当前WAITING_INPUT，OCR交换0。没有重发模型或分派OCR。
静态核对发现目标max_output_bytes=8MiB小于服务器OCR任务32MiB，另有Worker编译器仍设费用0，
后者现改为共用6分常量，18项Worker运行时测试通过；这不是已有失败运行自动恢复。
创建回执/规划预检与结果均归档私有artifacts/lawcase-visible-loan-run与lawcase-visible-loan-planning。
下一单一目标：对已封存成功提案执行本地编译诊断与显式预算恢复；不得生成新模型请求来掩盖本地配置错误，
不得原地改写RUN_CREATED、任务历史或供应商回执。源图识别、法律规则和正式计算仍未完成。

2026-09-07 OCR费用迁移已部署：先逐一核对82项已应用迁移SHA-256一致且状态APPLIED，
停止API/frontend/gateway，保存artifacts/lawcase-before-0083.dump（0600、Git忽略，5897313字节，
SHA-256 47ba1f5cf19c0c2233db6d5c48bd1ce8868cfe7635fd7817b7d422993bbc133c，目录可读，非还原演练）。
规范postgres-migrate唯一应用0083；postgres-assert报告83项迁移、TLS、角色、RLS、种子及Keycloak隔离PASS。
独立读取schema_owner仍NOLOGIN/NOSUPERUSER；原API/frontend/gateway已恢复，API/frontend健康。
一次性Worker以真实身份和新源码组合完成前置检查，输出OCR_COST_POLICY_READY_NO_DISPATCH及b680384c…新哈希，
文书交付仅在该探针关闭，未运行消费者、创建Agent run或调用模型，结束自动移除。
运行仍为本地源码覆盖验收环境，不是不可变商用发布镜像；前端未重新构建，UI能力清单一致性仍需核对。
当前隔离计算案仍以版本20为后续起点；API重启会清空容器tmpfs，恢复必须使用已归档私有回执，不得重做已完成动作。
下一单一目标：建立确切借条页的有界识别任务，并校验编译后的费用预算/来源授权，随后仅分派该运行。
费用未知占用/供应商实际账单、旧策略恢复兼容、真实浏览器与全案计算交付仍未验收。

2026-09-07 OCR费用门实现（ADR-0094，未部署）：官方qwen3.5-ocr北京模型文档现列输入0.5元/百万、
输出2元/百万，最大输入49152/输出16384；全上限费用0.057344元，保守预留6分。
适配器在来源绑定/提交前要求整数预算至少6分；成功、已知失败和恢复终态按6分记预算占用，非供应商账单。
价格依据/币种/上下限/预留进入新策略哈希b680384ca766966a4e811bc9e6acd48c34844cebf1c1f3735c24b33d2abdae8f，
0083同步数据库策略并要求任务max_cost_minor_units>=6。12项适配器与3项迁移合同测试通过。
真实PostgreSQL事务内执行0083函数替换、新哈希读取为true，随后ROLLBACK；独立读取旧0059哈希仍在，
证明SQL可执行且回滚，不证明真实交换/预算并发或新迁移已部署。没有新模型调用、业务写入或API重启。
下一单一目标：在规范迁移/备份流程部署0083并校准OCR任务预算与Web/Worker策略目录，再执行一次确切借条识别。
实际供应商用量/账单对账、未知请求预算占用、价格变更复核和完整案件交付仍未验收，不能称商用费用控制已完整。

2026-09-07视觉任务准备：Web ExactCaseAgentRuntimeReadiness使用允许的目录组合而非固定无OCR目录，
但没有常驻Worker就不能报告实时可执行。发现qwen_visual_ocr_adapter成功与已知失败回执均写cost_minor_units=0，
尚未证明任务预算可约束实际供应商费用，不能据max_cost_minor_units字段宣称真实金额上限已生效。
未创建或分派新Agent run，也未外发模型。实际完成同一隔离合成案的模拟一审被告代理情境登记，
版本15→20，profile_id=216a758b-9ebd-45e8-8411-b607d3b1cfcf；只是测试委托情境，不新增证据事实或法律批准。
第一次请求因幂等键包含冒号在写前被服务拒绝，保留旧意图；按实际键规范改为连字符后唯一成功，
新意图和回执已归档私有检查点，临时会话均finally撤销。独立数据库确认版本20、Agent run仍0。
下一单一目标：核对OCR请求/响应的用量与价格合同，补齐可执行的费用预检或保守成本记账，再启动此确切页的受控识别。
不得把人工查看新图片、登记代理情境或空Agent列表称为模型理解已完成；历史法源、正式计算和整案交付仍未通过。

2026-09-07视觉识别部署接线修复：发现规范Compose的case-agent-worker未传入Qwen OCR三项参数，
而示例配置与Worker代码均已有支持。现补传既有OCR key/workspace及镜像内/usr/bin/pdftoppm；
真实Compose解析仅输出三项非空布尔值，无凭据泄露。7项管理入口测试通过，新增防部署遗漏断言。
一次性Worker使用实际Worker/独立验证数据库身份与既有镜像，在本地只读源码覆盖下完成
compose_production_case_agent_worker装配及数据库前置检查，输出OCR_WORKER_COMPOSITION_READY_NO_RUN_DISPATCH_NO_PROVIDER_CALL。
首次启动探针因遗漏planning_repository参数退出，未分派运行；修正后第二次正常退出并自动移除。
文书交付在该探针中显式关闭，未启动Worker循环、创建Agent运行或调用供应商，不证明OCR已识别借条，
也不是完整文书组合或不可变发布镜像验收。下一单一目标：核对Web/Worker能力目录一致性，
为版本15案件的确切借条页准备有费用上限、来源授权和恢复状态的视觉任务，再执行一次受控识别。

2026-09-07公报采集与借条前提修正：独立固定公报URL
https://gongbao.court.gov.cn/Details/48786dea74c9545c2f4fb27254ca08.html 实际返回HTTP502，
没有法源登记或规则审批。新意图与旧请求分离，未重复旧请求，临时会话已撤销。
视觉检查发现未辅助v1借条JPEG只有英文摘要及空框，没有中文正文；此前无EXIF检查不足以证明真实读卷输入。
golden_case_source现将新未辅助草稿版本升为v2，绘制可见中文借条、借贷双方、日期、月息及期限，
显著标注全合成/签名示意，不带答案元数据；F2仍字节相同，F3两张仅角度/裁剪不同且保留完整正文。
历史带辅助生成路径、冻结规格、已入卷原件和封存运行均不覆盖。7项来源测试通过，新增正文区域非空检查；
F2和F3裁剪稿已实际目视核对。此为干净排版的合成图片，不证明手写、模糊、低照度或真实手机照片能力。
F2新原件8f276d79067e7f8d6cb34f98608f486d9cbfb9d670874f17dfc545aef36d7d4b已通过真实通用入卷服务
唯一入卷当前计算案，107934字节，证据页0ed71e7c-8f9e-46e2-b3e9-ca382d9f0fc7，版本14→15。
回执VISUAL_OCR/NEEDS_LAWYER_REVIEW/AGENT_READY，所有正式事实/交易/法律结论/提交标志false；
没有Agent run或模型调用。新回执已归档现有私有检查点。不得再使用固定版本14的历史采集脚本盲目续跑。
下一单一目标：按既有受控视觉识别链预检此确切页的授权、调用预算与恢复状态，然后提取条款候选；
历史法源下载仍未解决，正式计算不允许绕过，完整47笔场景和商业用户旅程仍未验收。

2026-09-07金额法源采集未通过：现存两份官方快照均为CN-CIVIL-CODE-680，不能仅据此给2019年交易
批准适用规则。已核对最高法院2015年原始司法解释页面https://www.court.gov.cn/zixun/xiangqing/15146.html，
但实际API容器一次受控GET虽返回200/text-html，生产解析器拒绝为official source has no readable text。
未登记法源对象/快照、未批准规则、未计算；独立DB查询案件仍14，新source_id记录0，临时会话finally撤销。
该结果不能断言网站反爬或解析器错误；原脚本解析前未归档响应，首份响应不可恢复，不能用搜索工具正文代替。
脚本已改为解析前保存原始响应与下载摘要，解析拒绝时记录独立失败回执；该修复尚未再次实跑。
固定采集意图已归档现有私有检查点；没有删除意图重试。下一步通过明确的官方公报备用页面取得可认证原文，
保留独立来源身份与采集回执，再结合借条和新旧法期间核对规则，禁止为跑通直接设置利率。

2026-09-07隔离计算输入已真实落库：run_synthetic_calculation_intake.py仅接受固定合成银行PDF
8cff8981ecbd3293e2941004bee830d514dbc4e7a72e295a4b92a88a61b06c78，通过实际Web入卷服务
建立案件ad70242f-0a41-540b-a125-d4f809f0309e，10页原件、3笔CONFIRMED交易、3项APPROVED付款定性，
最终版本14，真实数据库独立读回一致。所有决定明确为SIMULATED_TEST_DECISION_NOT_LAWYER_APPROVAL；
案件标题标注全合成、模拟决定、禁止提交。第3/4页两笔借款分别分配loan-A/loan-B，第5页摘要为空的回款
仅按测试假设分配loan-A及REPAYMENT_UNSPECIFIED，不声称流水证明了法律归属或利息/本金性质。
原件相关页已文本/渲染核对；通过已登记页UUID和原件摘要绑定交易，不直接写APPROVED数据库行。
没有模型调用、规则批准、正式计算或提交包；临时会话finally撤销，进程正常退出，旧封存案件不变。
18项意图/阶段回执已归档artifacts/lawcase-synthetic-calculation-intake（私有目录、Git忽略）；
Docker cp无法读取容器tmpfs，改用容器内只读tar归档，未重跑入卷。固定脚本遇既有检查点/案件会停止。
下一单一目标：续此版本14案件，为loan-A建立有明确适用来源与条件的金额规则审批并完成实际确定性计算，
随后纳入第二笔借款和上游失效验证。该三笔局部基线不替代完整47笔合成案、Agent组织能力或律师商用验收。

2026-09-07正式计算前提纠正（ADR-0093）：实际7条法源包规则关联均为
PRIVATE_LENDING_RESPONSE_SOURCE_SCOPE；既有验收脚本仅授权来源审阅，却以NO_INTEREST承载，
不能作为零利息金额规则复用。正式计算入口现拒绝含该历史用途的包，包括混合包，保留合法的零利率规则。
4项聚焦测试通过；真实Web数据库身份调用实际计算服务，既有APPROVED包在交易读取/计算写入前
被source-review-only门拒绝，临时会话已撤销。实查正式计算仍为0、探针命令回执0。
API已重启且healthy，仍为本机源码覆盖环境，非发布镜像。这不是正式计算成功或完整法源用途治理。
下一单一目标仍为隔离合成案的真实来源→交易确认/付款定性→金额规则审批→正式计算持久化，
不得复用上述审阅占位包、改写封存案或直接插入APPROVED行。

2026-09-07事实纠正审批桥接：已补服务器端 `verify_for_fact_review`，对当前最新修改稿重新读取私有原件，
复核完整提案内容，并拒绝读取期间的撤权、版本变化、提案替换和来源变化。12项聚焦测试通过；实际受管API
一次性进程使用真实Web账号及原件读取器核验既有提案49d495f1-d866-4d3a-b6b6-b8139ea6251d，输出
LIVE_FACT_REVIEW_SOURCE_CHECK_PASS，案件版本11/修订1保持不变，临时合成会话已撤销。
未写正式事实、未批准、未调用模型、未开放新HTTP审批接口；这只是正式审批的来源核验段，不能记为整链完成。
同事务送审现已实现：0082新增既有事实的不可变纠正关联，submit_for_fact_review将候选、关联、审计、
版本和恢复回执原子保存；原提取入账与纠正入账互斥。真实PostgreSQL回滚探针已完成送审、同键恢复、
既有DISPUTED决定与文字篡改/删除拒绝，连同来源核验13项测试通过；结束实查案件仍为11、新列不存在。
0082已于随后规范迁移部署（82项迁移/角色/TLS/RLS检查通过）；升级前备份artifacts/lawcase-before-0082.dump
目录可读，非还原演练。接口/来源76项测试通过，送审POST与按原请求编号恢复GET已启用。
真实HTTPS仅送审一次既有合成修改稿，事实1f231fa9-d2f4-4855-9c58-3190e51b4a6f为CANDIDATE，
decision_hash/decided_by为空，案件11→12且仅一条回执；独立恢复一致，临时会话已退出，未调用模型。
网页送审/恢复现已接入：保存与送审独立操作，送审仅使用确切已保存、同版本提案；不确定响应保留独立请求编号，
只查询原回执。新增已送审关联读取接口，按案件角色读取实际事实状态，不依赖本浏览器保存的请求编号。
已送审稿只读，明确区分待审/确认/争议/否认/失效；来源版本变化、不同修订或读取版本不一致时禁止继续写入。
TypeScript、ESLint、聚焦接口测试及单worker生产构建通过；实际前端BUILD_ID=ilmMfDrsAnXkbCOOG7DBi。
API/前端已更新，HTTPS读回确认版本12、CANDIDATE及提案关联一致，会话退出204，首页HTTP200。
实际内置浏览器仍为ERR_CONNECTION_CLOSED，未到达应用；没有改代理/证书设置，不把HTTP200记为页面验收。
审批依据接线已更新并部署：事实投影返回原件名称/页码与纠正候选身份；审批事实行复用修改稿对照组件，
显示原候选、修改稿、理由与原始摘录。纠正事实在本版本来源对照加载成功前禁用决定按钮，重载失败或版本
变化会撤销页面已核对状态；这只是交互门，不能替代服务端批准前的原件/提案复核。
18项台账测试、67项接口测试、TypeScript、ESLint和单worker构建通过；真实Web账号投影及HTTPS /review
确认唯一关联原件定位和CANDIDATE，临时会话退出204。前端BUILD_ID=ZBwpeU6N-p_C2fV0vuzdX，首页HTTP200。
文书绑定代码已确认校验run.snapshot_matter_version=当前案件版本，但未把它视为审批后全链失效的运行证明。
服务端决定复核已实现并接入API：独立原件读取在事务锁外完成，以当前案件版本核验已送审稿，不误用保存时的旧版本；
正式写事务锁定案件后再核对当前角色、关联事实、提案头与完整内容。缺少复核器的旧适配器拒绝决定纠正事实，
普通事实维持既有路径；已完成请求先核对当前权限并返回原回执，不为恢复重读旧版本原件。
40项聚焦测试通过。真实Web账号/私有原件读取器在实际PostgreSQL执行DISPUTED测试决定，事务内12→13，
随后回滚，独立确认仍为12/CANDIDATE且测试回执为0；临时测试会话已撤销。API已重启接入，无新模型调用。
这不是律师实际批准，也不证明界面/断网/下游文书的完整用户旅程。
事实决定恢复已接入并部署：发送前在sessionStorage只保存不透明请求编号；结果不明时任何决定点击先查询原请求，
不生成新请求批准。错误保留事实列表及“查询上次事实决定”控件；服务器按当前案件/主办律师/确切事实读取
DECIDE_FACT回执，拒绝跨事实，未找到仅返回null，不触发写入。22项聚焦测试、类型/静态检查和单worker构建通过，
真实HTTPS查询先前已回滚的测试请求得到null/court_ready=false，临时会话退出204。没有新事实决定或模型调用。
下游实查修正了下一步前提：当前读卷合成案的calculation_runs/calculation_scenarios/submission_bundles均为0；
整个测试库calculation_runs与submission_bundles也均为0。另有12份文书候选，全部属于旧合成案
1317cc5c-eb51-4bfe-be90-8f00436915b0，当前案件40而候选快照25/27/30/32，不能作为当前有效成果基线。
真实Web账号抽查一份READY_FOR_REVIEW旧候选：文书入口授权无行、下载被阻断，临时会话已撤销；
这证明该旧件不能下载，不证明由某一次审批引起失效。新增入口current_matter.version=run.snapshot_matter_version
检查，避免仅依赖后台is_stale标志；13项文书服务测试通过，API已重启接入。
下一单一目标改为先用隔离合成案通过实际服务完成规则批准→确定性正式计算→持久化，建立非空的当前成果基线，
再执行上游变化与失效验证。不能继续用更新0行或旧候选拒收代替整链验收；浏览器连接/断网验收仍未完成。
浏览器旅程和商用验收仍未完成，不以以上源码/集成证据代替。

### 本机资源限制（用户最新说明，优先执行）

2026-09-06 授权更新：用户明确允许由助手启动 Docker，取代下文“不得自动启动”的限制；
仍按资源余量分阶段启动，不据此允许清空数据、重建已有案件或同时拉起完整服务栈。
本次已执行应用启动及 docker desktop start，后者报告 already running；引擎 info 查询超时，
不能将应用进程存在记作引擎就绪。启动前磁盘约 7.1GiB、系统空闲内存比例约 39%，不是运行稳定性保证。
同日已恢复：虚拟机日志确认 09-05 正常停止且实时无 virtualization 进程；官方 desktop restart 30 秒退出失败。
对核实的 Docker 残留发送 SIGTERM 后，7592/7627 仍不响应，复查无 VM 后仅对这两个残留 SIGKILL，再启动应用。
未删除锁文件、镜像、容器或卷。引擎 _ping=OK、server 29.2.1。原有 9 个容器按自身策略自动恢复，
clamd/API/keycloak/PostgreSQL/对象存储/frontend/document-renderer 显示 healthy；gateway/KES 为 Up，未据此证明业务。
未启动 Agent Worker 或模型请求。恢复后空闲内存比例约 30%、磁盘约 5.0GiB；不额外启动原生服务或构建镜像。
此前“扫描服务不可用”的障碍已有新状态，下一步只检查现有受管入口与版本，再继续无辅助标记合成材料入卷。
受管扫描实跑：新 F1 PDF 哈希 1e0933758a7b74d842e71b2e7485634b27056bb10ac86ef4c80d54b3c135ac1b，
通过 API 内真实 ClamAV 1.4.3/28113 扫描为 CLEAN，生产 PDF 读取器得到 8 页/2028 字符，读取后哈希不变。
临时容器文件自动清理，原件保留；没有 Web 入卷、案件创建或 Agent/模型请求。
四个部署源码文件 web_material_upload、web_material_upload_postgres、evidence_intake_worker、pdf_reading_worker
与宿主 SHA-256 完全一致，此局部入口无需先构建。API 仍含历史源码覆盖挂载，非干净可发布镜像。
下一目标：独立零模型入卷动作，保留幂等键和上传回执，不调用会预确认事实及触发 Agent 的旧整案验收 main。
独立入卷已实际完成：run_unassisted_material_intake.py 只接受固定 F1 哈希，先保存意图/目标/slot，再调用既有 Web 服务。
案件 767fda38-e3de-5a15-816f-510a686c7600，上传 8d0c4ce2-6ba2-4d8f-a4e3-8c3f8db3bcc5，
原件 3166c2ba-8abf-426d-a6d8-9271128c6970，8 页、案件版本 2，COMPLETED，数据库实际 attempt_count=1、Agent Run=0。
未预确认事实；使用既有 loopback 合成身份，不证明浏览器 OIDC。检查点已从容器 tmpfs 归档到 artifacts/lawcase-unassisted-intake。
收尾 session_authority.revoke 报 missing or invalid，而只读数据库显示该临时 session 仍活动；命令整体非零退出，不能误判入卷失败重传。
为清理本轮临时授权，管理员仅按上述精确 slot/matter 定位并撤销该会话，未改案件或上传；这不证明产品注销路径正确。
下一目标先修复并实际验证会话撤销的权限/可见性问题，再从已登记原件做页级授权回读，绝不重传此次成功文件。
ADR-0089/0079 已实现：UPDATE 的 WHERE/RETURNING 同样受 SELECT RLS 限制，0024 会话网关原本仅有
律所/token 摘要 SELECT，app.web_session_id 只在 UPDATE 策略出现；0048 的精确 SELECT 仅适用于其他所有者。
新策略只给 lawcase_web_session_gateway 精确 session UUID 行可见性，不扩大应用角色、列写权限或关闭 RLS。
真实 lawcase 数据库事务内生成两条临时会话：基线可见 0，新策略后精确可见 1，UPDATE 1，空选择器可见 0，
管理员核对只有目标被撤销；整个事务 ROLLBACK，无测试会话/策略残留。脚本 backend/tests/sql-runtime/session-revocation.sql。
此为真实权限验证，不是生产部署；0079 尚未应用。下一步经规范迁移流程部署并核对版本，再续已入卷原件读取。

2026-09-06 本机受管升级已完成：77 条历史迁移 SHA-256 全部一致且无 APPLYING；停止 API/frontend/gateway，
确认无剩余 lawcase 业务连接，保存 artifacts/lawcase-before-0078-0079.dump（0600、Git 忽略，约 5.4MB，
pg_restore 可读取目录；未做还原演练）。通过原 postgres-migrate 一次性入口依序应用 0078/0079，
postgres-assert 的 TLS、79 项迁移、角色、RLS、种子身份及 Keycloak 隔离检查通过；未构建或拉取镜像。
schema owner/recovery lock owner 均为 NOLOGIN、非超级用户，原应用容器已恢复，API/frontend healthy。
实际 WebSessionAuthority.issue→revoke 成功，数据库确认新临时会话 5174974c-88e1-497f-9116-12a1bb37bcb4 已撤销。
没有管理员代撤销、重传材料、创建 Agent 运行或模型调用。0078 的部署不等于正文修订业务验收；未开启消费者。
阶段复核：会话撤销修复已实际部署并经服务调用证明；浏览器 OIDC 旅程、律师整案使用及最终交付仍未验收。
对照商用执行计划仍为内部 Alpha，下一单一目标为授权回读上述已登记 F1 原件及页级来源，不重复入卷。

同日已完成 F1 私有原件授权回读：真实 Web 组合根签发短期合成身份，以 evidence snapshot 校验案件/页归属，
WebAgentEvidenceProjectionSource 经登记 locator 从私有对象库取回原件并调用生产 PDF 读取器。
8 页按页序得到 213/408/236/245/336/202/177/211 字符（合计 2028），逐页文字 SHA-256 重算一致，
原件 SHA-256 均为 1e0933758a7b74d842e71b2e7485634b27056bb10ac86ef4c80d54b3c135ac1b。
读前后案件版本均为 2、证据快照摘要一致；服务 finally 撤销会话成功，命令退出 0。
没有从宿主直接替代已登记原件，没有新增上传/事实确认/Agent 运行/模型请求。
这证明实际私有原件到页级 Agent 输入投影，不证明模型理解、OCR 或整案分析；不再重复此读取检查。
下一目标：在仅设定合成委托情境、不预置证据结论的条件下接通材料驱动规划及读卷任务，
核对任务真正消费登记页来源，而不是再次只生成代理档案摘要；外部调用仍须单独预算预检和恢复记录。

同日已通过真实 WebCasePostureService.confirm_complete_posture 为唯一合成读卷案件登记
王强（合成被告）/民间借贷/一审/一般授权/有效委托，幂等键 unassisted-synthetic-posture-v1，
案件版本 2→7，当前档案 0efa3aca-8d5d-4c77-8bb6-5d0d8632b2b6。仅设定验收的合成委托情境，
不代表真实律师确认，也未写入任何证据事实、诉请、争点、金额或法律结论；会话正常撤销。
随后复用现有 case-agent-worker 镜像配置，以一次性 Python 只读入口替换自动循环（--no-deps、--pull never），
真实 TLS 账号 current_user=lawcase_agent_worker，通过同一 REPEATABLE READ READ ONLY 事务读取当前快照和规划投影。
实际对象恰为 EVIDENCE_PAGE×8、POSTURE_PROFILE×1，posture_state=AVAILABLE，起止快照相同；进程退出 0并移除。
没有构建、启动长期 Worker、创建 Agent 运行或外部模型请求。此证明已登记材料进入真实 Worker 规划清单，
不证明模型已理解正文或选择了正确任务。下一目标为固定预算的单次材料驱动规划与任务执行，
先核对可用读卷/抽取适配器和预算，不复用只读代理档案的旧原生运行，不预填分析答案。

实际部署接线发现缺口并修复：规范 Compose 仍为旧 controlled-defence 预检，Worker 未传
LAWCASE_AGENT_WORKER_LEDGER_EXTRACTION_API_KEY/MODEL，导致 from_environment 不装配材料抽取凭据。
现按既有固定 deepseek-v4-pro 合同接入原已配置凭据，不新增供应商；预检改为 materials-configuration，
在 network_mode:none 内检查规划、抽取和研判配置，不新增探针模型调用，不将配置 PASS 当推理/余额证明。
既有 controlled-defence 模式保留用于历史运行。真实一次性 provider-preflight 三项配置 PASS；
真实 Worker 设置读取 ledger_extraction_configured=true，两个进程均退出并移除，无长期消费者或模型请求。
两项相关单元测试在 backend/.venv 通过（含缺凭据/模型不符拒绝）；首次系统 Python 因缺 psycopg 失败，
未据此改业务逻辑。当前配置修复不代表完整 Worker 已装配/运行，下一步完成单次规划预算与目标运行绑定。

2026-09-06 已创建并实际运行无预置事实的材料 Agent：run bd10a172-5a80-5ebb-bda3-a6c3cb05ab70，
matter 767fda38-e3de-5a15-816f-510a686c7600，创建案件版本 7，幂等键 unassisted-material-agent-v1。
本次短期服务实例仅覆盖该运行预算：12任务/12尝试/3外部调用/600秒/120成本单位/32MiB成果；不改全局 Web 配置。
目标为材料来源绑定的事实/诉请/争点/缺口候选，无 DEFENCE_STATEMENT 标记，使用通用模型规划而非固定应诉规划器。
一次性 Worker 完成真实双角色预检及运行装配，文书生成在本次读卷阶段关闭；余额查询 available=true。
只调用该 run 的 process_run_once 一次，DeepSeek 规划输出上限4096 tokens、60秒，持久外发账本恰为
SUBMISSION_STARTED→SUCCEEDED，结构化原提案已保存；没有启动后台循环或调用抽取模型。
模型实际给出三步：pdf_reading（全部8页）→case_ledger_extraction（同8页）→case_context_review（档案），
包含明确依赖。Worker 返回 PLANNING_FAILED/PLANNER_PROPOSAL_REJECTED，不能称任务图已接收或分析完成。
对同一持久提案做零外发编译重现，具体错误为 compiled task graph exceeds the run runtime budget。
本轮600秒上限未考虑各策略按最大尝试次数累加的预留，是验收预算设计失配，不是模型未产生计划。
原运行和提案保留；下一目标修正预算前检及受控恢复方案，不重写历史预算、不新发同一规划请求或虚报成功。

ADR-0090 源码修复：任务重试是上限，不是每项必须保留全部尝试。编译器保持原聚合预算规则，
若完整预留超限，仅把 IDEMPOTENT 任务的可选重试回收到首次执行，再按任务序号逐轮用余量分配重试。
非幂等任务、所有单次超时、外部调用/费用/输出上限、任务/依赖/审批均不变；首次完整执行仍超限就拒绝。
通用及动态计划编译使用同一函数，旧已接收任务图不重写。当前真实三步计划的对应策略从1380秒预留
可降至540秒，仍满足原600秒上限；这不是扩大历史运行预算，也不是删任务换通过。
16项规划器测试通过，新增覆盖600秒完整首次执行、1000秒有限重试、1380秒保留原图、首次超时及外发/输出超限拒绝，
并核对除尝试次数外任务字段不变。尚未部署新编译器，也未重新接收历史提案；没有新模型请求。
下一目标是在当前来源/提案指纹不变的条件下，用新编译器核对已保存提案，并沿既有受控恢复写入任务图。
任意模型计划的完整预算可行性仍须编译后判断；本修复不声称已解决所有调用前预算预测。

真实留存提案复核更正：新增临时 planning-recovery 构建阶段只复制编译器，tar 上下文仅两个源码文件，
基底镜像 ecb180c74006… 未变，临时镜像 8bff83f3188d…，未替换任何现有服务，不是完整发布。
首次直接将本地镜像ID作为FROM参数被BuildKit当远端标签而失败；改现有本地标签后确认解析到上述同一摘要，构建成功。
新镜像对原持久提案只读重编仍拒绝：服务器自动增加 legal_rule_research_planning，故实际四任务为
研究计划120秒/读PDF300秒/抽取120秒/上下文核对120秒。先前540秒估算遗漏强制任务，正确首次合计660秒。
原run仍WAITING_INPUT、next_commands为空，不存在可直接执行的恢复命令。没有新模型请求或任务图写入。
预算异常现携dimension/required/available/task_count，并新增四任务660>600必须拒绝的回归；不删强制法源任务。
下一目标设计并接通保留成功提案、明确新预算和审批事件的恢复操作，而不是调用原循环或改写历史600秒预算。

ADR-0091 第一阶段已实现：新增 PLANNING_BUDGET_REVIEWED 强类型事件及编解码，携当前快照、
旧/新运行时间、原请求/规划输入/提案摘要及复核人。状态机仅接受无图、无任务、未失效且
PLANNER_PROPOSAL_REJECTED 的 WAITING_INPUT；时间必须严格增加且仍受总上限约束，事件操作者须匹配复核人。
重放只更新当前运行时间预算并回到 PLANNING，其他预算及历史状态不变。原run未写此事件。
35项状态机/规划器测试通过，含编解码往返、旧状态不变、来源/复核人/数字/摘要错误拒绝。
新事件明确不加入普通人类/Worker/审批写入白名单；当前数据库无许可、无API/UI入口，不可由普通append_event写入。
下一目标为专用事务写端：锁定当前运行、验证原成功提案与当前来源、按新时间预算实际编译后再追加复核事件；
随后以原成功尝试恢复任务图，不允许缺失提案时退回重新调用模型。此阶段未完成真实数据库或用户旅程验收。

ADR-0091 持久校验第一部分已接入 _append_event_locked：在既有幂等读取、run/matter锁、当前案件授权和
状态机核验之后、任何事件INSERT之前，预算复核会锁定最新规划尝试，读取同范围最后外发记录。
要求尝试/外发均SUCCEEDED，案件版本、规划输入、请求摘要与请求编号一致，再用真实提案解析器核验goal/hash，
按规范JSON重算提案指纹（数据库JSON数组与Python元组按相同JSON语义比较）。缺失、未知、失败和篡改均拒绝。
36项相关测试通过；首次发现并修正导入缺失和tuple/list结构误比较，没有改动真实提案数据。
这是模拟连接验证的写前守卫，不是已完成持久命令：通用写入白名单仍拒绝新事件，未增加数据库许可或开放入口。
下一步仍须专用命令中的新预算可编译性检查、当前源码/成功提案恢复绑定及真实SQL权限验收，之后才能恢复原run。

2026-09-06 真实Web权限验证已完成：以原基底+规划器/状态机/事件仓库三个源码文件构建临时adr0091镜像，
现有服务未替换。一次性API进程使用实际 lawcase_web_application TLS账号，在原run/matter锁和当前授权下，
读取留存成功提案并执行预算复核指纹守卫，PASSED_REAL_WEB_ROLE，event_version=3、WAITING_INPUT；整个事务ROLLBACK。
没有调用模型、写事件、恢复任务或要求用户登录。该结果证明真实读取/锁权限及原提案匹配，不证明写事件已可用。
专用 review_retained_planning_budget 命令已实现，独立于通用append白名单，复用原幂等账本及事务写路径；
事件INSERT前必须使用真实CaseAgentPlannerCompiler、同快照/规划指纹对留存提案按新时间上限完整编译。
无编译器、来源不符或预算仍不足就拒绝，其他预算不变，不接收模型生成的新提案。
37项状态机/规划器/守卫测试通过，含编译前来源匹配和不可行预算拒绝；专用命令尚未真实写入。
下一目标是新增前向数据库事件许可及预算投影/恢复绑定检查，然后对原run执行一次有审计的复核与图接收；
数据库迁移和UI仍未开放，不能宣称案件已恢复或该能力已商用验收。

0080前向迁移已实现、未部署：扩展事件类型约束，预算复核INSERT增加Web角色/有效主办复核人、
原运行等待状态、当前案件版本、旧检查点预算、严格增加的有界时间、最新成功尝试与请求/提案指纹守卫。
JSON递归紧凑序列化仅用于已规范提案摘要，与Python规范JSON语义对齐；不修改原run_budget不可变列，
有效预算继续从事件和检查点重放。真实lawcase数据库事务内DDL已执行通过，成功留存提案得到1799字符规范JSON，
随后整个事务ROLLBACK，未留下函数/触发器/约束变更；这不是正式迁移或真实INSERT权限验证。
下一目标在同一回滚窗口验证新事件INSERT守卫和Python/SQL提案摘要一致，再正式迁移并恢复原成功提案。

0080已于2026-09-06部署至本机受管Alpha：先在回滚事务SET ROLE lawcase_web_application，
实际正确事件INSERT通过、篡改proposal_hash被拒绝，SQL规范JSON摘要与Python独立结果
c64c790767eb1a10c3cb9f01d765e057d8c534039ccde39e4a2510c08cfe4689一致。
此SQL守卫测试使用占位事件摘要且整体ROLLBACK，仅证明触发器与角色边界，不冒充完整应用事件回执。
随后停API/frontend/gateway并保存0600私有备份 artifacts/lawcase-before-0080.dump，pg_restore目录读取通过；
经原postgres-migrate入口正式应用0080，postgres-assert的80迁移/TLS/角色/RLS/隔离检查PASS，原服务已恢复。
临时adr0091源码镜像更新为b650a57e86d9…，尚未替换主API。原run仍event_version=3、WAITING_INPUT、初始600秒，
仅一次规划SUBMISSION_STARTED，没有预算复核事件或第二次模型调用。
下一目标用专用维护入口协调Web复核身份与Worker只读编译，在原run追加660秒复核、恢复成功提案；
必须限制到该合成run并禁止规划外发，同时明确旧主API尚未支持新事件，不能直接当作用户端已部署能力。

2026-09-06 原run已恢复并实际执行读取：固定范围维护脚本 recover_unassisted_planning.py 使用临时adr0091镜像，
以独立Web账号执行合成主办身份预算复核，Worker账号构造当前规划快照/编译器；NoPlanningSubmission阻止新规划。
事件4为REVIEW_RETAINED_PLANNING_BUDGET回执，事件5为PLANNED/PLANNER_RESULT_RECOVERED_AFTER_RESTART，
初始600秒记录不变，有效预算660秒。未重新外发原规划，未覆盖失败事件；维护进程不启动后台消费循环。
随后只对该run单步执行：法源研究计划任务5776df01…返回TASK_SUCCEEDED，事件7。
经真实Web submit_approval 对合成案卷8页内部读取范围授权（不是事实批准），事件8；
Worker实际执行PDF任务843976de…，尝试4f9a26c6…返回TASK_SUCCEEDED，事件10。
所有临时进程退出并移除，临时会话正常撤销；本轮未调用抽取/规划模型，未作律师真实性或整案交付声明。
此证明受控恢复后的真实任务执行，不证明抽取质量或独立成果核验。下一目标为一次有界的材料事实抽取，
核对原件页码、原告诉请/双方陈述的区分、矛盾与缺口候选；不预填结论，不重新做入卷或读卷检查。

2026-09-06 材料抽取及独立核验推进：原run bd10a172-5a80-5ebb-bda3-a6c3cb05ab70 的抽取任务
9e1aa236…已在事件13成功，受管候选 b1ae6862-32b1-5d76-8b0a-68359f2fce33 为13703字节、17条。
本轮经独立verifier身份回读私有对象，重算内容摘要并通过规范解析；没有重新调用抽取模型。
候选包含12条事实/陈述和5笔交易，识别本金200000/205000口径差异，并保留原告诉请、被告主张等归属。
质量缺口：3笔被告主张的还款被标CONTRADICTS_CASE_LEDGER，但本案没有已确认台账；
抽取请求仅有pages，无法支持此台账比较标签。不得把该标签当作已证明矛盾，也不得静默改写原模型成果。
随后只对该run单步执行case_context_review成功至事件15，独立成果验证VERIFICATION_PASSED至事件17。
既有REQUEST_FINAL_REVIEW路径实际暂存17条，返回WAITING_HUMAN/LEDGER_EXTRACTION_REVIEW_REQUIRED。
只读数据库核对：FACT/BULK_PROMOTION_ELIGIBLE=7、FACT/EXCEPTION_REVIEW=5、TRANSACTION/EXCEPTION_REVIEW=5。
这证明来源绑定候选已进入复核队列；批量可复核不是已批准，结构/来源验证不是语义正确或律师认可。
本轮没有事实确认、模型外发、最终审批、文书生成或提交包锁定；没有启动长期Worker。
对照商用计划仍为内部Alpha；主API新事件兼容和浏览器旅程仍未完成。
下一单一目标：闭合这17条候选的律师复核体验，先修正无台账比较依据的标签合同，再接通实际浏览器查看、修改和确认；
保留本次原始成果与问题，不以新模型重跑掩盖缺陷，不自动批准争议候选。

同日完成页材料抽取提示合同修正（源码，未部署）：禁止没有台账输入时输出CONTRADICTS_CASE_LEDGER，
要求保留原告诉请/被告主张/材料记载归属，并明确材料指令不可信。历史候选及异常分流未修改。
因提示内容参与请求指纹，reconcile保留旧提示的精确指纹lookup兼容；新发送仅使用现行提示，
未知结果不重发，两版均不匹配则拒绝。17项适配器及持久交换测试通过，新增新旧指纹、无发送、错误ID拒绝检查。
这不是模型质量复验，也不是浏览器交付；下一目标仍是17条候选的实际律师复核体验及主API兼容部署。

同日主API兼容已局部部署：实时检查旧API枚举不含PLANNING_BUDGET_REVIEWED；规范Compose加既有
sealed-recovery覆盖层与活动API的配置环境键值一致、挂载目标集合一致，目标adr0091镜像已本地存在。
仅对api执行no-deps/no-build/pull-never替换为lawcase-planning-recovery:adr0091，旧基底镜像保留可回退；
未启动Worker、数据库迁移、模型请求或其他服务。API恢复healthy，新枚举可读。
实际API进程通过lawcase_web_application账号、合成主办角色回放原run，返回事件17/READY_FOR_REVIEW/4份成果。
这证明主API已能读取该运行，不证明HTTP会话、浏览器复核或律师认可；历史源码挂载仍在，非干净发布基线。
该镜像仅包含此前三个规划恢复源码，上一轮页材料抽取提示修正尚未包含，不能误报已部署。
浏览器观察：内置浏览器到工作台为ERR_CONNECTION_CLOSED，Chrome创建操作也超时，不能断言其最终页面状态。
同一主机curl标准TLS验证访问工作台返回HTTP/2 200；未绕过证书验证、修改代理或让用户重新登录。
磁盘约1.8GiB，仍不执行完整重建。下一目标在既有浏览器中恢复页面观察并完成17条候选的用户复核旅程。

同日浏览器连接诊断及复核服务实读：Chrome上轮目标标签仍存在，读取标签再次超时，未再建重复页或发起登录。
系统HTTPS代理启用于127.0.0.1:7990，例外列表没有两个nip.io测试域名；同一工作台URL经该代理TLS超时，
显式直连且保留标准证书验证返回200。此差异支持代理路径是浏览器阻碍的解释，尚未实际改代理或证明修复。
现有API组合根用短期合成身份实际调用list_batches成功，batch fff3576b-0a82-504e-81ba-a7f85caf969a，
REVIEW_READY，17候选/7低风险/10异常，当前及来源案件版本均7；finally通过正常服务撤销会话，退出0。
没有批准候选或模型外发。建议只对workbench.127.0.0.1.nip.io及identity.127.0.0.1.nip.io设置代理直连，
保留HTTPS/证书/MFA；涉及项目目录外共享网络设置，须先获具体范围授权。其间可继续项目内复核逻辑工作。

同日代理设置授权待回复期间，推进项目内律师复核投影修正：CONTRADICTS_CASE_LEDGER此前被翻译为
“与本案当前台账记录存在矛盾”，把模型标签呈现为已核实结论。现明确“模型提示可能、尚未核实、
不证明已有对应台账或实际冲突”；含此标签的历史异常组摘要采用同样的审慎呈现，风险标题改为待核实。
只改Web派生展示，保留历史候选/组ID/分流政策/审批权限及数据库内容，未批准候选，未新增模型调用。
16项Web复核服务测试通过，新增覆盖历史组不变、展示归属、候选与组警示、无确认写入；源码尚未部署。
这修正了律师被错误标签误导的风险，不代替真实浏览器查看与语义复核。下一目标仍为实际浏览器复核旅程；
待明确两个本地域名代理直连的授权后再动共享网络设置。

同日两项修正已进入本机API局部镜像：现有api.overlay.Dockerfile新增review-recovery阶段，复用此前
三文件规划恢复层，仅再复制抽取适配器与Web复核投影。显式tar上下文为Dockerfile及5个Python文件，
不含案件artifacts、凭据或整个仓库；network=none、pull=false，未重建依赖。构建前磁盘2.8GiB，未执行完整重建。
镜像lawcase-review-recovery:local，摘要e7db1b4cdf16296277dc3916695e8acc9e4cf60ec7417604a69f7e3cf114c0f5；
只替换API，原adr0091镜像保留可回退，健康检查healthy。主API实际组合根以合成身份list_batches读取：
17/7/10仍不变，3条模型台账标签对应组现在显示“模型提示可能与台账矛盾，尚未核实”，并核对新提示已加载。
正常session_authority.revoke收尾成功，进程退出0；没有事实确认、数据库迁移、模型调用或长期Worker。
这仅证明局部镜像/API输出已生效；未来Worker须显式选择该镜像，规范默认镜像未改。仍有历史源码挂载，
不能作为干净发布基线。浏览器显示与操作尚未验收；代理直连授权仍待用户回复，不重复登录或改系统设置。

同日低风险确认验收意图（执行前记录）：已通过实际API短期合成身份逐条读取7条低风险候选及逐字摘录，
为案号、案由/当事人、受理/举证/开庭日期、明确归属于原告请求的本金及利息口径；摘要与所引材料一致。
仅模拟合成主办角色确认材料记载，不承认债务、不批准利率或计算参数，10条异常仍不得批准。
限定matter 767fda38-e3de-5a15-816f-510a686c7600、batch fff3576b-0a82-504e-81ba-a7f85caf969a、
expected_version=7、幂等键unassisted-low-risk-confirm-v1；调用真实Web确认服务，不启动后续模型循环。
若命令观察异常，先按此键读取回执/当前状态，不能新建键盲目重试；执行结果以下续记为准。

上述合成低风险确认实际成功：真实Web服务按固定键返回matter_version=8、confirmed_fact_count=7、
confirmed_transaction_count=0。回读批次LOW_RISK_CONFIRMED_EXCEPTIONS_OPEN，10条异常OPEN；会话正常撤销。
随后一次性Worker账号仅调用apply_pending_snapshot_refresh，返回None，旧run仍事件17/READY_FOR_REVIEW/快照7。
管理员只读刷新队列确认：7→8请求已持久生成，状态BLOCKED_BY_OPEN_EXCEPTIONS；不是漏写刷新请求，
也不是旧运行已刷新。0046机制明确等待异常处置，不能把None误报为刷新成功或强行追加新版本。
本轮无模型调用、无新Agent运行、无正式收付款确认、无提交锁定；不代表真实律师审批或浏览器验收。
下一目标处理10条异常的合成处置旅程及补证/纠正路径，再验证刷新与后续任务衔接；不得为通过而批准争议事实。

异常处置验收意图：同一batch的5组/10条已逐条读摘要及摘录。交易组ec99db70…（30万元交付缺项）、
e031c860…（3笔被告主张还款）、3a074afe…（20万/20.5万元口径）拟REQUEST_MORE_EVIDENCE/EVIDENCE_GAP；
事实组3229920e…（利息冲抵主张）和93a71389…（4条低置信陈述）拟DEFER_WITH_REASON/NEEDS_LEAD_REVIEW，
写入具体原因，不能确认债务、利率或付款。每组使用unassisted-exception-{完整group_id}-v1独立稳定键，
执行前重新读取当前版本，仅按既定5组逐次执行，不发模型请求，不创建重抽取任务。
真实可用性缺口：4条陈述组包含“原告自认已支付利息”的主体措辞问题，现可选仅重抽取/暂缓，
缺少律师直接纠正候选的路径；不得把该组暂缓等同问题已解决。回执以下续记为准。

上述5组处置已通过真实Web服务按固定幂等键成功保存，案件版本保持8，分组处置计数依序1→5，
末回执exception_review_status=RESOLVED；仅指分流选择完成，不能解释为证据/法律争议已解决。
管理员只读后续台账确认MORE_EVIDENCE/ACTIVE=3，DEFERRED_REVIEW/ACTIVE=2；未生成重抽取、未批准交易。
随后一次性Worker只调用刷新消费返回None；刷新队列仍BLOCKED_BY_OPEN_EXCEPTIONS，旧run事件17/快照7。
0049的活动后续事项仍构成约束，不以人工改表解锁。所有会话正常撤销，没有新模型调用或后台循环。
下一目标：将其余已准备合成材料按具体补证事项关联入卷，验证补证绑定/满足条件；并实现律师纠正候选的缺失入口。
不能用“暂缓”冒充修正，也不能把已准备但尚未入卷的材料写成缺失于完整案卷。

同日银行补证链实际推进：只读PDF技能检查合成F6银行材料10页，SHA-256
8cff8981ecbd3293e2941004bee830d514dbc4e7a72e295a4b92a88a61b06c78；第3/4页记载两笔放款。
该文件为简化合成文本，不是逼真银行流水版式；未据此声明真实账单视觉理解通过。
经现有Web材料上传服务创建slot 6d1e9dea-0bff-43a2-a427-f8558ec36e4c并完成入卷，
file 501232eb-3d6e-477d-b8b5-8a06d0172a7c、10页、案件8→9，原件哈希匹配。
意图/slot/回执保存在artifacts/supplement-bank.diUuWg（私有持久检查点），无重传或模型调用。
真实Web后续服务对两项放款补证均只列出上述新增EVIDENCE_FILE；按固定
unassisted-bank-supplement-{followup_id}-v1键提交来源关联，b5b1a165-cac5-498f-8d59-628e882de45c
及75669a4d-1602-48ad-b5fc-7a4ac2e5fd23分别SATISFIED，案件版本10/11。
理由明确仅确认银行材料补入，仍需与借条/诉请核对，不确认债务金额、利率或法律结论。全部会话正常撤销。
未完成争议还款补证及两项待研判；下一目标补齐对应微信记录并接通候选纠正，不能将补证已入卷等同争议已解决。

候选纠正第一阶段已实现：ADR-0092明确复用现有事实候选/审批但必须新增原提取关联，不覆盖原稿、不自动入账。
现有case_agent_ledger_extraction模块新增纯函数纠正提案合同，要求确切原成果/候选摘要，复制原候选及来源，
只接受FACT改稿和必填理由，不继承置信度/批准，不接受交易自由文字改写或无修改提案。
6项抽取合同测试通过，新增原稿不变、确定性输出、错误身份摘要/空白/超长/无修改/交易类型拒绝覆盖。
仅为源码合同，不是持久保存能力，也不是律师可用入口；不修改实际17条成果或已确认7条事实。
下一单一目标为只追加纠正提案的持久关联、角色/版本/幂等写端及真实权限验证，然后接现有异常复核界面。

纠正提案存储结构已实现（0081源码，未部署）：只追加表绑定原候选/同案来源、预期案件版本、连续修订链、
提案作者、唯一幂等键和正文；RLS仅本案活动主办/复核律师可读，生产INSERT权限暂不授予。
触发器锁案件并核对版本/前驱，逐字段核对原成果及候选JSON，不允许以修改稿覆盖来源、改类型或冒充批准。
真实PostgreSQL回滚事务已创建DDL；再临时授予事务内探针写权限，以lawcase_web_application及合成主办身份，
正确提案INSERT通过，UPDATE、旧案件版本和篡改来源摘要均拒绝，输出REAL_WEB_ROLE_CORRECTION_STORAGE_PROBE_PASSED。
整体ROLLBACK，未留下新表、权限或候选；使用占位请求摘要，仅证明SQL存储约束，不证明应用幂等/来源重读命令。
下一目标实现同事务授权/来源重核/精确幂等读回的保存命令，再规范部署；跨角色、并发及浏览器验收仍未完成。

纠正提案应用保存命令已实现（未接入组合根/未部署）：PostgresFactCorrectionProposalStore提供save/find_by_key，
两阶段读取，私有原件重读期间不持有数据库锁，写事务重新锁案件、核对当前角色/版本/原绑定/前驱后INSERT。
同律所操作者请求键有事务锁及唯一约束，同键同内容返回原回执（即使案件随后推进），异内容拒绝；
读取回执仍要求当前案件权限，不把回执当来源或事实批准。保存不写正式事实/Agent事件，不推进案件版本。
4项保存命令模拟测试和6项原候选合同测试通过，覆盖同键查回、权限撤销、读期间版本变化、前驱冲突、
原字节篡改及Worker身份拒绝；不是实际持久save调用或并发数据库验证。
生产原件/页级重核reader尚未接线，0081仍不授予INSERT，UI未开放。下一目标复用既有私有来源读取实现该reader，
再以真实Web数据库权限验证完整保存与回执，避免依赖浏览器提供的来源或只验证一个旧哈希。

纠正来源reader已实现：复用Web成果服务的独立复核链和私有对象回读，并提取共享内容校验，保留原浏览器投影行为。
新增服务器内部read_verified_extraction_original，仅接受普通已暂存提取成果，不接收封存恢复件；无HTTP原字节出口。
WebFactCorrectionOriginalReader复用WebAgentEvidenceProjectionSource逐页回读原PDF，核对原文件摘要、页码、
文字摘要及所有摘录，读取前后案件版本一致才返回原成果。当前原生文本路径可用，OCR/视觉来源明确拒绝，
须后续接对应独立来源reader；不称为所有材料纠正能力完成。
20项来源reader/成果投影/保存命令测试通过。实际API短期合成身份调用内部原成果入口成功读取13703字节，
SHA-256=460ec6c4de5ba239d04ab52b8efdd755c55cdf6a067f317e7802a899eef9810e，会话正常撤销。
该实跑只证明原成果读取，尚未将新reader串入真实save；0081未部署、写权限仍关闭，未保存纠正稿或启动模型。
下一目标组合新reader和持久save，在真实Web角色回滚事务中验证完整保存/按键查回，再经规范迁移上线。

来源组合实跑补记：已在现有API容器的独立短期进程中装载当前纠正模块源码，使用现有合成身份及生产
私有原件/页投影适配器完成WebFactCorrectionOriginalReader全路径调用，输出
ORIGINAL_AND_CURRENT_PAGES_REVERIFIED，13703字节，摘要与上述原成果一致；会话已撤销。
本次证明原成果及其当前原生PDF页可重新核对，不证明持久save、事务提交或浏览器体验；未调用模型。
补齐read_current保存稿读取合同（未部署）：本案当前主办/复核权限和原候选绑定通过后才能读回最新修订，
返回原候选、改稿、理由、作者、时间及修订号；案件版本不同明确标记stale，所有读回均待律师审核、不可提交。
保存/读回及来源reader共8项聚焦测试通过，包含未保存、原稿保留、版本变旧和撤权拒绝；不代替真实数据库验收。
下一单一目标仍为完整来源reader与save的真实数据库组合验收，然后接API/页面；0081未部署，产品纠正入口仍未完成。

完整保存组合回滚验收已通过：新增显式启用的test_fact_correction_runtime，固定隔离合成案件/候选，
实际API短期进程执行原件与当前PDF页重核，应用save在真实PostgreSQL的Web角色事务内保存提案；
同请求重放及find_by_key回执一致，read_current读回原候选/改稿/理由，court_ready保持false，
同键异内容拒绝，表内仅1条提案。该测试2.764秒通过，随后独立查询证实临时表不存在、案件版本仍为11。
为共享未提交0081，测试使用单事务psql桥代替生产独立psycopg连接；因此不证明独立提交、连接中断恢复
或并发双写，仅证明真实来源读取与应用SQL、数据库触发器/RLS组合。未持久变更迁移、权限或正式事实。
下一单一目标接入应用服务/API的保存及恢复合同，然后规范部署0081并完成独立提交与页面路径；
不得继续重复本回滚探针或把它当整案Agent验收。

纠正API代码已接入（未生产装配）：新增同源保存、读取当前稿、按Idempotency-Key查询回执三个端点，
复用服务端会话身份和既有请求键校验；请求严格限制案件版本、前驱修订、改稿、理由，不接受来源或操作者字段。
保存回执与恢复响应均带版本化schema和court_ready=false；来源/权限/版本冲突返回不含内部信息的409，
未配置返回503。接口使用同步执行线程，避免私有原件回读阻塞异步事件循环。
依赖校验要求save/find_by_key/read_current齐备；Worker混合律师角色也不可查询回执，避免重放分支绕过来源reader的角色拒绝。
Web接口及保存命令70项测试通过（4.493秒）；其中修正一项既有终审测试对document_review_versions默认空元组的过时预期，
未修改该终审功能行为。新增接口测试覆盖保存/读回/恢复、服务端身份、拒绝额外来源字段与布尔版本、缺少请求键、未配置与冲突脱敏。
非发布overlay补充新导入模块的只读挂载配置，未重启API、未运行迁移、未开放页面，当前进程仍不具备新入口。
下一单一目标在运行时组合根按部署就绪状态装配纠正存储/原件reader，再完成0081规范部署和独立提交恢复；随后接律师页面。

纠正运行时及0081已部署：组合根接入真实私有原成果/页级reader及Web角色持久存储；端点就绪探针要求
表存在、强制RLS、只读/追加权限且无UPDATE/DELETE、不可变触发器启用，缺失时503而非假保存。
74项Web/组合根测试通过。0081此前未部署，本次源码开放SELECT/INSERT，规范postgres-migrate记账到81，
postgres-assert输出TLS/迁移/角色/RLS/种子/Keycloak隔离PASS；未修改任何已应用旧迁移。
迁移前备份artifacts/lawcase-before-0081.dump为5855884字节、0600，pg_restore目录读取通过；
SHA-256=f3214a72608979982f372a666ed9b11b76730fe47053b5ddba55fa19236a510e。此证据不是隔离恢复演练。
初次API重启因新组合根/旧文书复核镜像混用参数失败；未删除content_binding_resolver校验，改本地非发布overlay
为case_api/case_kernel两包一致只读源码挂载，替代逐文件混用；API重新启动后running healthy。
仍使用旧基础镜像加宿主源码，并非不可变商业发布镜像；没有启动新常驻Worker或付费模型调用。
实际生产组合根save成功提交合成付款主体纠正稿49d495f1-d866-4d3a-b6b6-b8139ea6251d，revision=1，
source_matter_version=11，键unassisted-fact-payer-correction-v1。另一个独立进程find_by_key/read_current
确认相同回执及完整修改稿，NEEDS_LAWYER_REVIEW、court_ready=false、stale=false，案件仍版本11；两次会话撤销。
以上证明独立Web数据库连接提交及跨进程读回，不证明HTTP实测、网络中断注入、双写并发、律师审核或浏览器体验。
下一单一目标将保存/恢复/原文改稿对照接入律师异常复核页面，以这份已保存合成稿验证用户路径，禁止重复创建同一稿。

律师事实纠正页面代码已接入既有现代案卷室异常复核面板：已处置组不再隐藏成员与来源，处置回执和
“不等于事实已确认”说明保留。只有FACT投影暴露确切原候选编号以打开修改稿，交易不开放自由文本改金额入口。
页面显示原候选、修改表述、必填理由、修订号/失效提示；保存调用实际接口，刷新可重读服务器稿。
未知保存只查询原请求，sessionStorage仅记录不透明请求键、不存案情正文；明确4xx拒绝保留文字并要求重新读取，
5xx/断网仍保留未知请求，不自动重发。明确提示未保存文字仅保留当前页，不能宣称未保存草稿可跨刷新恢复。
TypeScript及相关3文件ESLint通过；异常组/PostgreSQL投影/Web接口89项测试通过，另新增FACT编号暴露/交易不暴露测试通过。
既有组决策仍不接受逐项候选ID，纠正提案是独立待审核写入，不替代整组处置或正式事实审批。
本轮未构建/部署前端、未运行真实浏览器：当前磁盘仅2.3GiB，不做全镜像构建；不能将代码检查称为用户验收。
下一单一目标在受限资源内构建并部署这次前端变更，通过既有合成已保存稿验证真实页面的查看、对照和刷新恢复。

前端构建/受管发布已推进：在768MiB Node堆上限下构建，首次Webpack编译虽通过，但替换现有Turbopack裁剪镜像
产物后出现app-page.runtime.prod.js缺失、HTTPS500；立即恢复原前端（HTTPS200），未把编译通过当发布成功。
改回与既有镜像一致的Turbopack构建通过（编译9.7秒、类型3.6秒），再只读挂载web/.next，保留镜像Linux Node依赖，
不挂macOS node_modules、不重建依赖镜像。新前端HTTPS200、启动日志正常，API重启加载候选编号投影。
真实HTTPS/CA校验、独立cookie jar读取/api/v1/session及纠正回执、已处置异常组成员和修改稿成功：
HTTPS_SAVED_DRAFT_AND_DECIDED_GROUP_READBACK_PASS 1，确认旧提案编号及court_ready=false；没有再次保存或模型调用。
此HTTP验收使用现有本地合成会话机制，非真实OIDC用户验收；临时客户端退出，服务端会话按既有短期策略到期。
浏览器工具创建页面超时；随后读取实际浏览器清单，内嵌页仍为ERR_CONNECTION_CLOSED。本机7990系统代理启用，
排除列表未包含workbench/identity的nip.io域名；未自行修改共享网络设置。浏览器视觉、实际点击/刷新恢复仍未验收。
下一单一目标解除本地浏览器连接阻碍后完成现有已保存稿的页面验收；不可用HTTP200或接口读回替代浏览器结果。

构建后能力清单check通过、前后端healthy，部署BUILD_ID=SkDnaGDY8WdYoBTyitCs2。随后磁盘仅318MiB；
实测.next整体196MB、cache147MB，不能把全部空间变化归因于前端产物。仅删除本轮生成的webpack/turbopack/swc
编译缓存（可重建），保留运行server/static、原件和备份，暂停进一步构建；不擅自清理系统或其他项目。

发现并修正修改稿版本恢复缺陷（源码，尚未重新发布）：旧页面重读后仍使用首次批次版本，可能持续409。
read_context现在即使draft为空也返回同次授权读取的current_matter_version；前端每次读取更新提交版本。
已失效稿核对后允许保持原改稿文字另存新修订，不要求制造无意义文字变化；仍不自动批准事实。
7项聚焦测试通过，覆盖空稿/已保存稿当前版本、旧稿失效和接口读取；TypeScript无增量检查及相关ESLint通过。
本轮未构建、未重启、未调用模型或修改代理；磁盘从438MiB回升到1.7GiB，原因未独立确认，不能归功于本轮清理。
下一目标仍为部署修正并完成真实浏览器修改/刷新路径；本机代理直连授权尚未收到，HTTP/代码验证不能替代该项。

低内存构建已完成：next.config以LAWCASE_BUILD_LOW_MEMORY=1显式启用cpus=1，普通部署默认不变。
本轮768MiB堆限制、Turbopack编译5.7秒/类型4.8秒、单worker生成页面通过，能力清单检查通过，
新BUILD_ID=6G0UaVVW2kmm9QJlo3iVr；磁盘约2.6GiB，未清理其他项目或系统。
发布未确认：前端回切命令（执行会话10750、进程75441）和Docker只读inspect（会话17476、进程75545）
仍实际存活但无输出；Docker后台进程仍在，HTTPS200不证明前后端版本一致。未重复提交这些命令、未强杀Docker。
注意：构建开始时回切命令尚未返回，故不能假定在线挂载已解除；新产物可能已被当前前端看到，
API版本恢复字段尚未重启加载，存在短期契约不一致风险。下一步优先跟踪这两个现有句柄，恢复Docker操作后
统一更新前后端并核验新响应字段；不要在该运行面继续开展保存操作或宣称交互已验收。

Docker恢复续记：两个CLI进程超过3分钟仍存活无输出，Unix socket的/_ping四秒超时；Docker UI可见Engine running，
仅本项目栈运行，另一local栈停止，容器内存显示2.23GB/3.74GB。取消本次挂起客户端后，10750以130、17476以1结束，
不据此推断服务端回切是否执行。通过官方“重启Docker Desktop（保留所有容器和设置）”入口发起一次恢复，
截图按钮呈处理中；后台49542/49546/49547尚未更换、接口仍超时。未重复点击、未清除/重置或强杀虚拟机。
原CLI句柄已终止，不再等待或重发；下一步只核对这次官方重启是否完成，再检查实际挂载和前后端合同。

运行环境阻断确认：同一Docker管理接口故障已跨连续三轮重现。官方重启未恢复；正常窗口退出不代表后台已退出。
官方docker desktop stop --timeout 20（未加force）明确以1退出，报告49542/49546/49547后台、49575虚拟化等进程仍在，
context deadline exceeded；该停止命令句柄27869已终止。日志存在持续event streamer EOF，不能由此断言具体根因。
当前没有可等待的成功重启任务回执，不再无限轮询。进一步强制停止可能造成数据库崩溃恢复，需用户明确授权，
或用户恢复运行环境后再继续；禁止清除卷/恢复出厂设置。已有迁移前备份，不等于恢复演练或最新稿完整备份。
全项目商用目标保持未完成，发布/浏览器验收均未闭合；先恢复运行环境与前后端版本一致，再推进原计划。

2026-09-07恢复复核：Docker Unix接口返回OK，9个项目容器运行，API/前端healthy，磁盘6.5GiB；
未执行强制停止，恢复原因未独立确认。实际前端挂载.next且BUILD_ID=6G0UaVVW2kmm9QJlo3iVr，API为两包只读源码基线。
HTTPS验收确认read_context新合同已加载：既有提案及另外3个空稿均返回current_matter_version=11，
原提案49d495f1-d866-4d3a-b6b6-b8139ea6251d保持待审/不可提交；输出LIVE_CURRENT_VERSION_CONTRACT_PASS，
临时合成会话通过CSRF退出得到204。未再保存、重建或调用模型。此前Docker接口阻断已解除，不再沿用旧阻断结论。
剩余页面门：系统7990代理仍启用且未排除工作台/身份域名，尚无修改代理授权；不重复开页/登录，
不把HTTPS读回当浏览器验收。下一目标仍为在可访问的浏览器中完成已保存稿原文对照、修改及刷新恢复。

运行授权更新：用户随后明确表示“你可以自己启动docker”。已确认Docker引擎和现有9个项目容器运行，
带健康检查的服务正常；启动限制以此新授权为准。内存约束仍有效，不额外启动整套服务或常驻Worker。

用户说明：同时运行 Docker 会造成内存不足，导致 Codex 强制退出。此后不得自动启动或重启 Docker，
也不在本机启动完整容器栈；本机仅进行代码开发和轻量检查。此前 Docker 关闭的具体触发机制仍未独立证明，
但用户报告的内存约束已足以改变执行方式。完整部署、浏览器业务流程与文书渲染验收需安排内存充足的环境；
不得降低既有安全门或用轻量检查替代整案验收。远程环境的选择、费用及数据路径须在实际部署前明确。

### 原生 PG16 验证试点（优先于继续增加未实测的恢复 UI）

当前重新测量：8GB 内存，磁盘可用约 7.2GiB，开始时系统空闲内存约 33%。项目内构建原生 PG16，
不是 Docker/完整服务栈，不连接既有数据库或案件。临时目录为 `artifacts/native-pg16/build.AV40mw`，已被 Git 忽略。
PostgreSQL 16.15 与 OpenSSL 3.5.8 发布包已核对 SHA-256；OpenSSL 动态库编译完成，最大 RSS 119,521,280 bytes。
首次 PG 静态 OpenSSL 链接触发 libpq 禁止退出调用检查，未跳过检查；改动态链接后编译、安装及真实 pgcrypto 安装成功。
证据日志为该目录 `postgres-shared-build.log`、`postgres-shared-install.log`、`pgcrypto-install.log`。
原生适配器复用现有迁移/ACL 脚本，同一 psycopg 会话保留 SET ROLE/事务；仅接受项目内空白、禁用 TCP 的 PG16。
原生 PostgreSQL 16.15 空库已实际启动并串行执行迁移，在 0064 因 ledger confirmation owner 无 public schema CREATE
权限失败；尚未执行到 0078 或 37 项 ACL。失败日志为 `native-migration.log`，隔离现场 `artifacts/native-pg16/data.fcBgZS`
保留，实例已正常停止。已实查禁用 TCP、shared_buffers=16MB、fsync/full_page_writes 开启；迁移客户端最大 RSS
54,411,264 bytes，不代表整个数据库总内存。结束测得系统空闲内存 46%，不据此保证完整应用可在本机运行。
失败后 schema owner 已降回非超级用户。0064 显式切换到受限 owner 后重建函数，而前序迁移撤销其 schema CREATE；
已按 ADR-0087 修正部署入口：仅 64/66/67 临时恢复函数所有者的 schema CREATE，每文件后及失败清理撤销，
不修改旧迁移。新隔离库 `artifacts/native-pg16/data.2E5MnU` 实测全部 78 项迁移与后置加固、37 项 ACL 检查通过；
证据 `artifacts/native-pg16/build.AV40mw/native-owner-window.log`，1.70 秒，客户端最大 RSS 55,951,360 bytes。
额外实查两个专用所有者均不能登录、不是超级用户且无 schema CREATE；schema owner 已降权。临时实例已停止。
这证明空库迁移和所列权限检查，不证明业务行 RLS、触发器原子性、并发或整案 Agent。下一目标：在同一低内存路径
用合成案件执行正文修订/恢复的真实业务事务，先补实际 SQL 证据，不继续增加未实测的 UI。
恢复锁角色接管函数前的临时 schema CREATE、接管后撤销已由本次真实迁移及权限查询确认。

已有真实业务测试增加 `CASE_WORKBENCH_NATIVE_PREPARED_SOCKET` 入口，连接前拒绝网络目标，写入前核对项目隔离
目录、PG16、无 TCP、已迁移且 firms 为空；此模式不执行 DROP SCHEMA，也不重跑迁移。
复用四项既有用例，合成建案幂等/审计、跨律所读取隔离、Agent inbox 触发认领、事实候选及确认版本均实际通过，
日志 `artifacts/native-pg16/build.AV40mw/native-business-configured.log`；测试 0.402 秒，客户端最大 RSS 92,110,848 bytes。
随后将原生模式的建案/台账 Store 切至 lawcase_web_application、运行认领切至 lawcase_agent_worker；同四项用例
在新隔离库 `artifacts/native-pg16/data.3VV1Gv` 通过，日志 `native-runtime-roles-business.log`，0.157 秒，客户端最大 RSS
91,209,728 bytes。已真实查询 current_user、非超级用户及无 BYPASSRLS，并将此核查纳入入口。
合成夹具仍由管理员建立，业务调用通过连接选项 SET ROLE 使用运行权限；不证明独立账号登录或 TLS。
实例已正常停止，配置持久化禁用 TCP。正文修订仍缺真实事务夹具，不再重复上述四项。
新增文书前置夹具通过真实 Web Store 依次建案、确认主体/程序/地位/委托/当前档案，不关闭触发器或约束。
在隔离库 `data.o7GETI` 实际完成这些命令及档案同键重复确认；原测试因错误地将档案输入版本与命令输出版本等同而失败。
源码核对确认既有合同是输入版本 5、输出版本 6；已修正断言。只读数据库核对 CURRENT、5→6、六条审计记录一致，
不把修正后尚未重跑的测试标为通过。日志 `native-document-posture.log` 保留原失败，实例已停止。
下一步在该前置夹具上接工作计划和原始文书登记，再验证正文授权/入队；尚未生成或恢复任何文书。
已核对 Agent 目标必须经已验证任务图晋升工作计划，不使用普通计划登记绕过此要求。
新增前置步骤以 Web Store 写入真实 RUN_CREATED（同键幂等），以 Worker 的单事务投影读取当前档案及案件快照，
外部调用预算为零。`data.zwVIwO` 上该用例通过，含代理档案的修正版本断言；起止快照和档案哈希一致。
日志 `native-document-planning.log` 同时保留另一档案审计用例的测试代码错误（Matter.version 误写为 matter_version），
已按模型字段修正，未将整组失败说成全通过。实例已停止。下一步仍是实际任务图→验证→工作计划晋升，不是已完成规划或文书。
新增规划认领真实用例，在 `data.7DjQJt` 发现生产事件摘要的时区错误：UTC 写入、CST 读回导致同一事件摘要不同。
按 ADR-0088 统一事件时间的 UTC 序列化，跨 +08/-05 及一微秒篡改回归通过。原失败现场未改历史事件，实际 Worker
重新读取原事件、成功写入 CLAIMED/PLANNING_STARTED，事件版本 2 再次回放通过；没有外发或模型成功结果。
本次档案确认/审计完整用例也通过。原失败日志 `native-planning-claim.log` 保留，修复后的原现场检查为定向运行，
不声称整组重跑通过。实例已停止。下一目标：在持久规划结果接收/任务图/验证链上继续，不绕过成功回执要求。
接收端补齐任务图与认领的 graph_id/version、运行/案件/律所、目标/快照、planning_hash/案件版本一致性，
并对持久 proposal 按原目标和规划输入重新解析；原成功状态与租约检查保留。此前接收端未逐项检查这些坐标。
14 项 Worker 持久适配器模拟测试通过，新增错误图编号/版本/目标/快照/规划输入在追加事件前拒绝及正确结果通过；
日志 `planning-acceptance-unit.log`。这是接收端合同证据，不是实际模型结果或数据库成功图登记；本轮未启动数据库/Docker。

### 单次真实规划试跑（2026-09-05）

复用已配置 DeepSeek 官方接入和 deepseek-v4-pro，使用独立合成档案，最多一次请求/2048 输出 token/60 秒，
准备请求体上限 32KiB；无客户材料、无新供应商、无重试。测试默认跳过，须显式开启单次试跑；正式请求边界仍由
既有数据库 request guard 先记录，不能直接用 HTTP 探针代替产品链。未来调用必须重新明确额度，不自动循环试跑。
`data.DZSVON` 的 Run `5e0dcbeb-0060-4147-b78a-016b3f4fe8ce` 实际通过：Web 创建→Worker 认领→真实模型提案→
持久成功结果→生产编译器→接收检查→任务图回读。用例 4.479 秒，日志 `native-single-provider.log`。
额外实查只有一次 SUBMISSION_STARTED 和 SUCCEEDED，service_id=deepseek-v4-pro，local planner events=0。
运行处于 EXECUTING/event_version=3，唯一任务为“整案上下文核对与缺口研判”，Skill=case_context_review、网络 DENY。
任务尚未执行；其用途文字提及审阅稿基础研判，不代表已生成应诉文书。当前输入只有合成代理档案，不能证明真实
案卷处理或法律分析质量。本轮实际推进了模型规划链，不是规则规划替代。实例已停止，Docker 未启动。
下一目标：执行已持久任务、独立验证其来源与缺口输出，再接工作计划；不得再次调用规划器重复生成当前任务图。
成果存储接线检查：生产上下文适配器经 PostgresReviewCandidateStagingPort 保存到私有 S3 并登记，不能用内存
替身冒充真实成果。官方 MinIO macOS 下载端点可达，但本机未安装；当前磁盘从此前约 7.2GiB 降至约 1.9GiB，
本轮未下载或启动对象服务。仅清理自行编译 PG/OpenSSL 的可重建中间文件，两源码目录合计约 414→320MiB，
余量约 2.0GiB，已安装 postgres 16.15 仍可运行。原始发布包、日志、所有隔离数据库及待执行 Run 均保留。
Docker/数据库本轮均未启动；当前任务未认领执行，也没有新外发。下一步先解决真实对象存储的可用运行条件，
不要为赶进度关闭加密、把内存结果登记成成功或重发规划请求。
同版本原生 MinIO RELEASE.2025-09-07T16-13-09Z 已下载至 `artifacts/native-minio.EU6APN`，SHA-256
`7c3b3039b76e55a1b80935848ed83998d5e8d317374f87851f46a019ff5c0aa4` 与官方 GitHub release asset digest 一致。
新增 `backend/tests/sql-runtime/minio-probe.py`：私有项目目录、摘要核对、仅 loopback、96MiB Go 内存目标、
256MiB 观测 RSS 上限、20 秒启动等待、版本化 SSE-AES256 读写及 finally 停止；不安装系统服务，不使用原存储。
实际启动前磁盘从约 2.0GiB 降至约 1.0GiB，低于 1.5GiB 预设下限，探针在创建密钥/数据及启动进程前拒绝。
证据 `artifacts/native-minio.EU6APN/probe-result.log`；脚本语法通过，加密读写尚未执行。没有启动 MinIO、PG 或 Docker，
没有新模型请求。下一步先恢复磁盘余量；不降低资源门槛、不重置现有 Run，也不声称存储已经可用。
磁盘随后最低约 339MiB。核对原始发布包 SHA-256 和已安装动态库路径后，回收本轮自行解压/构建的 PostgreSQL、
OpenSSL 两目录约 320MiB，原压缩包/安装目录/日志/数据库均保留，可重新解压构建。已装 postgres --version 正常。
其后空间回升至约 4.3GiB，幅度大于本轮清理，不归因于本轮全部释放。按原门槛启动同一 MinIO 探针成功：
版本化 SSE-AES256 写入及按版本回读通过，观测峰值 RSS 196,752KiB（约 192MiB），服务已自动停止。
证据 `artifacts/native-minio.EU6APN/probe-run.log`。私有密钥、存储数据保留在该忽略目录，不能丢失/提交 Git。
这不是生产 KES/TLS/独立 IAM 验收；下一目标是启动此隔离存储和已有 `data.DZSVON` 数据库，执行已持久任务，
不重新规划、不重复模型请求，完成候选保存及独立回读。
此目标已在原 Run 上实跑：`minio-probe.py --resume-run` 复用加密存储，`native_task.py` 使用现有生产 Worker、
上下文 Skill、Postgres 保存端和独立成果读取/校验端；NoNewPlanning 明确拒绝任何重新规划，最多三个工作步骤。
日志 `artifacts/native-minio.EU6APN/task-execution.log`：TASK_SUCCEEDED/event 5 → VERIFICATION_PASSED/event 7 →
WAITING_HUMAN。数据库实查 READY_FOR_REVIEW，任务认领次数 1，成果候选 1；工作计划晋升为 CANDIDATE/version 1，
含一项 ACTIONABLE REVIEW、NOT_APPLICABLE 交付目标。外发记录仍只有原一次开始/成功，没有重复模型调用。
MinIO 与 PG 均已正常停止。核验者是不同数据库角色及不同本案 SYSTEM_WORKER；测试存储仍共用 root 凭据、loopback
HTTP 与本地测试 KMS，不能证明生产独立 IAM/KES/TLS。当前成果只是合成代理档案的上下文审阅，非答辩状/案件风险
全面分析或法院提交包。下一目标：只读审查真实候选内容及律师复核入口，基于缺口接入代表性合成案卷，不把这一个
REVIEW 任务冒充完整文书工作计划；保留原模型与执行记录，不重做已完成链路。
只读内容审查本轮受约 1.4GiB 磁盘余量限制，未启动存储/数据库，也未读取加密候选正文。
沿实际执行适配器检查发现：上下文生成器按已有来源状态分类汇总，并非独立法律攻防分析；
其单项标题允许 500 字，律师成果读取端却仅接受 240 字，可能使合法候选无法打开。
已将读取端对齐到 500 字，不截断来源或修改存量成果；新增 500 字保留/501 字拒绝检查，
成果读取模块 12 项轻量测试通过。这是读取合同修复，不是浏览器或真实存储回读验收。
下一目标仍为只读核对保留成果及代表性案卷接线；不重跑已完成的上下文任务、不增加模型请求。
该只读核对随后实际完成：余量恢复约 2.1GiB 后，使用新增互斥 `--inspect-run` 模式启动原隔离存储，
数据库会话默认只读，经生产独立读取端按对象版本读取，并检查长度/SHA-256、律师区块解析及运行前后完全一致。
原成果 `9f4e2aa2-6a38-559b-85e8-908b5c621a3c` 只有一个代理档案来源和一个 CASE_CONTEXT 区块，
正文为代理被告/一审/民间借贷/一般授权；open_questions 为空，没有证据分析、风险判断或文书。
READ_ONLY_INSPECTION_PASSED/event 7；没有执行 Worker、写入验证回执或新增模型请求，两个服务均已停止。
这揭示产品呈现缺口：“0项待律师确认”仅统计已提供对象，不代表整案无缺口；下一目标是明确覆盖范围与未覆盖事项，
再接代表性合成案卷，不再重复此单来源试跑。此模式仍是隔离合成检查，不是正式律师授权读取/浏览器验收。
覆盖说明已接入现有律师成果读取投影：CASE_CONTEXT 列表/阅读标题统一为“案件台账核对（非整案分析）”；
按解析后的来源种类/编号去重统计，不使用候选 summary_counts。仅代理档案时明确未覆盖证据、攻防、法律适用和文书，
其他情况也不把对象数量冒充覆盖率；零待确认项不代表无风险或可提交。沿用现有 review_notice 展示，不改原候选字节。
13 项成果读取轻量测试通过，含代理档案单来源、虚假统计不采信、原始内容不变及来源链接保留；浏览器未实测。
已定位 `docs/GOLDEN_CASE_SYNTHETIC.md` 与现有独立复算脚本；该规格是合成样本设计，不代表材料文件已入卷或法律口径
已核验。下一步查找已有生成案卷及受管输入接线，复用现存材料，不重新创造单代理档案样本，也不直接把规格答案喂作证据。
进一步核对生成器发现 PDF 含 @GC_TX/@GC_ID 机器标记，JPEG 辅助页读取直接取 EXIF 270；
受管验收脚本还会调用 _confirmed_facts 预置确认事实。因此此旧夹具保留作“预整理事实→分析/文书”接线验证，
不能直接当自主原始读卷验收。脚本成功报告新增 evidence_scope，明确不证明自主读卷、视觉 OCR、律师认可或商用；
聚焦报告范围测试通过，未执行验收命令、未改历史报告。尚未接入无辅助标记案卷。
下一目标调整为复用冻结合成案情生成独立的无机器辅助标记原始材料版本，并通过真实上传/读取链进入候选；
参考答案仅供独立评估，不预置事实确认，不修改旧生成器默认产物、历史 M2/M3 或失败记录。
新增显式 unassisted_draft 生成模式及独立 draft schema，旧默认模式保持不变。草稿不包含 @GC_TX/@GC_ID、
JPEG EXIF 270 辅助内容、材料陷阱说明或外币处理答案；旧元数据读取器对新 schema 明确拒绝，必须走生产读取链。
7 项生成器检查通过，覆盖 11 文件/88 页、精确重复保留、相似不同文件不合并、标记移除及旧读取器拒绝。
实际草稿保存在 artifacts/unassisted-source-draft，未上传/入库/调用模型。PDF 技能要求的实际渲染发现中文缺失，
Poppler 同时报 Adobe-GB1 映射/字体配置错误；当前预览不合格，不能交作验收材料。旧模板本身仍是简化材料，
去标记不等于真实案卷或完整诉讼文书。下一步修复草稿字体可移植性并补内容真实性，随后才接受管入口；保留当前失败预览。
补充验证：仅配置本机 Poppler 字体/映射路径仍未显示中文；新草稿已复用产品现有嵌入字体策略，旧模式继续使用原字体。
新隔离产物 artifacts/unassisted-font-pawxzag9 的法院材料第 2 页实际渲染中文可见；7 项生成器测试通过，
并增加 PDF FontFile2 嵌入检查。该页面只有两行主张摘要，说明旧 88 页模板不能单凭去标记成为真实案卷。
尚未完成整套排版/内容验收或入卷，未声称提交件。下一目标优先补关键文书的真实结构与来源正文，而非继续堆页数。
F1 新草稿已补入完整诉请、原告事实理由、证据目录、被告陈述及规格内程序信息，保持 F1p2/F1p5 等来源页位置。
双方关于第二笔金额、本金是否归还、现金/外币/周转款的主张保持分离；不注入时效答案或事实确认。
新增按实际字宽换行，沿用嵌入中文字体；7 项生成器测试通过（含关键诉请、两方主张、无评估答案）。
artifacts/unassisted-court-g763df86 的起诉状第 2 页已实际渲染可读，尚非整套视觉或内容验收，其他材料仍有简化内容。
旧夹具/历史失败产物保留，未入库或调用模型。下一目标：新正文经生产 PDF 读取器形成来源绑定结果，不走 golden 元数据读取。
生产字节读取已实跑：native_materials.py 核对新草稿清单和实际文件哈希后，复用 read_authorized_pdf_document，
7 PDF/84 页全部返回原件哈希、页号和文本 SHA-256；4 JPEG 保持 NOT_READ_REQUIRES_VISUAL_ADAPTER，未读辅助元数据。
仅本地合成文件授权句柄，不是 Web 授权、持久来源 ID、上传入库、OCR 或 Agent 成果。未启动服务/模型，未改原件。
下一目标为真实入卷登记→页级授权→受管读取结果保存；不继续重复本地解析测试，也不向现有单来源 Run 伪造追加任务。
真实入卷环境核对：当前宿主磁盘约 1.0GiB，低于原生存储 1.5GiB 启动门；PATH 与已装 bundled dependencies 未找到
clamscan/clamdscan/clamd，当前无 PG/MinIO/clam 进程。Compose 声明的 /opt/lawcase-clamav/clamdscan 是容器包装器，
实际调用 /usr/bin/clamdscan 并连接共享 Unix socket 的独立 daemon，不是宿主可用扫描器。WebMaterialUploadService
必须取得真实扫描回执才能接后续对象存储及原件登记，不能用合成白名单或 mock CLEAN 代替。
本轮未启动 Docker/原生服务、未下载新运行时、未修改业务代码。该路径下一步需获得内存和磁盘充足的测试环境；
远程主机、费用和数据路径仍未获指定，不擅自部署或转移资料。本地继续开发不能替代这一真实入卷环境要求。
启动时发现 pg_ctl start 不保留前次命令行配置，第一次连接因此失败且执行零项；空库曾短暂使用 localhost 默认监听
（初始化时 host 认证已设 reject）。现已将 Unix-only/16MB 等参数持久化在该隔离库配置，并正常停止实例。

### 正文修订当前状态（合并重复阶段记录）

需求与边界以 [ADR-0086](decisions/ADR-0086-lawyer-content-revision-proposals.md) 为准。以下均为源码状态，
不是功能上线、律师认可或用户验收。0078 尚未部署；没有正文生成任务或可交付后继文书。

| 链路 | 已实现 | 尚缺的验收或接线 |
|---|---|---|
| 修改与保存 | 原文旁单段修改、理由、既有来源；只追加提案；当前身份/案件/版本和来源二次核验 | 真实数据库行级权限、并发与 HTTP/浏览器操作 |
| 读取与恢复 | 20 项游标分页、旧版标记、详情完整性检查；按原请求键只读核对 | 浏览器刷新、身份切换、存储失效的实际旅程 |
| 技术复核 | 原稿重放明确修改，比较候选、差异、来源指纹与作者 | 独立部署身份执行；技术完整性不等于证据支持或法律正确 |
| 生成授权 | 本案主办/复核人授权 GENERATE_REVIEW_COPY；同事务登记已有 revision_requests；页面修改详情填写复核说明并授权；身份命名空间前置匹配 | 真实提交/回滚、触发器和浏览器操作；仍不等于终审 |
| 正文入队 | 0078 为已校验的正文请求增加同事务 READY 任务与初始审计；应用角色仍无直接任务 INSERT 权限 | 迁移未部署；需降权后的真实 RLS/触发器、原子回滚与幂等验收 |
| 后台取稿 | 真实 Worker 身份、自身有效认领、版本栅栏、当前根包/来源/授权人核验；要求已有版本请求 | 实际 RLS/租约竞争；不借用律师身份 |
| 编译与登记合同 | 显式修改的 DOCX；正文模式/认领回执、0078 插入守卫及应用写入参数/事务复查已接线，旧模式 SQL/v1/v2 兼容 | 实际 SQL/写入验证、独立文件复核与真实 PDF 排版；生产入口仍关闭 |
| 持久执行 | 正文消费者串联认领→授权重放→渲染/登记→独立读取→回执；成功回执通讯失败最多原样补交一次，明确冲突不重试，绝不重渲染 | 44 项交付/修订轻量测试通过（含模拟回执丢失）；实际事务、已持久 UNKNOWN 的文件对账与恢复仍未验收 |
| 后台装配 | 现有入口按显式开关装配正文与模板消费者，轮换优先级，每轮最多一项；启用前双身份只读结构预检 | 未开启部署配置；预检不代替 SQL/RLS/真实转换验收 |
| 追加恢复存储 | 未部署 0078 增加不可变记录、写入守卫、统一结果视图及同事务 UNKNOWN→SUCCEEDED/原审计触发；必须匹配原认领，任一步失败回滚 | 76 条 SQL/9 函数仅解析通过；仍无运行角色权限，生产写端及现有读取未接线；真实 RLS、行锁权限、事务竞争及 21 项 ACL 检查未执行 |
| 恢复写端 | 独立核验身份按原请求读取登记与原 UNKNOWN 摘要，重读三份文件、匹配坐标、构造数据库同域摘要后幂等追加；无渲染/对象写入能力，默认关闭 | 25 项相关模拟测试与 4 条写端 SQL 解析通过；没有生产装配/调用入口，恢复角色权限与统一读取仍待接线 |
| 当前结果读取 | 生成状态、当前包、模板请求状态、正文取稿前驱与终审事务按迁移能力选择统一结果视图；原审计/回执幂等与 UNKNOWN 原证据仍读原表 | 86 项相关模拟测试及 5 条 SQL 解析通过；新视图已存在但无权限时拒绝而非回退；0078 必须与最小读权限一起发布，SQL 后继守卫及生产入口仍待接线 |
| 数据库结果接线 | 0078 正文请求/登记/回执竞争检查使用统一结果；前向替换两个既有模板守卫的读取关系，原 0073 不改；三角色仅获案件范围 SELECT，无恢复写权限 | 80 条 SQL、9 函数、1 个 DO 及两份改写后函数体解析通过；27 项实际 ACL 检查未执行；生产恢复入口与写权限仍关闭，整迁移未验收 |
| 单次恢复入口 | 复用部署配置，指定案件/运行/原请求；显式启用、只读预检后独立核验并追加，不构造 Agent/模型/渲染链。0078 仅核验角色可受守卫 INSERT；NOLOGIN 锁角色经受限函数提供行锁，不向核验角色授予案件修改权 | 34 项相关模拟测试通过；93 条 SQL/10 函数/1 DO 仅解析，37 项实际权限检查未执行；没有启用、调用或恢复历史任务，真实权限/事务和律师操作入口仍未验收 |
| 成果读取 | Web 读取/下载接受已独立核验的正文后继，原成果链接解析到当前版本；检查正文认领编号及 DOCX 类型 | 当前版本查询与独立存储读取仍需真实数据库/浏览器验证；不代表已有生成任务 |
| 生成状态 | 未知时核对原请求登记、当前授权人与原 UNKNOWN 回执摘要，再复用独立访问端读取三份文件；UNKNOWN_FILES_VERIFIED 仅是本次读取，不开放下载或终审 | 24 项后端、7 项前端协议测试、相关 ESLint 及 SQL 解析通过；实际 RLS/对象库/浏览器未验收；持久恢复须统一接入版本/守卫/终审，方案见 ADR-0086，尚未实现 |
| 终审前读取 | 修复把状态页当成成功复核稿的漏洞；必须返回正确文书 DTO、当前版本、可下载且有 PDF 页数 | 修订版本/文件指纹与律师终审事件的完整绑定尚未接通，不能宣称旧批准失效已完成 |
| 审阅版本标识 | 已核验文书返回包身份/回执绑定的 review_version；界面读到变化或无标识时清除旧文书操作记录 | 终审请求/不可变批准及下载并发的服务端版本核验尚缺；前端记录不是审批证据 |
| 下载与终审身份 | 下载携审阅版本、服务端匹配且回传，客户端不匹配不保存；三种原成果入口统一返回根候选 review_artifact_id | 实际网关请求/响应头、浏览器下载及并发换版尚未验证 |
| 审批版本合同 | RunFinalReviewApproval v2 绑定完整文书编号/版本集合；Web 比较，持久事件事务在写前按根链排序加锁并重读当前 PASSED 版本；未知重放不改清单 | 未部署；真实锁竞争、事务回滚及候选/下载入口身份映射需验收 |
| 终审恢复 | 原请求/审计/完成事件重算摘要，并与实际审批投影交叉核对审批人、版本摘要、任务图和事件序号 | 真实数据库恢复和 v2 全链路故障演练仍待执行 |

恢复信息仅保留请求键、版本、时间；sessionStorage 按操作者命名空间/案件/运行/文书/段落隔离，不存正文或令牌。
存储失败不发送；缺失记录只表示 UNCONFIRMED，不据此重发；完整详情或明确拒收才清除。关闭标签页不承诺保留草稿。
正文请求不进入旧模板 inbox，不覆盖原件、不继承批准、不自动终审或提交。NEXT_PUBLIC_DOCUMENT_CONTENT_PROPOSALS
默认关闭，本机简化 API 不展示入口。文书存储 enable_content_revisions 默认 false，访问数据库或写对象前拒绝正文模式；
消费者要求显式启用与匹配执行/校验身份。LAWCASE_AGENT_WORKER_DOCUMENT_CONTENT_REVISIONS_ENABLED 默认 false，
要求文书交付同时启用，并检查双数据库角色、四表强制行级权限、入队/任务触发器、认领列和禁止直接入队权限。
当前部署没有开启该开关，未部署的入队触发器尚未创建任何实际任务。

此前源码复核：97 项修订消费者、来源绑定、文书交付/持久层、Web 服务与装配轻量测试通过，包含正文/旧模式写入参数对齐、
渲染任务/候选授权查询合同、计划/执行人不符拒绝、回执模式/坐标区分、
旧摘要兼容、认领编号边界/回执绑定、原稿重放、同键重复、登记异常传播及未接通时零数据库/对象写入。
共用回执写入器按请求锁后先读；同结果不重复 INSERT，冲突结果拒绝，成功只用独立校验身份，未知不写成功包。
这修正了 ON CONFLICT 仍会先触发 BEFORE INSERT、从而拒绝已成功正文任务重放的问题。
正文消费测试在内存生成 DOCX 并核对修改保留、处理顺序、错误候选拒绝、登记/渲染未知和成功回执失联不重渲染。
数据库、独立读取端口及 PDF 转换使用模拟实现，不证明实际装配可用；未知结果的后续对账仍未闭合。
本轮修复 Web 仅接受模板修订、拒绝正文后继的读取断点；10 项 Web 文书服务测试通过，新增原链接读取修改正文及
Word/PDF 下载、错误版本/回执/认领编号拒绝。模拟包读取和文件字节不证明真实文书已生成或实际浏览器可用。
后台装配、运行循环和修订模块 46 项轻量测试通过，包含关闭时不构造正文消费者、开启时复用服务装配、
轮换/故障隔离、缺结构项拒绝和双身份只读查询。装配工厂、数据库及渲染服务使用模拟，不证明真实运行。
状态读取变更后，44 项后端修订/交付及 5 项前端协议测试通过；只读状态覆盖缺失记录、回执冲突、
过期租约和成功待复核，旧服务无状态时显示不可判断，不推断未生成。62 条 SQL/7 个函数解析通过，未执行数据库。
页面生成授权接线后，9 项前端协议/恢复存储测试、19 项 Web 服务/运行装配测试及相关前端 ESLint 通过。
授权后详情读取失败保留恢复编号；只有明确的写请求拒收或已读到生成授权状态才清除。切换身份由服务器命名空间
预条件拒绝；浏览器只保存编号、版本、时间。以上不含真实 HTTP/OIDC、点击旅程或数据库事务测试。
终审前状态读取修复后 36 项终审/控制/文书服务测试通过，覆盖 GENERATING、UPDATE_REQUIRED、FAILED、UNKNOWN
状态页、错误成果编号、模板不符、缺 PDF 和非类型化返回值拒绝。它不证明修订批准指纹绑定已经完成。
审阅标识接线后，24 项文书/终审/运行服务测试及相关前端 ESLint 通过。验证相同包标识稳定、正文后继改变、
不可下载状态不携标识。界面清理操作记录仅有源码/静态检查，未做浏览器旅程；尚不能排除服务端下载/终审并发换版。
审批版本合同修改后 63 项状态机/持久编解码/Web 控制测试通过，覆盖旧 v1 原字段与摘要、v2 JSON 往返、
版本变动摘要变化、重复/外来文书及非法摘要拒绝。没有真实 SQL 事务执行；空绑定保留历史 v1，未来文书终审入口必须要求 v2 完整绑定。
Web 接线后，36 项终审/控制/运行服务测试、9 项前端计划协议测试及相关 ESLint 通过：完整清单匹配通过，
缺失/重复/外来编号和换版拒绝，客户端负载复制版本清单避免重放时被新状态改写。新文书终审拒绝空清单；
无文书任务及历史同键完成恢复仍保留原合同。服务端读取与事件写入之间仍有并发窗口，必须补事务核验后验收。
该窗口现补应用写入事务内检查：运行/案件授权锁之后，按稳定根包顺序获得与修订守卫相同的链锁，再重读
当前 PASSED 文书包并比较 review_version。未解决请求（含 UNKNOWN）、缺少根包或版本变化阻断事件插入；
要求 READ COMMITTED 以保证等锁后重读新快照。原幂等回执仍在这些检查之前返回，不改写历史完成。
变更后 65 项持久/状态机/Web 控制测试通过，新增模拟连接验证锁顺序、先锁后读、换版/未知拒绝；4 条 SQL
通过 PG16 语法解析，未执行真实数据库，不据此宣称实际锁竞争/隔离或直接 SQL 写入边界验收通过。
下载及入口身份接线后 65 项文书/Web/修订测试、6 项终审读取测试、7 项前端协议测试及相关 ESLint 通过。
模拟验证同版 Word/PDF 下载、换版拒绝、三种链接共享根候选身份，客户端错误响应版本不保存文件。
请求版本头保留旧调用兼容，但当前律师 UI 必须携版本且核对回传；旧服务无根候选编号不计入终审。
恢复读取修复后 65 项持久/控制/状态机测试通过：即使请求账本和审计摘要彼此相同，只要与实际完成事件负载不同，
也拒绝返回恢复成功。旧测试曾用任意摘要占位，现改为实际请求摘要并加入不一致负例；这仍是模拟连接证据。
审批投影交叉核对后 47 项持久/Web 控制测试通过，覆盖审批记录缺失、摘要/审批人/任务图/事件序号不符拒绝。
查询使用 LEFT JOIN 保留“原请求存在但审批缺失”的错误，不伪装成未找到原请求；新增查询通过 PG16 语法解析，未执行数据库。
内存 DOCX 测试和模拟 PDF 转换不能证明真实渲染、实际事务、RLS、并发或整案可用。此前 7 项前端协议/恢复测试通过，
不替代浏览器验收。本轮未启动 Docker、执行高内存 SQL 测试、部署或调用模型。

SQL 证据边界见 [可选 SQL 检查说明](backend/tests/sql-runtime/README.md)：此前最小夹具复现默认角色授权缺口，
修复后 6 项实际 ACL 查询通过，但早于当前新增策略/登记段；当前 14 项表/列权限检查未执行。全迁移 0048 的旧失败来自
测试未复现真实部署的临时管理窗口，不能归为已证实的产品故障。引导已修正、全迁移仍未通过。
PGlite 曾用约 1305 MiB RSS，不作为日常轻量测试；PostgreSQL 18.3 WASM 不能替代生产 PostgreSQL 16。
正文入队 SQL 已增加仅限 schema owner 的 INSERT 策略，覆盖 FORCE RLS 下的人类授权事务及初始审计，
不扩张人类任务读取/修改权限。变更后的 60 条 SQL、7 个函数通过 PG16 语法解析，未执行；
可选高内存脚本增加 Web 直接入队拒绝检查，现共 15 项权限检查，本轮只检查脚本语法，不运行数据库。
本轮新增低内存语法证据：开发环境 pglast 6.16（PG16 解析器）解析当前 0078 的 55 条 SQL、6 个函数，
发现并修复回执条件中 CASE 表达式的括号错误后通过；实测峰值约 41 MiB RSS、无数据库启动。
该解析不验证表列是否存在、权限、事务或并发；解析器仅开发使用，不进入应用发布依赖。

正文 SQL 守卫源码现检查当前计划/快照/根包、授权人与候选指纹，并锁定 RENDERING 任务验证执行人、租期和认领编号；
迁移未执行，不能据此声称数据库隔离或并发通过。下一单一目标：处理正文生成 UNKNOWN 的文件/回执对账，只恢复可证明的已登记结果，不自动重渲染。
优先闭合现有链路，不增加平行注册表，不以新增检查数量替代可交付文书。

### 审计整改执行阶段（用户已授权，优先于旧的单一下一目标）

- 需求：把正常律师交付链打通，同时控制项目体积；不以恢复历史失败件代替正常产品能力，不机械重跑案件。
- 顺序：先修正终审清单，再设计并实现有来源的分析输入与分类失败恢复，随后推进律师修订闭环与用户视角验收。依据 `docs/PROJECT_AUDIT_2026-09-05.md`，不整体重写。
- 本阶段计划与已完成源码：API 从当前激活计划投影所需文书类型；前端按该清单要求打开和取得可编辑稿/PDF，支持答辩状单独交付与混合交付；缺清单不放行，清单变化使旧审阅记录失效。下载不被表述为实际认可内容。后台终审、权限、版本和恢复候选隔离门不变。
- 复核：后端控制/终审 25 项、前端终审 8 项针对性测试通过；前端全量类型检查被 `src/app/layout.tsx` 的 `LayoutProps` 类型缺失阻断，不能宣称全量检查通过。未进行模型调用、历史案件写入或部署，未经律师及用户验收。
- 运行风险：本次实测宿主余量约 3.2GiB，仍低于 5GiB 完整构建门槛；不能将源码修复称为网页已上线。
- 第二阶段已完成源码：ADR-0084 替代 ADR-0080 的全面输入遮蔽。每条来源提供与正文标签对应的结构化日期/带币种金额，保留来源状态；不展开账号等普通数字，不计算差额，不改变输出数量校验与律师批准。请求显式标识 `SOURCE_BOUND_QUANTITIES_V1`，未外发任何材料。
- 第二阶段复核：分析合同、传输、独立验证器和封存恢复共 50 项测试通过；前端根布局改用显式 `ReactNode` 子节点类型后，全量 TypeScript 检查及该文件 ESLint 均通过。未部署、未完成真实模型质量评估或律师验收。中文大写金额、简写日期与比例读取仍是已知能力缺口，不能将本次投影改进称为原始材料全理解。
- 当前部署风险：最新实测约 3.5GiB 可用，仍不满足 5GiB 构建门槛；没有绕过门槛或清理案件/会话数据。
- 第三阶段已完成源码：ADR-0085 在新 Web 运行创建事件中记录独立分支继续策略；旧事件缺省仍停止。局部已知失败不再阻断独立 READY/RUNNING 工作；独立待审批工作仍需审批，失败依赖仍停住，无剩余工作时不能进入整案验证或完成。状态说明区分局部继续、待审批和受阻，不再一概要求律师补资料。
- 第三阶段复核：状态机、持久层及 Web 控制共 63 项测试通过，覆盖新旧策略、独立分支、失败依赖、审批及策略序列化；未部署、未操作历史案件或调用模型。此次不代表错误自动修复、部分文书终审或商用验收已完成。
- 下一单一目标：将前三项整改接入可运行的统一验收版本，处理剩余运行环境阻碍并用浏览器确认正常交办与成果链；随后完成律师实质修订闭环及原始材料独立样本验收。不能继续仅以单元测试替代用户路径交付。
- 运行恢复阶段的新证据：Docker Desktop 启动后曾实际返回 API、数据库、前端与文书渲染健康，且没有 Agent Worker 容器运行。但虚拟机日志记录北京时间 13:08:55 启动、13:10:34 正常关闭；后端在 13:09:52 开始 graceful shutdown。触发关闭的来源尚未查明，不能据此归因于磁盘、内存或项目代码。随后 HTTPS 443 连接失败，管理员 `verify` 的有界读取超时；浏览器未取得可用页面。已终止本轮卡住的只读 Docker 查询，未再次重启服务或改动案件。
- 此阶段未部署任何整改；最新空间快照约 4.8GiB，仍低于 5GiB 重建门槛。镜像清单未发现本项目无容器引用的可删镜像，不清理其他项目或会话历史。下一步运行诊断应定位关闭触发方，不继续反复启动和登录；在该阻碍解除前仍可推进既定律师修订闭环源码工作，但不能声称已完成用户路径验收。
- 修订闭环实施顺序：先建立绑定候选文件哈希、段落原文哈希、修改理由与当前授权来源的差异预览，再接入持久请求/API/界面，最后重新生成与复核。已在既有修订模块实现纯函数预览；它重读严格候选结构和绑定，不改原文、不保存、不批准、不调用模型。越界位置、重复修改、陌生来源、过期原文均拒收。该函数尚未接入用户入口，不能称为律师已可修改文书。
- 运行协调检查：查看其他任务近期记录，确认存在独立的磁盘整理工作，但未取得其触发 Docker 关闭的证据；不据此归责，也未向其他任务发送操作指令。

当前项目已经具备受控 Agent、真实模型外发、可追溯法源绑定和隔离文书渲染的同案闭环证据；它仍只是
**律师复核候选能力**，不是商用放行、律师验收或法院可直接提交的产品。

- **已配置与曾实际验证**：受管环境包含受控 API、独立验证器、隔离文书渲染、私有对象存储、最小权限
  PostgreSQL 和合成主办律师身份。ADR-0081 的恢复投影已部署到受管验收 API；在 Docker 运行面仍可读取时，
  PostgreSQL、对象库、身份、前端和边缘网关均曾被验证健康。其当前状态以下方“当前运行状态不可认证”为准；Agent Worker 被刻意禁用，其唯一已停止、可重建的本地容器已移除以回收
  空间，未触及任何命名卷或案件数据。测试入口只在固定 loopback 合成环境
  免除反复人工 OIDC/MFA，生产身份链不因此降级。恢复候选只投影给明确声明
  `sealed-recovery-v1` 能力的新版界面；未声明能力的旧界面只能读取普通候选，不能把恢复件误显示为
  正式成果。该能力声明不是授权，既有身份、案件角色和来源页门仍为必经条件。
- **已测试**：本轮定性模型投影、中文量化门、封存恢复、Web 控制和运行时门的 84 项后端测试通过，
  Python 编译、diff 检查和前端 ESLint 通过。M2 封存响应的只读回放仍是 0 模型调用、0 案件持久写入；
  ADR-0081 的固定 M2 恢复命令则仅写入 1 份独立恢复候选及其私有候选字节。边缘 API 在声明
  `sealed-recovery-v1` 时已实际返回该候选与 9 个律师审阅区块，并带“原任务仍阻断、不可作为文书或
  提交材料”的明确提示；未声明该能力的旧客户端实际只得到 1 份普通候选。
- **真实模型证据**：M2 只发生一次 `qwen3.7-plus` 外发，供应商以 `stop` 返回 11,506-byte 原始响应及
  7,767 token 用量回执。当前解释器只删除四处能由唯一来源独立证明的差额字面量，绝不把它们作为正式
  金额接受；经 ADR-0078 的注册条款投影后，生成 `NEEDS_LAWYER_REVIEW`、`court_ready=false` 候选。
- **M3 是真实失败证据，不是成功**：独立的 M3 合成案只发生一次 `qwen3.7-plus` 外发，供应商响应已私有
  归档并由账本确认。模型把两项来源金额相减后，在残余风险中写成可能的现金交付/借款事实；该数额不在
  任何字段来源绑定内，任务以 `LAWYER_ANALYSIS_OUTPUT_REJECTED` 失败，运行停在 `WAITING_INPUT`，没有
  决策包候选、文书或提交材料。该事实证明当前真实受控链可发出、接收、归档并失败关闭，但**不证明**当前
  正常状态机已经交付可审阅文书。
- **ADR-0080 修复边界**：未来模型仅接收稳定的定性量化标签和来源编号，不能看到原始日期/金额；自由文本
  只能定性表达，中文金额、日期、比例和期限也在本地拒收。不得把 M3 的“现金交付或其他形式借款”文字当作
  差额展示而扩展 ADR-0077 脱敏；M3 不重跑、不改写，且不得创建 M4 或自动重试。
- **M1、M2、M3 的历史状态不可改写**：M1 仍为 `RECONCILIATION_REQUIRED` 且 inbox 为 `QUIET`；M2 仍为
  `WAITING_INPUT`、事件版本 8、外发台账 1 条；M3 仍为 `WAITING_INPUT`、一次外发及失败回执。三者均不因
  回放、部署或修复而被改记为成功。
- **ADR-0081 恢复边界已实测**：迁移 `0077` 已应用。M2 的唯一恢复候选为
  `NEEDS_LAWYER_REVIEW`、`court_ready=false`，与原失败任务分表保存；它不写入正常
  `case_agent_artifacts`、Agent 事件或验证回执，不能进入终审、文书、锁包或提交链。实际读取同时核验原
  图、快照、事件版本、失败回执、唯一外发和策略哈希；没有重发模型。
- **交付/权限边界已实测**：对 M2 的只读数据库审计确认其持久化文书包、文书修订请求和当前可交付文书
  均为 0。对同律所的临时活动用户（未授予目标案件角色）执行与 Web 文书授权相同的 SQL 门，已存在且
  通过独立核验的候选文书返回 0 行；该事务完整回滚，测试用户计数为 0。
- **运行风险**：Docker Desktop 异常恢复后，数据库与套接字卷中只发现全零的瞬态锁文件；在 PostgreSQL
  已停止的前提下它们被移动为可追溯的 `.stale-20260905` 副本，数据库、对象存储和身份数据均未清空、
  重建或改写。随后只移除了已停止的可重建 Worker 容器和已无容器引用的旧 `local-v5` API 镜像，并先将
  ClamAV 切换到当前 API 镜像、实测健康；未触及任何命名卷或案件数据。随后核验并清理了仅属于本项目旧 API
  Dockerfile 的 943.6MB 可重建 BuildKit 私有缓存，保留仍被当前镜像共享的 800.3MB 缓存；可用磁盘曾一度回升至约
  1.5GiB。ADR-0082 现要求本机与正式管理员入口在任何 Compose 静态校验、停止消费者或镜像构建前都先核验 5 GiB
  宿主余量；本次审计时实时预检约为 2.3GiB，完整重建被硬阻断，且入口每次都会重新读取而不把单个空间快照当作启动依据。最新前端恢复提示虽已通过 lint，但尚未能安全地重新打包进前端镜像。当前 API 的两处
  小型安全修复通过本地验收 Compose 的只读源码挂载生效，不是可发布镜像；旧前端会被 API 主动隐藏恢复候选，
  而不是将其误标为正式成果，因此本机不能被称为稳态发布基线。ADR-0083 已将所有宿主 Python 源码覆盖从规范
  Compose 移至显式、非发布的封存恢复覆盖层；本机管理入口既不会加载它，也会在启动/运行核验中拒绝或报告此类覆盖。
- **当前运行状态不可认证**：清理可重建前端缓存后，Docker Desktop 后端进程仍存在，但 Docker Unix socket 的两次 5 秒
  只读 ping 和本机管理 `verify` 的 30 秒状态读取均超时。系统因此只报告“无法判断服务是否仍在运行”，不再将此前健康快照
  当作当前运行事实；本轮没有停止、重启或重建任何容器。
- **单一下一目标**：不创建模型运行。先在不触及其他项目或案件数据的范围内恢复至少 5GB 可用空间，重新
  建立可读取状态的 Docker 运行面并打包最新前端，再在用户明确选择的浏览器中完成律师候选审阅与恢复旅程，验证 M2 的候选可见、权限门和
  失败恢复状态不绕过审批或提交门。之后才以证据评估是否值得申请一条新的、独立、单次预算、不可自动重试的
  正常模式合成运行；不得为了“补成功”而机械创建 M4。

## 2026-08-28 当前事实状态（优先于下方历史状态）

此前案件 `1317cc5c-eb51-4bfe-be90-8f00436915b0` 的 6 份混合材料运行，已经证明受管规划、来源绑定和 DOCX/XLSX/PDF 生产链能够运行；但输出主要是案情审阅意见和付款台账，没有充分证明律师真正需要的争点、举证责任、对抗分析、策略选择、补证和程序行动。原 `PROVEN_FOR_INTERNAL_REVIEW` 定级已撤回为“底层工作成果链已实跑”。

2026-08-26 新增 88 页、11 个文件、47 条原始交易记录的全合成民间借贷金标案，并按 [ADR-0041](decisions/ADR-0041-tool-backed-lawyer-decision-package.md) 验收“律师案件决策包”。当前结果分三层：

- **律师内容合同：`PASS_WITH_CACHED_MODEL_EVIDENCE`**。恢复候选包含 5 项首要风险、8 项争点/举证责任矩阵、4 项对抗分析、2 套策略、6 个当事人问题、6 项行动、3 项律师决定和 5 段文书蓝图；8 类关键风险全部覆盖，证据/法源非法引用为 0。
- **确定性计算与权限边界：`PASS`**。四种金额情景由代码工具生成并独立核对 341 个字段，差异 0；模型没有生成正式金额，也没有事实确认、法律立场批准、情景选择、锁包或提交权限。
- **新鲜 Agent 端到端运行：`FAIL`**。独立运行 `fresh-v3-run-01` 已由 `qwen3-vl-plus` 正常返回完整响应和 usage 回执：16,947 tokens、0.055638 元、无重试；模型完成 8 个争点、4 组对抗分析、2 套策略、6 个补证问题和 3 个律师决定分析。但硬控制器拦截正式利率百分比，律师复核另发现时效追问主体倒置、策略与情景冲突及未经确认的时效结论。该运行永久保持 `BLOCKED_SINGLE_CALL_NO_RETRY`，不因正常结束改记为通过。
- **严格受控分析核心 v4：`IMPLEMENTED_OFFLINE_NOT_LIVE_ACCEPTED`**。[ADR-0044](decisions/ADR-0044-strict-lawyer-core-and-controlled-projection.md) 已把律师包迁移到 `qwen3.7-plus` 严格 JSON Schema。模型只负责证据强弱/缺口、攻防理由、策略执行条件/风险和登记决定理由；角色安全追问、主体与“已提出/仅预判”状态、策略目标与情景、正式利率、金额、期限、行动和文书结构全部由代码控制。v3 明确拒收，35 项聚焦测试通过。
- **调用费用与结构预检：`PASS_OFFLINE`**。基于同一真实输入快照的不发网预检确认 `json_schema`、`strict=true`、无 `max_tokens`；请求 51,832 bytes、Schema 15,961 bytes，按官方最大输出和北京公开原价计算的最坏费用为 1.093024 元，低于固定 1.20 元单次上限。该状态不等于模型真实返回通过。
- **成果渲染：`PASS_OFFLINE_FIXTURE_ONLY`**。8 页 A4 DOCX 与 8 页原生 PDF 已逐页通过中文字体、分页、候选标记和长日期/金额/ID 换行检查；它们来自离线合同测试，不是 v4 真实 Agent 交付件，新的真实响应必须重新编译与复验。
- **执业律师认可、真实客户与商用放行：未完成**。全部材料为合成材料，没有律师批准、最终锁定、真实匿名案卷、跨租户、备份恢复、供应商治理或跨浏览器证据。

本阶段不再重复登录、继续调参或机械复跑。下一单一目标是先完成 v4 失败样本回归、受管案件 Worker 接线和 v4 文书渲染门，然后只执行一次新的独立真实调用；通过门为严格结构化响应、语义硬门、零非法引用、确定性计算一致及 PDF/DOCX 逐页通过。通过后才进入浏览器律师旅程和匿名真实案律师验收。完整证据见 [律师 Agent 真实能力验收](docs/LAWYER_AGENT_ACCEPTANCE_2026-08-28.md)。

## 2026-08-15 当前执行总览

当前共享树已经从“只有 Agent 设计”推进到**代码级内部 Alpha，且具有局部真实运行证据**：统一 Web Agent、代码总控、持久 Worker、运行/规划记忆、受控搜索、可选视觉 OCR、整案情境候选、动态 Word/Excel/PDF 候选和独立校验均已有生产组合根。2026-08-24 在本机受管环境用 6 份脱敏混合材料执行了真实规划和 Worker：最终有界运行 `ac7087e8-f483-5389-8bd5-6c3beb86330a` 成功完成案件情境、DOCX/XLSX 与 3 个 PDF 页读取并生成 3 份受管候选；两张扫描件都真实调用 Qwen，供应商 exchange/outcome 均为 `SUCCEEDED` 且各保存 1600 字节哈希响应。运行时随后因内部重复解析响应信封把两项误记为 `QWEN_VISUAL_OCR_PROVIDER_TEXT_INVALID`，台账任务未执行。重复解析已修复，36 项聚焦测试通过，并用这两份已封存真实响应离线回放得到 2 份非空 OCR 文本、共 666 字符、0 次新网络调用。由于失败事件不可追溯改写，本轮没有继续创建第三个付费 Run；因此视觉候选入库、台账提取、冲突决定、重规划、独立验证和可编辑成果仍未形成同一 Run 的端到端证据。0046—0052 的台账到计划主干仍是代码级 `WIRED`；完整混合案卷仍不是 `PROVEN`，更不是可商用交付。

### 律师视角的能力真相

| 律师看到的能力 | 当前成熟度 | 当前代码事实 | 当前缺口 |
|---|---|---|---|
| 交代目标、查看进度、处理决定、查看成果 | `WIRED`（局部实跑） | Web/API/Worker 组合已接线；已有真实 DeepSeek 规划、任务状态迁移和 3 份受管候选证据 | 本轮经服务端控制层创建和批准，未形成浏览器终审与整案成果，未达到整案 `PROVEN` |
| PDF 自动读取、来源页候选和异常复核 | `WIRED` | 读取、候选暂存、独立回读校验已接线 | 未证明匿名真实案卷覆盖、恢复和漏页控制 |
| Word/Excel/图片等混合材料 | `WIRED`（DOCX/XLSX/PDF 已实跑，OCR 响应已回放） | 0041 的安全接收、Web API、生产运行时和律师材料页已接线；DOCX、XLSX、PDF 读取成功；两张图片的 Qwen 调用均有成功回执，修复后可从原封存响应解析 666 个 OCR 字符 | 失败 Run 不可改写，尚未证明视觉候选在同一新 Run 入库并被台账任务消费；不能宣称 6 份材料已端到端处理 |
| 动态代理情境 | `WIRED`（本地受管记录已使用） | 代理对象、程序阶段、诉讼地位和委托范围已有 Web 查看/确认/更新链；本地受管案件的 `CURRENT` 档案已实际作为规划输入 | 尚无执业律师对该匿名验收案的浏览器终审记录 |
| Agent 提取事实/交易并整组确认 | `WIRED`（代码级） | 0045 提供可恢复 DeepSeek 交换；0042 私有暂存且不推进版本；0048 从服务器持有的有效 Web 会话解析当前主办律师，分短时授权、来源回读和最终提交两段确认完整低风险组；Web 复核面板、生产组合根和数据库预检已接线 | 一次性本地 PostgreSQL 验证不证明真实 OIDC/MFA、私有 S3、DeepSeek 或律师浏览器确认 |
| 异常分流与后续事项 | `WIRED`（代码级） | 0047 固化完整异常组及四种受限路由；0049 将重新提取、补充证据和延期物化为有当前头的 `ACTIVE` 后续事项，接入 Web 分页/动作、受管来源、Worker 精确重新提取及控制恢复；未结事项在数据库层阻断计划晋升和激活 | 尚无真实对象库补证、律师大批量操作、Worker 中断恢复和跨浏览器证据 |
| Agent 图谱形成动态计划并由律师复核 | `WIRED`（台账到计划主干） | 0046/0047/0049 产生持久 snapshot refresh，Worker 追加 `CASE_SNAPSHOT_CHANGED` 后由 Supervisor `REPLAN`；`ACTIVE` 后续事项可驱动义务重规划但阻断 0043/0044，全部终结并重新验证后才由 0043 登记新 `CANDIDATE`、0044 律师整案激活；0050—0052 以持久恢复意图、旧/新 inbox、最小列权限、固定恢复目标和领取围栏保证原子控制转移 | 尚未在生产同构环境证明真实并发、杀进程恢复、浏览器激活和匿名案件全链 |
| 联网研究与官方法源 | `WIRED` | 搜索线索 Worker、持久外发和官方原文私有存取边界已接线 | 搜索线索到官方原文、效力核验和正式引用尚未闭环 |
| 短期/长期记忆与 RAG | `WIRED` | 运行检查点、案件记忆、权限/撤回和 ACL-first PostgreSQL 全文检索已接线到规划 | 缺知识库管理界面、真实 RLS 隔离证明和混合向量检索 |
| 利息测算 | `WIRED` | 正式规则、确定性计算和独立复算已有 Web 业务链 | 尚未由统一 Agent 从已批准计划自动组织 |
| Word/Excel/PDF 成果 | `WIRED`（可选，默认关闭） | 可恢复模型交换、来源绑定、独立校验、预览/下载已接线；生产 Worker 在显式启用时只使用带签名预检的隔离渲染侧车 | 尚无真实 LibreOffice 容器转换证据；律师修改/再上传、批准、锁定和最终提交包仍未闭合 |

详细成熟度、支持格式和不可宣称边界以当前执行基线的能力矩阵为准。

### 唯一用户路径

```text
登录并打开案件
→ 确认代理对象、程序阶段和授权范围
→ 上传材料
→ 向办案 Agent 交代整案目标
→ Agent 自动盘点、读取、OCR、分类、研究、核对和起草
→ 律师只处理冲突、低置信、法律口径和数据外发审批
→ 已确认候选进入正式台账并触发动态重规划
→ Agent 形成来源绑定的 Word、Excel、PDF 和提交清单候选
→ 律师修改、批准、锁定并导出
```

材料、证据、法律、计算和文书页面只是同一个 Agent 的专业复核工作区，不能继续建设成五条互不相干、要求律师手动推进的流程。

### 剩余 P0 / P1

P0 只做一件事：闭合一宗匿名混合案卷的真实内部 Alpha。台账到计划的权威状态机已经代码级 `WIRED`：验证后的事实/交易候选私有暂存（不推进案件版本）→律师确认完整低风险组（推进版本）→异常整组分流→后续事项真正终结→持久 snapshot refresh→基于最新台账重规划→0043 新 `CANDIDATE`→0044 律师整案激活；0042 确认和 0044 激活仍必须落在各自正确版本，旧计划不得复用。P0 现在要在生产同构环境证明这条链，并继续闭合搜索线索到官方原文、规则和确定性计算 → 隔离文书渲染、律师修改/批准和提交候选 → 整案实跑与 Worker 恢复。一次性可丢弃 PostgreSQL 迁移/聚焦集成运行不等于 OIDC、S3、DeepSeek、浏览器或律师全案 `PROVEN`。

P1 才处理封闭试用和商用放行：真实 OIDC/MFA、最小权限 PostgreSQL/RLS、私有对象库/备份恢复、供应商数据治理、文件与网络攻击、跨租户、浏览器矩阵、律所知识治理，以及律师匿名全案验收。代码、测试或容器能启动均不能替代这些证据。

### 下一里程碑

当前唯一里程碑为 **M1 混合案卷 Agent 闭环**：同案输入文本 PDF、扫描页、DOCX、XLSX 和 JPEG/PNG；Agent 自动覆盖全部可处理对象并生成来源绑定的案件情境、研究、计算及至少一份 DOCX、一份 XLSX 和 PDF 预览；律师主要处理异常和法律决定；Worker 重启后可继续，未知外部结果不盲目重发。台账确认后重规划的代码主干已是 `WIRED`，但 M1 还包含混合材料、真实模型/对象库、法源、计算、可编辑成果和恢复，因此整体仍只能定级为 `IMPLEMENTED`，尚未达到 `PROVEN`。完成 M1 只代表具备申请封闭试用的条件，不代表商用放行。

当前有界阶段已停止：唯一新 M1 Run `e11bef01-3e2d-567c-a219-fe9a3e3cb283` 在同一 Run 内完成案件情境、Office、PDF、JPEG OCR 和 PNG OCR，共 5/6 项并形成 5 份受管候选；台账任务在 0045 exchange 建立前发生本地失败，固定代码顺序及数据库缺少 exchange/outcome 证明供应商未被调用。Run 已取消并保留审计。随后仅完成一轮阻断调整：模型前故障改为 `FAILED / NOT_SUBMITTED / 0 external calls`，终态 Run 在任何刷新/回收前 `IDLE`，记忆检查点只锁 `run` 行。58 项针对性测试一次通过，新 Worker 启动，真实最小权限 run-row lock 通过，取消 Run 的 inbox 已收敛为 `QUIET`。本阶段不再创建 Run；台账结果、冲突决定、一次重规划、独立验证、可编辑 DOCX/XLSX/PDF 和律师浏览器终审仍未完成。

实施记录：`2026-08-15 核验 0046—0052 与当前 Web/API/生产组合根/Worker：低风险台账确认、异常整组路由、ACTIVE 后续事项、snapshot refresh、CASE_SNAPSHOT_CHANGED/REPLAN、0043 候选、0044 激活和控制恢复均已有 fail-closed 接线；0051 移除应用角色对律师会话 UUID 的读取，0052 把替代 Run 与服务端固定恢复目标不可变绑定。开放后续事项会阻断计划晋升与激活，恢复替代 Run 在持久意图转移前不可领取，也不能被普通建 Run 请求占用。本轮一次性本地 PostgreSQL 迁移/集成路径仅作为数据库合同证据，不升级真实 OIDC/MFA、私有 S3、DeepSeek、浏览器或律师匿名全案状态。`

实施记录：`2026-08-24 在本机受管环境用案件 1317cc5c-eb51-4bfe-be90-8f00436915b0 的 6 份脱敏混合材料执行真实 Agent。严格 DeepSeek 函数规划把媒体类型和 Skill 输入范围编译进 6 个任务；case_context_review、office_reading（2 份）和 pdf_reading（3 页）均一次成功，生成 3 份受管候选。Qwen 真实小图片探针通过，但同一 JPEG 案件页在受管任务中多次进入 UNKNOWN_SUBMISSION；45 秒与探针 120 秒的配置差异已修正并通过 32 项聚焦测试，但最后有界运行仍在约 3 秒收到不确定结果，证明还有非超时的供应商/交换契约问题。系统没有自动重发；PNG、台账提取、独立验证、律师冲突决定和动态成果生成尚未达到。`

实施记录：`2026-08-24 运行 ac8d950c-bd21-52f8-99b3-410aac1fe9fc 是修复后的唯一有界复跑。浏览器复用已验证登录，律师目标明确要求覆盖 6 份材料、冲突交律师和一次重规划；DeepSeek 仍编译 6 个媒体受限任务，整案、Office、PDF 三项成功并生成 CASE_CONTEXT_REVIEW_CANDIDATE、COMMON_DOCUMENT_REVIEW_CANDIDATE、PDF_TEXT_REVIEW_CANDIDATE。0055 已把完整 HTTP/响应拒绝改为 FAILED/SUBMITTED，并以 59 项聚焦测试、数据库 APPLIED 状态和 Worker 启动预检证明；本次 JPEG 仍在 3.186 秒进入 UNKNOWN_SUBMISSION，故现存阻断是真实传输/响应读取不确定，不是明确 HTTP 失败、余额或 120 秒超时。按本里程碑的停止规则不再盲跑；当前 Run 保留 RECONCILIATION_REQUIRED 恢复状态，PNG、台账、独立核验、律师冲突决定与可编辑最终成果仍未完成。`

实施记录：`2026-08-24 最终有界运行 ac7087e8-f483-5389-8bd5-6c3beb86330a 绑定案件版本 12 和 6 份脱敏混合材料。DeepSeek 规划生成 6 项动态任务；整案情境、2 份 Office 与 3 个 PDF 页读取成功并形成 3 份受管候选。Qwen 对 JPEG/PNG 各完成一次固定请求，数据库 exchange/outcome 均为 SUCCEEDED、各保存 1600 字节哈希响应，无 UNKNOWN 和自动重发；任务适配器因把传输层已拆出的 OCR 文本再次按外层 JSON 解析而以 QWEN_VISUAL_OCR_PROVIDER_TEXT_INVALID 终结。0059 已同步运行时/数据库策略哈希并加入 Worker 启动预检；重复解析已修复，36 项聚焦测试通过，两个原封存响应离线回放为 2 份非空文本、共 666 字符且 network_calls=0。失败事件保持不可变，本轮未创建第三个付费 Run，因此台账、冲突决定、重规划、独立验证、可编辑成果和律师终审仍未完成。详细证据见 docs/MANAGED_AGENT_ACCEPTANCE_2026-08-24.md。`

实施记录：`2026-08-25 唯一新 M1 Run e11bef01-3e2d-567c-a219-fe9a3e3cb283 在案件版本 12 上完成 5/6 项：案件情境、Office、PDF、JPEG OCR、PNG OCR 全部 SUCCEEDED，两份 VISUAL_PAGE_REVIEW_CANDIDATE 为 3119/3118 字节。台账任务在通用 STARTED 后、0045 exchange 前失败；数据库无 exchange、outcome 或供应商响应，故外部调用数实际为 0。Run 按首个新阻断停止并取消于事件版本 21。ADR-0039 将该类故障确定化为 LEDGER_EXCHANGE_NOT_CREATED、FAILED/NOT_SUBMITTED/0 calls，将终态 Run 提前静默，并把记忆行锁收窄到 run；58 项测试一次通过，Worker 真实身份锁验证通过，目标 inbox 收敛为 QUIET。本阶段不再复跑。`

## 以下为历史决策与实施记录

## 产品交付目标（2026-08-11 重新校准）

本项目不是“法律研究工具”或“安全能力展示台”，而是由律所部署一次、律师在任意受支持浏览器使用的中文办案工作台。律师的真实使用路径应当是：

1. 新建或打开案件；
2. 选择该案资料文件夹；
3. 让工作台整理材料、找出与对方有关的证据和待核对事项；
4. 确认案情、还款、利息口径和法律依据；
5. 生成可编辑的答辩材料、证据目录、核算表与法院提交包；
6. 律师逐项复核、锁定并导出。

产品首页和导航必须围绕这条路径组织；哈希、Worker、审批链、模型路由和安全策略只能作为每项结果的可查看保障，不能成为律师首先面对的功能入口。AI 的工作必须清楚可见：先整理、提示缺口、给出可编辑候选；事实认定、利息口径、对外发送和最终文书始终由律师确认。

商用放行标准不是“页面能展示”或“模型可调用”，而是该路径在真实受控桌面环境中可完成、可恢复、可追溯，且不会把合成数据、模型候选或未核验材料伪装成可提交结论。

状态：`律师主流程、材料收集页、案情/法律/利息/提交页与工作台设置已完成第一轮产品化重构；既有证据、法源规则、正式计算和法院提交包的受控代码链已经接入该主流程。此前完成的 macOS/Tauri 本机案卷仅作为可复用实现和迁移参考，不再是产品交付形态；新的主交付目标为可自托管的跨平台 Web 工作台。`

实施记录：`2026-08-12 Web 主入口进一步固定为浏览器工作台：各业务路由直接进入 WebLawyerWorkbench，/security 不再暴露桌面技术设置；修复证据页分页查询被浏览器 API 客户端错误拒绝的问题；材料页增加 Chrome/Edge 材料文件夹选择增强；应诉页接入服务端绑定的文书批准和材料包锁定动作，浏览器不接触审批哈希。ZIP/可恢复分片、真实部署基础设施和最终导出下载仍保持明确未完成状态。`

实施记录：`2026-08-12 Web 材料接收继续闭环：新增 ZIP 私有归档接收、解压安全校验、RLS 恢复记录和 PDF/ZIP 接收编号状态核验；前端支持 PDF/ZIP/文件夹增强，并对结果不明的上传提供服务器状态刷新而非盲目重传。ZIP 当前只到 STORED_PENDING_PROCESSING，逐文件 PDF Worker、断点分片和文书 Worker 仍未宣称完成。`

实施记录：`2026-08-12 Web 主流程继续收口：新增同案 PostgreSQL 台账的案件要点只读 API 与律师端页面，真实展示事实、诉请、争点和收付款，不从文件名、案件名称或模型推测金额、币种或法律结论；首页、材料证据、案件要点通过案件编号保持连续导航；法律与利息、应诉材料在 Web 服务端前置条件未满足时保持明确锁定。98 项 Web 关键链路测试、前端 ESLint/TypeScript、Compose 结构检查通过；未连接真实 OIDC、数据库、对象存储或浏览器，因此不宣称商用放行。`

实施记录：`2026-08-12 为 Web 案件要点页补入事实候选的律师决定入口：只有主办律师角色可确认/争议/否认，决定哈希由服务端根据案件、事实、版本和决定派生，浏览器只提交状态和幂等键；确认回执不返回内部哈希。该入口尚未替代完整事实、付款分类、法源和利息审批链。`

实施记录：`2026-08-12 Web 案件要点页继续补齐律师核对动作：主办律师可确认诉请范围和收付款记录，确认哈希分别由服务端绑定案件、对象、版本和固定状态派生；协办律师只能查看，版本冲突和重复操作沿用服务端幂等/审计链。100 项 Web 关键链路测试、前端 ESLint/TypeScript 和 Compose 结构检查通过；真实生产基础设施验收仍未伪造。`

实施记录：`2026-08-12 接入 Web 法律依据只读链：同案 PostgreSQL 法律台账向浏览器仅投影官方法源元数据、规则版本、法律事件、事实绑定和当前规则包状态，明确过滤对象存储定位；法律页面可以复核官方地址和规则参数，但不自动选规则、不推定起算日、不生成利息金额。`

实施记录：`2026-08-12 在 Web 案件首页增加服务端办案前置状态投影：材料锁定、事实/诉请/交易确认、官方法源、法律事件和规则包分别返回 READY/BLOCKED 及下一步说明；页面不暴露案卷正文或内部定位，也不把健康接口当作商用放行。102 项 Web 关键链路测试、前端 ESLint/TypeScript 和 Compose 结构检查通过。`

实施记录：`2026-08-12 将确定性利息测算接入 Web 案件页：主办律师只提交案件义务编号、计算期间和还款抵扣顺序；法律规则包由同案服务端已批准台账绑定，审批哈希由服务端派生，正式计算继续由 PostgreSQL 确定性引擎检查事实/交易/规则完整性并执行独立复算。新增当前测算读取、正式测算提交和浏览器安全结果投影；没有完整规则包、版本不一致或台账前置条件不满足时不显示金额。后端全量 637 项测试（4 项跳过）、前端 ESLint/TypeScript 和 diff 检查通过；真实 OIDC、数据库、对象存储和律师浏览器验收仍未完成。`

实施记录：`2026-08-12 将应诉材料页从锁定占位改为同案提交台账只读核对：显示文书候选、材料包生命周期、证据清单/法律规则包/测算绑定和导出核验状态；浏览器投影明确过滤对象存储定位，不提供未锁定或未独立核验的法院下载。提交包的创建、锁定、导出访问仍保留为下一条受角色和审批门控制的写链。`

### 产品形态校正：跨平台、自托管 Web 工作台

**已纠正的前提。** 产品不应要求律师安装 macOS `.app`，也不能把 Windows 使用者排除在外。正式形态改为：律所或其受托运维人员在一台受控服务器、私有云或律所内网服务器部署一次；律师在任意受支持的 Windows、macOS 或 Linux 电脑上通过浏览器进入同一工作台。桌面壳和 macOS Keychain 方案不再是主路径。

**必须保留的现实边界。** “任何电脑可用”不等于网页可以擅自读取任一电脑的硬盘。浏览器只能在律师明确选择后接收资料：Chrome/Edge 可优先选择资料文件夹，所有主流浏览器均须支持多文件上传与 ZIP 上传兜底。原始资料进入每案受管对象库后再被盘点、逐页处理和生成衍生件；绝不要求律师手填本地绝对路径，也不以浏览器模拟本机文件系统权限。

**目标架构。** 浏览器前端（Next.js）→ 受认证的 Web API → PostgreSQL（案件、权限、审批、审计）+ 私有对象存储（原件与派生件）+ 受控 Worker（PDF/Office/研究/模型）。服务器端密钥进入部署平台的密钥管理或受控环境，不进入浏览器、案件文件夹或前端构建物；模型和外部检索仍须逐案、逐次获得律师确认。多律所数据由服务端身份、逐案角色与 PostgreSQL 行级隔离共同控制，不能由网页参数决定。

**迁移判断。** 现有中文界面、案件领域内核、PostgreSQL 台账、证据派生器、PDF/Office 安全处理、法源/利息规则和可复用的本地 PDF 审阅组件可以迁移；Tauri、原生文件夹选择、loopback 会话、macOS Keychain、安全原生输入与 `.app` 打包链降级为历史实验代码，不再扩展。不能把当前仅允许 `tauri://localhost` 的受管路由直接暴露给浏览器，必须先建立 Web 身份、CSRF/会话、上传和对象访问边界。

**新的执行顺序。**

1. 固化 Web 产品决策和部署边界，建立可重复的本地/容器化 Web 运行基线；
2. 建立浏览器身份、律所/案件角色、短时 Web 会话和服务端密钥边界，替代桌面登记与 Keychain 依赖；
3. 实现跨浏览器材料接收：文件夹选择优先、多文件/ZIP 兜底、流式上传、服务端哈希、私有对象入库与可恢复任务；
4. 接入 PDF 逐页审阅、保留/排除、红框、锁定和两份可下载 PDF，使“微信流水筛选”成为第一条真实可用办案路径；
5. 在同一 Web 身份和审计边界下，逐步接回案情、法源、利息测算、文书和提交包；
6. 用 Windows 与 macOS 浏览器分别走完整案件流程，再进行私有部署、备份恢复、权限隔离和律师验收。

### 已暂停的桌面本机 PDF 切片

原定“本机 PDF 证据整理”不再按 Tauri/SQLite/本机资料根继续实现。已完成的纯前端 PDF 审阅组件可作为 Web 材料页的可复用界面；任何尚未完成的 native-only 后端工作均停止，待第 3 步改为受认证上传与服务器端对象存储后再接入。

### 已暂停方案记录：本机 PDF 证据整理

**问题判断。** 现有本机模式已经能真实建立案件、关联资料根并生成只读清单，但将“收集材料”锁住，仍不足以完成律师最先需要的微信/银行流水筛选。直接把受管 PostgreSQL 链强行降级到 SQLite，或直接开放整卷 OCR，都不是正确解法：前者会混淆审计边界，后者会在没有逐页授权时外发案卷。

**本轮目标。** 在不要求 API Key、律所登记、数据库服务或联网的前提下，新增一条本机 PDF 证据闭环：重新选择已关联资料根 → 明确读取当前 PDF 页 → 律师逐页保留/排除 → 在页面上标记红框 → 锁定选择 → 生成“相关页 PDF”和“标红相关页 PDF”。原件不改写，生成件只保存在本机私有工作区并可由律师另存。

**边界。** 首轮只处理静态、未加密、哈希未变化的 PDF；精确重复的整份 PDF 会明确提示，不能将“相似”误报为重复；不会自动判断当事人、还款性质、利息或法律结论。图片、Word、Excel 与 OCR 将作为后续受控扩展，不能为了界面完整而显示虚构页面或结论。

**实现计划。**

1. 为本机 SQLite 增加页级证据、追加式页面决定/红框、锁定清单和派生 PDF 元数据；每次写入绑定案件版本与审计事件。
2. 仅由桌面原生壳向 sidecar 传递短时文件夹选择标识；浏览器只能取得当前案、当前资料根、当前会话内的预览和操作回执，绝对路径不进入 SQLite、HTTP 回执或页面状态。
3. 复用已验证的确定性 PDF 派生器生成未标红和标红 PDF，重新校验来源哈希与页数；不能验证来源或坐标时停止生成。
4. 将“收集材料”开放为本机案件的第一项真实工作，并以律师任务流呈现，不出现演示案情、模型入口或技术设置。
5. 以真实临时 PDF 做端到端验收：选根、盘点、逐页处理、锁定、生成、打开，并验证重启后必须重新选择资料根才可读取原页。

实施记录：`2026-08-11 将中文桌面界面从“研究/安全仪表盘”重构为律师办案工作台：首页按“收集材料 → 核对案情 → 确定法律与利息口径 → 形成应诉材料”组织，明确 AI 和律师各自承担的工作；材料页按“选择材料 → 逐页筛选 → 标红并确认 → 生成提交版 PDF”组织，重复页、无关页和红框均以提交材料语义呈现；案情、法律、利息、提交及设置页同步移除面向开发者的优先表述，系统/审计/权限信息下沉到高级管理员层。演示数据继续显著标识，未被伪装为真实案件。`

实施记录：`2026-08-11 接通 DeepSeek 受控办案步骤规划：律师只能从五类固定任务中选择，不接受自由提示词、原始文件、网址或工具指令；服务端向原生层释放仅含案件阶段和项目计数的无案情最小快照，原生层固定使用官方 Chat Completions 端点与 deepseek-v4-pro，预授权、提交开始、成功/失败/未知状态均写入追加式账本。模型返回只能成为 Skill/Tool 白名单内的步骤清单，页面以律师可理解的步骤显示，绝不自动执行或替代法律结论。前端将严格使用“授权回执版本”而非预取版本发起原生调用。`

实施记录：`2026-08-11 重新打包并启动 macOS 桌面应用：新包已验证从办案首页进入“收集材料与还款证据”页面，sidecar 报告本机服务就绪；开发环境未配置律所登记和专用 PostgreSQL 时，桌面端现在显示明确的“本机工作台未就绪”而非伪造真实案卷或继续展示演示案情。前端构建、桌面编译、DeepSeek 链的 Rust/Python 目标测试均通过。`

实施记录：`2026-08-11 修复打包桌面从演示到真实办案的入口断点：前端只有在原生运行态确认 READY、已登记律师、信任目录与短时会话 READY、持久化资料库 CONFIGURED，并再次取得受限 desktop session grant 后，才在内存中开启案件工作区；令牌不进入页面状态或存储。没有已选案件时进入“新建或打开案件”，建案或打开后直接进入“收集材料”；未就绪时显示可恢复空状态和工作台设置，不用演示案情替代真实案卷。`

实施记录：`2026-08-11 新增 commercial-production 运行时与只读部署前置检查：商用模式必须使用独立明确确认、有效且以 _production 结尾的专用 PostgreSQL 名称，并沿用重新验签登记、OS 会话、Keychain 工件密钥、0700 受管对象目录及逐案角色复核；预览与商用确认不能混用。检查只核验静态形状，绝不连接数据库、读取 Keychain、执行迁移或宣称生产已放行；真实迁移/回滚、登记、签名公证、备份恢复与律师验收仍为单独门。`

补充记录：`2026-08-09 按用户要求加入开发工作区与备份基线；该补充不改变已审查的产品边界、法律责任边界和架构总控决策`

实施记录：`2026-08-11 补齐律师真实主流程的第一段“新建案件”：持久化 API 仅接受中性案件工作名称、服务端生成 UUID、主办律师角色从已登记会话导出，创建事务同时写入 matters、该主办角色、审计事件与幂等回执；桌面首页在持久化环境尚无案件时不再落到合成样例，而是提供新建案件入口。创建成功才激活本机会话内案件并开放资料文件夹接收，未预填当事人、金额、币种、期限或诉讼立场。真实专用 PostgreSQL、律所登记与首次桌面会话仍是启用该入口的前置门。`

实施记录：`2026-08-11 补齐多案律师的案件列表与切换：服务端仅列出当前已登记身份在数据库中仍有有效角色的同律所案件，前端不接受客户端传入的人员、律所或角色；切换只保存会话内案件 UUID，清除后回到受控案件选择页。新建案件与既有案件均会在无材料时直接引导至本地案卷文件夹选择，不把空案件伪装为已有事实、金额或利息结论。`

实施记录：`2026-08-11 将律师批准的本地案卷接收从“可排队”补至 sidecar 内可执行的受控 Worker：API 排队后只在当前进程内保存短时文件夹授权、OS 会话、案件版本和运行标识，数据库与浏览器均不保存本地路径或密钥；Worker 以独立 SYSTEM_WORKER 身份逐项领取短租约，调用本机 ClamAV、原件哈希复验和既有证据登记/规范化链。扫描或磁盘异常会先保留租约并等待到期后重试，第三次过期由追加审计命令明确标记为恢复失败；不会把“仍在处理”“授权已失效”或“没有可领取项目”伪装为成功。该 Worker 默认关闭，须在已登记持久化环境、受检本机 ClamAV 和显式运维开关同时具备时才装配；真实专用 PostgreSQL、安装包内扫描器供应、实际律师案卷全链验收仍是商用放行条件。`

实施记录：`2026-08-11 修复首次接收材料的桌面主路径：空案件进入“证据核验”时不再因“尚无来源页”遮住案卷文件夹入口；律师可在同一页面选择文件夹、确认短时只读授权、盘点、批准范围并建立材料接收任务。任务进入队列后，页面每 3 秒从持久化快照刷新已登记、等待/处理中、待转换、阻断/失败的真实计数；关闭页面不会中断已入队任务，读取异常则保留最后一份已确认状态而不显示虚假的完成结论。`

实施记录：`2026-08-11 将 native sidecar 的材料接收 Worker 状态纳入桌面运行状态：前端现在能严格区分“本机接收服务已装配”和“任务已建立但当前机器尚未装配接收服务”，并据此给出真实状态提示；就绪回执在 Rust 父进程仍保持字段白名单与固定状态值校验。该状态不携带文件夹路径、案卷内容、模型密钥或 Worker 凭证。`

实施记录：`2026-08-11 补齐通义千问百炼的受限调用地点配置：API Key 继续只保存在 macOS Keychain；Qwen 另需由操作人固定官方业务空间 ID 及“华北2（北京）/新加坡”之一，代码拒绝自定义 URL、其他地域和越界空间标识。桌面页只显示是否已固定地点和地域标签，不显示业务空间 ID；该配置只是后续 Qwen 单页 OCR 请求的必要前提，不自动发起调用、更不授权上传案卷。`

实施记录：`2026-08-11 将官方法源抓取 Worker 状态接入桌面与法律工作台：就绪回执严格携带固定状态值，法律页能区分“已入受控队列且本机抓取服务已装配”与“任务等待本机服务”；存在 QUEUED/RUNNING 法源任务时每 3 秒刷新持久化队列/哈希/失败码，暂时读取失败保留最后确认快照，不把抓取完成或解析完成伪造给律师。`

实施记录：`2026-08-11 开始接通商用单页 OCR 的本机执行边界：Qwen3.5-OCR 的 API Key 与业务空间配置只由 macOS Keychain 向原生传输层短时读取；原生请求固定为官方业务空间域名、固定模型、单张已验证 PNG、固定“逐字提取、不推断”指令和受控像素/输出上限，拒绝任意 URL、模型、提示词、文件路径或整卷上传。持久化 sidecar 新增只对持有父进程令牌的原生进程开放的单页材料通道，并同时绑定当前 OS 会话、案件、证据页和短时文件夹授权。该阶段尚未开放实际网络提交：外部调用预授权与提交/结果账本仍需接入，未经律师预授权不会发送任何材料或产生费用。`

实施记录：`2026-08-11 将单页 OCR 入口再绑定到外部调用预授权账本：Native 取页前，服务端会以独立 SYSTEM_WORKER 身份锁定案件版本，并核对请求类别必须为 OCR、服务商必须为 qwen、地域仅能为已允许地域、字段范围必须且只能是当前证据页、以及律师预先批准的输入哈希必须等于实际渲染 PNG 哈希。此核验不创建“已提交”记录；只有后续 HTTPS 请求开始前才能写入提交回执，因而页面渲染/本机错误不会被误记为模型调用。未配置独立 Worker 身份时该私有入口保持关闭。`

实施记录：`2026-08-11 新增 OCR 加密候选件持久化内核与迁移：Qwen 结果文本只能以 UTF-8、最大 512 KiB 的内容寻址 AES-GCM 对象进入本地受管存储，数据库仅保存当前证据页、外部请求、页哈希、结果哈希、服务回执哈希和复核哈希；SYSTEM_WORKER 才能暂存，表触发器禁止删除或覆盖，仅允许主办/复核律师作出一次接受或拒绝决定。该候选件尚未接入实际网络传输或事实台账，不能被当作事实或提交材料。`

实施记录：`2026-08-11 将单页 Qwen OCR 由“安全配置”接成可执行的受控桌面流程：律师必须在当前单页预览上填写已确认的保留/训练政策、成本上限并逐页确认；WebView 只传递案件、页、短时文件夹授权和预授权标识，Rust 原生层才从 Keychain 读取密钥、从私有 sidecar 获取并二次哈希核验该 PNG 页、写入提交回执后请求固定官方 Qwen3.5-OCR 端点。返回文本立即由 sidecar 加密暂存为律师复核候选，律师可查看、一次保留或驳回，均不自动形成事实或法院文书。网络不确定结果记为 UNKNOWN_SUBMISSION 并禁止自动重试；一次调用上限仅限制再次提交，不阻止该次的终态审计回执。真实供应商计费、专用实库迁移、已登记律所环境和完整桌面主路径验收仍是实际使用门。`

实施记录：`2026-08-11 接通 OCR 复核到事实台账的人工编辑桥：仅当律师已经保留当前 OCR 候选时，页面才允许把律师重新整理的一项短事实作为 AGENT_CANDIDATE 写入；命令固定当前原始文件 SHA-256、页码和页标识，创建的仍是 CANDIDATE，须在“事实与争点”中另行确认、争议或否认。系统不把整页识别文字、模型输出或红框自动转化为事实。`

实施记录：`2026-08-11 补齐证据到利息计算前交易台账的人工主路径：律师在已打开并核验的单页原件上可手工录入日期精度、金额、币种、方向、主体、渠道和备注，系统固定原始文件 SHA-256、页码与页标识后只创建 TRANSACTION CANDIDATE；不依赖 OCR，也不把转账自动定性为借款、还款、本金或利息。交易候选须在“事实与争点”再次勾选核验后由主办律师确认，付款性质随后独立建立并批准，正式利息计算只读取该下游已批准分类。前端、持久化 API 回执与测试覆盖均已接通。`

实施记录：`2026-08-11 加固 Agent 执行计划的策略边界：每个计划请求携带的策略哈希现由服务端依据当前 Case Skill Registry 独立计算并严格核对；浏览器不能以伪造哈希使模型获得未登记、已门禁或已变更的能力。文书草稿候选使用同一策略投影，避免两条 Agent 计划路径对策略版本产生漂移。已补充伪造策略拒绝测试，并恢复/覆盖只读执行提案的事务边界。`

实施记录：`2026-08-10 完成律师选择文件夹的版本化只读盘点、增量差异、主办律师范围批准及第一段材料接收链：排队前重验当前 Manifest，逐文件短租约、本机恶意文件扫描谱系和静态 PDF 结构检查通过后才登记真实页数；非 PDF 明确进入转换/人工处理。真实扫描器、常驻 Worker、其他格式隔离转换和专用实库仍未配置，不接真实案卷`

实施记录：`2026-08-10 完成 JPEG/PNG 与 UTF-8/UTF-16 文本的本地安全规范化第一段：格式检查和恶意文件扫描通过后，在临时隔离目录生成 PDF，原件处理前后哈希复验，规范化 PDF 加密保存并与原件、转换版本、转换哈希和页数同事务登记；非 PDF 页预览解密临时表示件且仍回查原件。DOCX/XLSX 读取、隔离转换、文书/表格编辑仍处于 GATED，不对用户声称已经可用`

实施记录：`2026-08-10 完成可执行的案件 Skill Registry/Tool 最小权限策略：材料读取、Word/Excel 的安全只读解析、图片/文本证据 PDF 规范化、官方法源研究与确定性利息计算可被代码授权；Office 转 PDF、Word/Excel/PDF 起草及提交校验均明确为 GATED，未接通隔离转换/文书 Worker 前网关拒绝调用。该架构用于让 Agent 获得类似通用助手的能力，但不获得任意文件、Shell 或无限制联网权限`

实施记录：`2026-08-10 完成基于审批哈希与来源引用的 Word、Excel、PDF 草稿生成内核：文书不覆盖原件，Excel 对公式型文本做注入转义，三类输出均以字节和 SHA-256 返回给受管加密存储层；尚未把审批哈希回查、版式回读和受管存储接入同一正式命令，因此对 Agent 仍保持 GATED`

实施记录：`2026-08-10 完成首段 CaseSkillToolGateway：已启用的图片/文本 PDF 规范化和 Word/Excel 安全读取只能接收文件夹授权层签发的原件句柄，拒绝任意路径、Shell 与通用网络调用，并返回可审计输出哈希；尚未启用的起草工具即使存在生成内核也在网关拒绝`

实施记录：`2026-08-10 复核民间借贷利率研究的官方发现链接：最高法 2020 年第一次修正原页可访问并确认 2020-08-20 生效与第二十六/第三十二条；2020 年第二次修正规则在受限抓取环境中主页超时，已登记最高法国际商事法庭官方镜像作回查。两者仍仅为发现登记，不替代产品内加密捕获、哈希、效力审查和律师批准`

实施记录：`2026-08-10 完成 PDF 文书候选件的正式工作产品接入：Worker 只以经批准输入快照生成 PDF、重新打开核验页数/尺寸、加密保存并登记候选件；新增 review_input_hash，律师批准必须严格等于绑定输入快照、文书类别、受众、PDF 哈希/字节/页数及语义文本哈希的值，旧无绑定候选件拒绝批准。Word/Excel 生成器仍未接入同一持久化/版式回读链，继续 GATED`

实施记录：`2026-08-10 完成 DOCX/XLSX 到 PDF 的隔离转换 Worker，并接入非 PDF 原件规范化登记分支：先阻断宏、ActiveX 和外链，再将原件哈希确认后的副本交给临时 Profile 的 LibreOffice；macOS sandbox-exec 拒绝网络，输出 PDF 重开验证后加密保存，并同事务登记原件、转换器版本、转换哈希、渲染复核哈希和页数。PDF 渲染器也在断网沙箱逐页输出 PNG，逐页像素几何和哈希必须通过。运行时要求显式受检的 LibreOffice/Poppler 路径且必须绑定加密对象库；标准 openpyxl XLSX 的包根关系已兼容，宏/外链继续 fail closed。桌面安装包尚未注入工具，Word/Excel 正式工作产品也未进入复核候选链，Skill 保持 GATED`

实施记录：`2026-08-10 完成系统生成 Word/Excel 草稿的隔离 PDF 复核对：可编辑 DOCX/XLSX 不写回原案卷，生成后用与证据相同的断网转换/像素渲染链产生复核 PDF；复核哈希严格绑定律师批准输入、可编辑源件哈希/字节、预览 PDF 哈希/字节/页数和渲染回执。DOCX 与 XLSX 实际转换均通过。该对尚未作为关联的受管工作产品候选件持久化，因此起草 Skill 仍保持 GATED`

实施记录：`2026-08-10 将 Agent 文书/台账 Tool 统一为复核对输出：注册表不再向 Agent 暴露只返回内存 DOCX/XLSX 的 Tool，改为只允许返回可编辑源件、断网 PDF 预览及共同复核哈希的 Tool；Gateway 同时要求已配置的隔离转换器。正式受管候选件的原子登记与律师批准接口尚未完成，故该 Skill 仍保持 GATED`

实施记录：`2026-08-10 完成 Word/Excel 复核对的正式持久化命令与数据库迁移：加密可编辑源件、审阅 PDF、页数、渲染复核回执、批准输入与共同复核哈希以单一候选件保存；入库前逐一解密认证并重新验证 Office 容器/PDF，律师只能以完全相同的复核哈希确认。数据库触发器拒绝删除和除该确认外的任何改动。此对仍是内部审阅材料，不会直接进入法院提交包；桌面端审批界面、实库迁移、打包转换器和正式 PDF 成品链尚未接通，因此相关 Skill 仍保持 GATED`

实施记录：`2026-08-10 扩展 2020 年第二次修正民间借贷司法解释的确定性条文解析集合：同一份经加密捕获的官方文本现须同时锁定第二十四条（无/不明利息）、第二十五条（合同成立时 LPR 四倍）、第二十六条（预扣利息与实际出借本金）、第二十七条（本息结算）、第二十八条（逾期利率）、第二十九条（逾期利息/违约金等合计上限）及第三十一条（过渡规则）。缺少任一标记即进入失败/人工复核，不能产生部分掩盖缺口的“法律结论”；规则仍需由律师基于捕获版本、事件时间点和案内事实批准后才能进入计算`

实施记录：`2026-08-10 新增民间借贷过渡规则计划器：它基于律师确认的合同成立日、起诉日、一审受理日和历史付款候选生成可审查的时段锚点；对旧合同且 2020-08-20 后新受理的一审案件，明确分离边界日前的历史规则审查与边界日后的起诉时 LPR 四倍候选，拒绝“每年 LPR 自动滚动”的错误简化。边界日受理、日期缺失、起诉/受理时间倒置、历史付款性质/证据缺失均被显式阻断或列为律师待办；该计划不替代法源审批、LPR 快照和正式计算`

实施记录：`2026-08-10 完成仅合成数据的法院 PDF 提交包编译、四项正式依赖锁定、ZIP/内部清单隔离、加密登记和本机一次性下载边界；未接入真实案件、真实身份、法院平台或自动提交`

实施记录：`2026-08-10 完成公开官方法源 credential-free TLS 抓取、加密捕获及民间借贷关键条文/LPR 确定性解析第一段；真实网络冒烟只使用公开数据和临时对象，任何结果仍需律师复核后方可进入正式规则包`

实施记录：`2026-08-10 完成官方法源持久授权队列、一次尝试短租约 Worker、确定性解析回写、稳定失败码和中文律师复核界面；仍未执行专用 PostgreSQL 实库迁移，复核批准也不会自动登记法源或启动计算`

实施记录：`2026-08-10 完成律师复核到正式法源快照的独立登记链，并新增许可依据/哈希门；登记从服务端重验加密原字节，旧来源缺少许可复核时不能支撑新规则，登记仍不会自动创建规则或计算`

实施记录：`2026-08-10 完成 Tauri 2 桌面壳、Next.js 全路由静态导出和专用原生案卷文件夹选择桥；生产 macOS .app 已实际启动并完成首页到证据核验台导航，但尚未装配 OS 身份、Keychain 初始化/轮换、API/Worker 常驻监护、签名公证或真实案卷`

实施记录：`2026-08-10 完成自包含 Python 本机 API sidecar、Tauri 父进程随机挑战核验、动态数字 loopback 端口、进程启动/退出监护和桌面状态展示；macOS .app 已实测服务就绪、健康响应及退出后无残留进程。OS 绑定短时会话后端契约已实现，但因尚无服务端登记律师/律所身份和专用 PostgreSQL，真实案件路由继续关闭`

实施记录：`2026-08-10 完成律所 Ed25519 签发、严格规范化凭证、30 天有效期上限、32 字节安装秘密绑定、macOS Keychain 双项目只读和短时会话装配契约；仅以生成密钥和假 Keychain runner 做测试，未写入用户真实 Keychain。真实签发/撤销服务、生产公钥和专用 PostgreSQL 缺失时继续禁用真实案件`

实施记录：`2026-08-10 完成律所登记、续期、远程撤销和本机停用的受控生命周期编排：激活请求不能携带用户/律所/角色，签名凭证先验证再 CAS 写入，续期禁止切换身份，远程撤销回执与本机停用严格区分；当前仅有接口与合成适配器测试，未配置真实签发服务或写入用户 Keychain`

实施记录：`2026-08-10 完成人民法院案例库、最高人民法院官网/国际商事法庭和中国裁判文书网的第一轮官方数据接入目录，并登记 6 个民间借贷真实案例候选元数据；版权/许可未证明前代码禁止批量抓取、全文仓库、商业训练和正式案件使用，真实案例只用于设计合成回归题与律师研究线索`

实施记录：`2026-08-10 完成 Tauri 原生 macOS Keychain 安装秘密初始化、只读状态和“仅停用本机登记”能力；秘密通过原生凭证库 API 传递，不进入命令参数或 WebView，初始化需显式动作并回读复核，已有/损坏秘密不覆盖。本轮实际 .app 只读验收显示“尚未初始化”，未点击写入；律所激活仍因真实签发服务和生产公钥缺失而禁用`

实施记录：`2026-08-10 将同一份 knowledge/official_cases/registry.json 接入中文法律规则页，展示 6 个官方真实案例候选的争点、评测用途、官方链接与“仅研究线索”状态；前端不复制第二份目录，Turbopack 根提升至仓库根以保持后端校验与桌面 UI 同源。页面明确阻断自动法源化、自动结论和未经许可的全文使用；1280×900 实际浏览器排版、生产构建与控制台检查通过`

实施记录：`2026-08-10 补齐 macOS 桌面构建产物的本地临时签名终检：固定校验产品名、Bundle ID、主程序和 sidecar 均为包内普通文件，按由内到外顺序 ad-hoc 签名后执行 codesign --deep --strict；不完整包会使 desktop:build 失败。重新打包、严格校验、实际启动和退出联动通过；该临时签名不冒充 Apple Developer ID、公证或可公开分发状态`

实施记录：`2026-08-10 完成律所登记生产信任目录与原生导入链：安装包只固定离线根公钥，目录需 Ed25519 门限签名并通过到期、连续版本、前序哈希、TLS SPKI pin 和活动/退休/撤销签发公钥核验；仓库默认 NOT_CONFIGURED 且不含私钥或测试根。Tauri 只从系统文件选择器读取 .lawenroll，以不进入 WebView 的父进程私有令牌请求 sidecar 验签，精确字节和当前凭证哈希匹配后才 CAS 写入 Keychain；生产信任根、真实签发服务和专用 PostgreSQL 未配置时仍禁用登录与案件路由`

实施记录：`2026-08-10 完成已保存登记的启动重验与短时会话链：只有生产信任目录 READY 才读取 Keychain，缺少登记为 NOT_ENROLLED，签名/时效/设备绑定等异常为 BLOCKED；有效登记按当前目录重新验签后，由 Rust 父进程私有令牌一次性交换最长 30 分钟会话。令牌和 session ID 不进入普通运行状态，专用数据库未 CONFIGURED 时 WebView grant 命令拒绝，sidecar 仍不挂载案件路由；真实签发身份、数据库实迁移和逐案权限尚未装配`

实施记录：`2026-08-10 完成可审阅 Word/Excel 草稿对的本机持久 API 访问边界：主办/复核律师可读取不含对象键的候选件元数据，先获取一次性审阅 PDF 或可编辑文件访问许可，再仅从 loopback 下载；许可绑定 OS 会话、案件、人员、草稿对和内容哈希，短时且单次使用，中文文件名采用标准 UTF-8 响应头编码。律师确认仍严格绑定共同复核哈希。桌面审批界面、实库迁移和正式法院 PDF 成品链尚未接通，相关 Agent Skill 继续 GATED`

实施记录：`2026-08-10 在中文“提交材料”工作台接入内部文书审阅区：律师可读取非敏感候选件元数据、打开 PDF 审阅稿、下载可编辑 Word/Excel 内部草稿，并在明确提示后提交与共同复核哈希绑定的确认；前端会核对内容类型、长度与服务端哈希，且不展示对象键。此区不将内部草稿冒充法院提交件；正式文书生成入口、实际 PostgreSQL 装配和生产身份会话仍待完成`

实施记录：`2026-08-10 完成 Agent 能力清单的策略同源导出：中文桌面“身份与安全”页展示每项已启用或安全门禁中的 Skill、范围与审批门；其 JSON 仅由后端 CaseSkillRegistry 生成，前端构建前与后端测试均校验不漂移，且不含案卷、路径、对象键、凭证或模型配置。清单用于解释权限边界，不授予任何工具执行权限`

实施记录：`2026-08-10 完成文书一致性与来源缺口审查的确定性第一段：Agent Gateway 只接受批准文书快照和律师确认的字段表，生成不可变审查报告，检查确认值缺失、冲突文本和实质段落缺少来源；适用于案号、法院、姓名/身份、金额、日期、币种、代理权限和证据编号等字段。审查不修改文书、不选择立场`

实施记录：`2026-08-10 将文书一致性审查接入持久化与提交 QA 阻断：SYSTEM_WORKER 只能登记由确定性报告派生的安全投影，保存已批准 PDF 工作产物及共同 review_input_hash、输入/输出/字段/来源哈希、阻断/警示代码，不保存正文、确认值或提示词；每条记录追加不可变并受律所 RLS。提交 QA 必须引用当前版本、无阻断、且覆盖完全相同 PDF 工作产物集合的审查，任一材料替换、版本改变、冲突或输出哈希不一致均 fail closed。逐段编辑差异和真实专用 PostgreSQL 端到端验收仍待完成`

实施记录：`2026-08-10 将一致性审查快照接入中文“提交材料”界面：前端并列读取提交、草稿与审查快照，展示当前通过/阻断状态、案件版本、阻断/警示数量、输出哈希和安全发现代码；不展示正文、确认值、对象键或提示词。审查快照不可用时单独说明原因，不能以旧页面或合成结果掩盖 QA 门`

实施记录：`2026-08-10 补齐文书一致性审查的受控执行入口：专用 SYSTEM_WORKER 只接收内部协调器给出的已批准结构化文书快照、律师确认字段表及已批准 PDF 工作产物绑定，先运行确定性审查，再派生并登记无正文/确认值的安全投影；入口不接收 HTTP、浏览器、文件路径、模型或外部网络输入，非 Worker 在审查前即拒绝`

实施记录：`2026-08-10 完成批准结构化文书的确定性版本差异内核：标题、段落/标题、来源引用、章节新增删除或顺序变化均只输出位置与 SHA-256 差异，不保存正文；任何语义差异均标为必须重新审批，批准元数据自身变化不被误判为内容变化。下一步将此结果持久绑定到工作产物版本链，旧审批不得随文书改动沿用`

实施记录：`2026-08-10 将文书版本链接入法院提交工作产物：有语义文本哈希的新候选文书获律师批准后，同案件、同文书类型、同用途且语义哈希不同的旧已批准版本立即标为 STALE，并记录固定中文原因；提交包选择只接受 APPROVED，因此旧版本无法再入包。内容哈希相同的重新生成件不误伤历史版本，未具语义哈希的外来 PDF 仍须走单独审查链`

实施记录：`2026-08-10 修复正式案件法律页在尚无来源快照时无法发起首个官方抓取的流程断点：持久化审查快照保留已登记来源，并合并经核验的只读官方来源发现目录；缺失项清晰标为 NOT_CAPTURED/仅发现，唯一作用是让律师在公开网络确认后建立受控抓取任务，不能进入规则包、不能显示利率或替代已登记法源`

实施记录：`2026-08-10 为经验证的官方法源备用页补齐显式律师选择：第二次修正民间借贷解释和民法典在主页面不可达时可改选已登记、同一确定性解析器支持的官方页；选择的精确 URL 仍进入一次授权、内容哈希与回执链。没有可解析备用页的不提供伪选项，失败保持可见且不自动改源`

实施记录：`2026-08-10 移除持久化案件首页残留的合成案情回退：正式模式在没有总览快照时仅展示“先确认案卷范围并接收材料”的真实空状态与证据入口，不再渲染合成金额、期限、对方信息或模拟审计记录`

实施记录：`2026-08-10 将持久化首页接入事实/交易工作区同源的案件审查快照：仅展示已核验案件标题、事实与交易台账数量、案件版本、快照和请求标识；金额、利率、期限及法律结论保持在对应受控工作区，不由首页推定。快照不可用继续明确阻断，不回退合成数据`

实施记录：`2026-08-10 完成 Agent 计划与工具执行的追加审计账本第一段：案件级 Agent Run 只可计划已启用注册 Skill/Tool，固定策略清单哈希、输入快照哈希、案件版本、审批门与所需范围；执行回执只由系统 Worker 写入成功输出哈希或稳定错误码。三张表均按律所 RLS 隔离且拒绝更新/删除，计划不授予执行权限、也不保存提示词或案卷正文；真实专用 PostgreSQL 迁移、模型调用 preflight 与 Agent Run 界面待接入`

实施记录：`2026-08-10 将 Agent 追加审计账本接入持久 API：律师读取本案 Agent Run/提案/回执元数据，创建计划固定服务端身份、案件版本、幂等键和哈希；系统 Worker 另行登记工具结果。API 未注入实库服务时返回明确 503 而非进程内回退。模型调用 preflight、真实系统 Worker、Agent Run 桌面界面和专用 PostgreSQL 迁移仍待接入`

实施记录：`2026-08-10 补齐 Agent 到可审阅 Word/Excel 草稿对的受控执行协调器：仅 SYSTEM_WORKER 可读取尚未成功的最小提案投影，批准 Word/Excel 输入先重新哈希，必须与该提案一致；Worker 只能调用提案所列的起草 Tool，隔离渲染/加密复核对登记成功后才追加工具成功回执。运行时仅在隔离转换器、加密对象库和持久复核对同时装配时动态启用 Office 起草 Skill；默认安装及浏览器不能切换此能力。该协调器不接收 HTTP、路径、提示词或模型输出，真实 Worker 调度和已登记桌面环境装配仍待完成。`

实施记录：`2026-08-10 新增 Agent Office 草稿桌面装配器并纳入 sidecar 启动门：默认不启动，显式启用时必须同时重验律所登记、从登记派生 SYSTEM_WORKER 身份、持久 Agent/草稿存储、AES-GCM 对象库、隔离 LibreOffice/PDF 渲染器和与加密对象库分离的 0700 私有工作目录。sidecar 会在开放案件路由前装配并核验该内部执行器，在就绪诊断中只报告 ASSEMBLED/NOT_CONFIGURED；执行器只向同进程未来 Agent supervisor 暴露 Word/Excel 入口，不开放 HTTP、WebView、路径或通用任务队列；缺少任一前置条件即拒绝启动。`

实施记录：`2026-08-10 补齐 Agent 对 PDF 原件的受控读取：新增逐页静态文本与页级哈希 Tool，只接受律师选择文件夹签发的短时原件句柄；每次读取前后重验原件哈希，拒绝加密、非 PDF、活动内容、过大文件/页数/文本量，不写回原件也不自动外发文本。PDF、Word、Excel 的读取能力由同一 Skill Registry/前端权限清单导出；文书编辑仍只走“可编辑草稿 + 断网 PDF 审阅 + 律师确认”的受控链。`

实施记录：`2026-08-10 新增模型结构化文书候选解码器：Agent 的外部或本地模型响应只可用严格 JSON Schema 表示 Word 段落/来源引用或 Excel 台账行；未知字段、Markdown 包装、公式对象、超限文本及不可追溯来源引用一律拒绝。解码器为候选内容重建稳定内容哈希与后续执行输入哈希，不接受模型给出的“已批准”标记，也不创建文书、计划或执行回执；下一步将候选件加密暂存并接入律师确认与内部调度。`

实施记录：`2026-08-10 建立结构化 Agent 文书候选的律师确认内核：Word/Excel 候选均绑定文件类型、精确执行输入哈希、内容哈希、指定 Skill/Tool 和共同复核哈希；只有主办律师或复核律师对同一复核哈希的确认才能形成批准对象，Assistant/模型身份和修改后的哈希均被拒绝。该内核尚未持久化候选正文或创建 Agent 账本提案，下一步将其接入加密对象库、追加式账本及桌面 Worker。`

实施记录：`2026-08-10 收紧 Agent 追加式账本的文书授权：即使受信任桌面运行时已动态启用 Office Tool，Assistant/模型身份仍不能经通用计划 API 登记 Word/Excel 起草提案；此类提案在任何数据库写入前必须由主办律师或复核律师发起。分析类 Tool 的既有受控计划能力不受影响。`

实施记录：`2026-08-10 为 Agent 文书候选的加密暂存补齐规范化载荷：Word/Excel 候选可从已验证结构重建唯一 UTF-8 JSON 字节流，载荷不含律师确认、执行回执或路径；重新解析后必须得到同一执行输入哈希。后续受管对象库将只保存该规范化载荷，数据库仅保存内容寻址对象引用、候选/审批状态和哈希。`

实施记录：`2026-08-10 新增 Agent 文书候选持久化迁移：候选正文仍在 AES-GCM 受管对象库，SQL 只保存内容寻址对象键、内容/执行/复核/理由哈希、受限 Word/Excel Skill/Tool、一次律师决定以及审批后对应的 Agent Run/Proposal。RLS、不可删除触发器和状态约束仅允许 CANDIDATE 到精确 APPROVED 或 REJECTED；该迁移尚待 Store/Worker 接入及专用 PostgreSQL 实库执行。`

实施记录：`2026-08-10 将 Agent 文书候选 Store 接入 RuntimeServices 与 desktop_persistent_runtime：持久化桌面装配只有在同一 Keychain AES-GCM 对象认证器可用时才注入该 Store，默认合成运行时保持空值；候选 Store 读取对象前再次验证密文解密后的字节数、SHA-256 与固定 JSON Schema。专用实库迁移、API/审阅 UI 与 Worker 领取仍待接入。`

实施记录：`2026-08-10 在中文“身份与安全”页接入只读 Agent 执行审计区：可见当前 Agent、输入案件版本、策略/输入哈希、每项 Skill/Tool、审批门、范围及追加式回执状态；不显示提示词、案卷正文、对象键、令牌或供应商凭证，也不提供绕过系统 Worker 的执行按钮。生产身份/实库未配置时明确显示阻断状态`

实施记录：`2026-08-10 完成外部模型/OCR/MCP 调用的追加式预授权账本与持久 API：主办/复核律师必须固定目的、字段标识、供应商、地域、保留/训练政策、服务、次数/成本上限、输入/授权哈希及到期时间；系统 Worker 才能写入提交/结果回执。过期、超过次数、终态重试和 UNKNOWN_SUBMISSION 后自动重试均被阻断，且不保存密钥、提示词或案卷正文。真实供应商适配器、成本计量和桌面预授权 UI 待接入`

实施记录：`2026-08-10 完成桌面模型 API Key 的第一段真实配置通道：中文“身份与安全 → 模型与外部服务”只显示 DeepSeek/通义千问百炼 Qwen3.5-OCR 的已配置状态；首版固定模型为 deepseek-v4-pro 和 qwen3.5-ocr。点击配置后 Key 仅进入 macOS 原生安全输入框，由 Rust 直接写入并回读 macOS Keychain，WebView、命令参数、页面状态、案件数据库和审计账本均不接触明文。移除须经原生确认；供应商仍是固定白名单，不能自填 Base URL。该通道不等于允许案卷外发，尚未接通真实供应商适配器、用量计量或正式预授权 UI。`

实施记录：`2026-08-10 修复桌面受控服务的实际启动兼容性：Sidecar 就绪回执已新增受控 Agent 草稿执行器状态，Rust 父进程保留 deny-unknown-fields 的同时只显式接受 NOT_CONFIGURED/ASSEMBLED 两个固定值；此前因此被当作“格式无效”的新字段已不再误阻断。新 macOS 包已实际重启并显示“本机服务已就绪（案件仍禁用）”。`

实施记录：`2026-08-10 收紧 LPR 正式规则的数值来源：新增不可变、租户隔离的官方 LPR 观察记录迁移；中国货币网快照只能从已复核抓取的加密原件由服务端重新解析后登记，解析哈希不一致即拒绝。LPR 倍数规则审批只接受法律依据快照、官方快照、精确定位和倍数，基准利率由数据库读取该观察记录，浏览器、模型和草稿均不能传入数值。专用 PostgreSQL 实迁移和执业律师对具体规则适用的验收仍待完成`

实施记录：`2026-08-10 在中文法律规则台加入受控 LPR 倍数规则审批入口：表单只能选择已核验且许可有效的法律/司法解释快照、已登记中国货币网快照和已批准事实键，要求明确触发事件、期间、精确记录定位、倍数与律师确认；浏览器不出现也不传输基准利率。提交回执后重新读取版本化法律快照，任一上游不一致仍由服务端拒绝。法律事件、事实绑定和连续规则包的写审批入口继续实现中`

实施记录：`2026-08-10 打通规则审批的事实前置链：中文事实台对持久化案件候选事实可写入版本/审计哈希绑定的正式确认；法律页再只允许从同案 `CONFIRMED` 事实建立规则事实键绑定。两个入口均会刷新当前快照，不能把候选文字、其他案件事实或旧版本悄然用于规则。法律事件与连续规则包写审批入口继续实现中`

实施记录：`2026-08-10 在中文法律规则台接入案件法律事件审批：律师选择事件类型、精确日期和已纳入本案的证据页，并作明确确认后才写入版本/审计哈希绑定的事件；系统不从文件时间或模型猜测合同、付款、起诉、受理或裁判日期。页面只展示首批已核验页，更多页面仍须按证据页工作台的版本化分页处理；连续规则包写审批入口继续实现中`

实施记录：`2026-08-10 在中文法律规则台接入连续案件规则包审批：可添加多个首尾相接的期间，每段固定选择已批准规则版本、触发法律事件、起止日期与适用锚点；前端拒绝缺口/重叠，服务端再次复核规则效力期间、事件类型与日期、事实锚点、双来源许可、参数来源和案件版本。新规则包获批会使旧规则包与依赖计算失效，不会覆盖历史记录。正式计算情景写入入口继续实现中`

实施记录：`2026-08-10 在中文利息计算台接入正式计算情景入口：只可引用当前已批准规则包，律师确认债务单元、计算区间及已批准冲抵顺序；利率、交易金额、付款性质、币种与计算输出不在浏览器编辑。服务端再核对同案已确认且已分类交易、重复组、同日顺序、连续规则覆盖、CNY、规则包哈希并进行独立复算；失败不产生计算结果。随后在交易台接入付款分类候选与单独批准：计算相关分类必须将交易全额分配至债务单元，默认逐项继承已确认交易的原始证据定位，同日顺序由律师明确填写；候选不会进入正式计算，批准后才生效，分类页不暴露付款人、收款人、备注或原始证据正文。`

实施记录：`2026-08-10 完成 desktop_persistent_runtime 受控装配：sidecar 只有在当前重新验签的登记身份、显式 _preview/_test PostgreSQL、Keychain 只读 AES-256-GCM 密钥和预创建的 0700 加密对象目录同时具备时，才一次注入全部持久 Store、一次性文件访问 Broker、案卷文件夹授权及 Agent/外部调用账本；缺任一项在案件路由监听前拒绝启动。它不接收 HTTP/环境密钥、不由浏览器选择对象目录，也不把身份/角色复制到客户端。真实 Keychain 工件密钥初始化/轮换、专用实库迁移、Worker 打包及真实律师环境整链验收仍待完成`

实施记录：`2026-08-10 发现并修正外部调用成本上限缺少币种的审计缺口：新增追加式迁移，历史不可改写记录标为 ISO 4217 的 XXX（未计价）而非猜测币种；所有新增模型/OCR/MCP 预授权必须带明确 ISO 4217 币种，API/持久层及中文审计界面均同时展示币种与最小金额单位`

实施记录：`2026-08-10 纠正官方法源 Skill 的执行表述并补齐网关适配器：Agent 现可在律师批准后基于已注册的官方来源与敏感信息过滤，生成可审阅的最小化研究候选规划和稳定输出哈希；该步骤不联网、不读取案卷正文，也不发起抓取。实际 HTTPS 抓取仍必须走独立的官方来源授权队列、一次租约、加密归档、确定性解析和律师复核链`

实施记录：`2026-08-10 完成官方来源抓取 SYSTEM_WORKER 的单任务执行入口：只能以独立 SYSTEM_WORKER 身份、精确案件/运行标识、案件版本及受限 ASCII 幂等键领取已授权租约；入口不接收网页 URL、检索词、案卷正文或密钥，取得租约后才调用现有固定查询、credential-free HTTPS、加密归档和确定性解析协调器。桌面监督进程对该入口的系统 Worker 身份配置与自动领取策略仍待接入`

实施记录：`2026-08-10 完成桌面 SYSTEM_WORKER 身份装配：Worker 用户 UUID 仅来自显式本机运行配置，律所范围必从当前重验成功的桌面登记 Actor 派生，忽略任何环境/网页指定的 firm_id；未登记、缺 Worker ID 或 UUID 异常时 fail closed。数据库领取任务时仍须逐案复核 SYSTEM_WORKER 角色，桌面登记不成为 Worker 授权旁路`

实施记录：`2026-08-10 将官方法源抓取的有界监督循环接入 desktop sidecar：默认关闭，只有完整持久化门、重新验签的登记、独立 SYSTEM_WORKER、与加密对象目录完全分离的预创建 0700 工作目录及显式 YES 同时具备才启动；每轮只从同律所未过期且未尝试的授权队列领取一项，仍由数据库租约和逐案角色复核。循环不读取律师案件原件目录，sidecar 退出时停止。真实 Keychain、专用实库和正式案件授权端到端验收仍待完成`

实施记录：`2026-08-10 完成 WebView 持久 API 会话客户端：所有正式案件读写通过原生 grant 解析动态数字 loopback 并注入短时 Bearer，固定地址若存在必须完全匹配；派生 PDF/ZIP/PNG 继续使用独立一次性许可。Rust 在 grant 返回前清除过期会话，服务端 CORS 只允许 tauri://localhost 所需头；默认生产信任和数据库未配置时仍不返回令牌或开放案件路由`

实施记录：`2026-08-10 完成律所登记续期与远程撤销原生链：服务 Origin/TLS SPKI 只来自门限签名目录，固定 HTTPS 路径无自动重试；sidecar 读取并重新验签当前 Keychain 材料后在内存 staging，Rust 按旧凭证哈希 CAS 保存续期或在已接受撤销回执后停止会话并删除。中文界面区分远程撤销与只停用本机；生产目录未配置时端点 404、按钮禁用且不联网`

实施记录：`2026-08-10 完成一次性激活码原生安全链：WebView 只触发无参数命令，macOS NSSecureTextField 收集并清除激活码；sidecar 经父进程私有 Bearer 读取 Keychain 安装绑定并拒绝覆盖已有登记，再通过签名目录固定的 HTTPS/SPKI 服务换取、验签并内存 staging envelope，Rust 以当前无登记为 CAS 前置条件保存。生产目录未配置时不显示输入框、不挂载端点且不联网`

实施记录：`2026-08-10 完成激活、续期和撤销断线未知结果消解：原生层在远程请求前把 UUID 操作号、类型、原凭据哈希和设备绑定写入第三个 Keychain 项；未知结果禁止重试并可跨重启恢复，固定 HTTPS 状态查询仅接受 PENDING/REJECTED/SUCCEEDED，成功结果重新验签并与待决标记、原凭据 CAS 一并提交。默认 NOT_CONFIGURED 仍不挂载端点或联网`

当前阶段：`M2 的一次真实 qwen3.7-plus 调用、封存响应的严格重解释、受控来源派生差额降级、注册法条投影及 DOCX/PDF 候选回放已通过。历史 M2 Agent run 保持 WAITING_INPUT、外发数保持 1，未被改写为成功；因此已证明真实模型到律师复核候选的受控交付链，不等同于新的持久化自动任务通过、律师验收或商用放行。`

实施记录：`2026-09-05：M2 原始官方法源可见正文为 111,098 字符，超过文书单源 40,000 字符门。按 ADR-0078，仅对已注册的 CN-CIVIL-CODE-680 且精确登记第六百七十九条、第六百八十条定位器重新验证标题、通过日期、条款顺序和字面标记，再生成 211 字符的可复算法条投影；原始私有对象、来源哈希、法源包和案件记录均未改变。`

复核证据：`M2 封存响应 11,506 bytes 经当前严格解释形成 32,570-byte 律师决策包候选，4 处可由来源唯一证明的差额字面量均被删除而非接受为正式数值。一次只读文书回放生成 3 页 A4 DOCX/PDF：DOCX SHA-256 为 39d4562c43e1b5829e9460bd0bec56024bec251a6db260db511079944d7a3a79，PDF SHA-256 为 73b5cbaca55c00c28d07f7a983c8b9441edc9ad6f7aaf8cad418293fefc0a1b3；回放明确记录 0 新 Agent run、0 模型调用、0 案件数据库/对象存储写入、1 次隔离渲染请求和历史运行未变化。111 项相关回归测试、实际 PDF 结构及三页视觉检查均通过。`

单一下一目标：`在不创建新的模型运行的前提下完成受管 Worker 的恢复/权限/持久化交付审计；随后才评估是否有必要以一条新、预预算、不可重试的正常模式合成运行证明状态机的实时成功路径。`

首个案由：`民间借贷案件被告一审应诉`

计划周期：`29—34 周达到商用 Beta 安全门槛，商业验证持续滚动`

本文件是项目的唯一总计划。阶段内的任务可以细化，但任何范围、法律责任边界、数据安全边界或用户审批点变化，都必须先更新本计划或新增 ADR。

## 1. 第一性原理

这个产品处理的是高风险专业工作。一次事实漏项、利率规则适用错误、金额计算错误或版本混乱，都可能影响当事人的实体权利。因此系统设计优先级为：

1. 正确和可追溯；
2. 律师可控制；
3. 一致和可复算；
4. 保密和可审计；
5. 节省时间；
6. 视觉精致和自动化程度。

AI 不应替代律师，而应降低律师在材料整理、事实核对、规则检索、数字复算、文书一致性和版本管理上的机械负担。

## 2. 项目目标

### 2.1 业务目标

- 将一件标准民间借贷应诉案件的材料整理和初稿准备时间降低 50% 以上；
- 封闭试点后，将律师实际复核时间逐步降低至 2 小时以内；
- 形成可按案收费、可被律所复购的标准交付；
- 通过 10—30 个真实案件验证流程，再扩展其他金钱给付案由。

### 2.2 产品目标

- 让律师从案件首页直接看到下一项需要决定的事项；
- 让每个事实、金额、法条和文书段落均可回溯；
- 让跨时点法律适用和复杂利息计算能够解释并复算；
- 让材料变化自动影响下游结果，避免“旧结果仍显示已完成”；
- 让提交目录始终只有一个律师锁定的法院提交版。

### 2.3 非目标

- 不追求全案由覆盖；
- 不追求 AI 全自动代理诉讼；
- 不追求第一版接入所有外部系统；
- 不用模型能力掩盖数据结构、流程和审核机制的缺失；
- 不以生成速度换取法律依据和数字准确性。

## 3. 产品级架构决策

### 3.1 总控

- 代码状态机是最高控制层；
- 主 Agent 是用户可见的智能案件经理；
- 专业 Agent 以有边界的工具形式被调用；
- 金额、日期、哈希、PDF 和版本检查由确定性 Worker 完成；
- 律师审批决定案件能否进入下一关键状态。

### 3.2 部署形态

第一版采用模块化单体：一个 Web 前端、一个 API 应用、若干后台 Worker、一套 PostgreSQL 数据库和一个对象存储。暂不拆分微服务。

#### 3.2.1 本地案卷与公开研究双库

最终交付形态是律师电脑上的本机优先工作台：律师为每个案件选择一个本地文件夹，应用只读扫描原件并在受管目录保存加密索引、衍生件和审计。这个文件夹是“本案私有案卷库”，不是互联网法律知识库。

法规、司法解释、LPR 和公开裁判研究必须通过独立的受控研究网关进入“法律与公开研究库”，保存来源、制定机关、公开/生效时间、抓取快照、哈希、许可和引用定位。外部请求只发送经最小化/脱敏的法律问题，不得自动上传原始案卷、当事人身份或聊天/流水内容。公开裁判只作为研究参考，不自动成为法源或个案结论。完整约束见 `docs/LOCAL_CASE_FOLDER_AND_RESEARCH_ARCHITECTURE.md`。

人民法院案例库第一版只保存元数据，并允许律师从官方页面主动下载单篇后导入；最高人民法院官网单篇内容必须逐次授权、加密和复核；中国裁判文书网在接口与许可未明确前只用于发现和回查。已核验来源及第一批真实案例候选见 `research/OFFICIAL_CASE_DATA_CATALOG.md`，机器可校验元数据见 `knowledge/official_cases/registry.json`。

法律规则和利息计算不允许只依赖“当前值”或模型记忆；必须根据合同成立、借贷、付款、违约、起诉/受理和裁判等时间锚点，引用可复现的有效文本和 LPR 原始记录。上游来源版本变化必须失效依赖的规则卡、计算、文书和提交包。

#### 3.2.2 开发工作区与备份基线

当前开发基线（2026-08-09）如下：

- 项目唯一权威工作目录为本机非同步位置 `/Users/cft/Projects/律所案件AI工作台`。`/Users/cft/Documents/律所案件AI工作台` 是 iCloud/File Provider 迁移后保留的只读保全副本，不得再作为开发工作区；路径位于 `Documents`、出现同步图标或存在“iCloud 云盘归档”均不能证明项目已经可靠上传或可恢复。
- 本地工作目录只是工作副本，不是备份；Finder/iCloud、OneDrive 等同步服务也不能作为唯一备份或版本权威，因为同步会传播删除、冲突和残缺状态。
- 代码、计划和脱敏文档以私有远程 Git 仓库的受保护主分支为版本权威；真实客户材料、身份信息、法院文件、诉讼策略、密钥和模型凭证禁止进入 Git。若提供商不能对私有仓库启用保护规则，则该远端只能作为恢复副本，不能视为正式合并控制。
- 非 Git 资产和允许保存的案件测试材料必须进入独立、加密、有版本、可审计的备份目标，并至少保留一个与本机故障域分离的副本；生产案件材料另按律所授权、地域、保留与删除规则存储，不能混入开发备份。
- “已备份”的判定必须同时具备：远程提交可见、备份清单与哈希可核对、指定恢复点可在隔离目录恢复、恢复后的关键文件可打开。仅显示“同步完成”不算通过。
- Phase 0 开始前完成首次开发资产恢复演练；此后每个阶段出口核对远程提交与备份状态，每季度及重大迁移后执行一次抽样恢复。发现本地与远程/备份不一致时停止继续改写，先保全现场并选择经哈希验证的恢复源。

### 3.3 MCP

首版内部调用使用强类型 API/Tool Gateway，不把 MCP 设为核心依赖。所有工具 Schema 保持可 MCP 化。第二阶段优先增加只读法律依据 MCP 和案件资源 MCP。

### 3.4 UI设计门

在业务页面前端初始化前，必须：

1. 生成三种明显不同的高保真视觉方向；
2. 用同一核心页面和同一真实数据密度进行比较；
3. 由产品负责人选择一个方向；产品负责人明确委托时，可由项目负责人基于设计简报作出可追溯的暂定选择；
4. 完成关键流程可点击原型。

三种方向完成并已有可追溯的暂定选择后，可以开始业务页面 UI 编码。Phase 0/1 期间允许以合成数据并行开展完整产品实现，包括技术契约、安全研究、桌面 Web、数据库迁移、状态机和 Worker；不得接入真实客户材料、真实外部服务或对外部署为可办案服务。至少 5 名目标律师的任务测试仍是 Phase 1 正式结束、试点启用和任何真实案件数据接入的出口门，而非合成数据开发的停工门。

#### 3.4.1 开发与真实流程验证的并行顺序

- 产品团队先以版本化的合成案件实现 Phase 2—7 的完整受控流程；
- 真实律师访谈、流程观察、金标准案件和可用性测试由产品负责人组织，持续为产品提供修正依据；
- 未完成 Phase 0/1 真实验证时，系统只能标记为“内部合成 Alpha”，不得声称已被律师接受、不得接收真实案件、不得生成可提交给法院的材料；
- 真实验证发现问题后，以问题影响范围决定返工，不倒置为“未验证就不开发”。

## 4. 项目团队建议

最小产品级团队：

| 角色 | 建议投入 | 主要责任 |
|---|---:|---|
| 产品负责人/创始人 | 全程 | 范围、客户访谈、优先级、验收 |
| 执业律师产品专家 | 每周 1—2 天 | 法律规则、审批点、金标准案件 |
| 产品设计师 | 前 8 周重点投入 | 研究、流程、视觉、原型、可用性测试 |
| 法律知识编辑/执业律师 | Phase 0起兼职 | 法源授权、版本效力、规则卡和法律回归审批 |
| 全栈工程师 | 全程 | Web、API、权限、案件工作台 |
| AI/后端工程师 | 全程 | 文档处理、Agent、知识库、计算引擎 |
| QA/安全工程师 | 第 4 周起兼职 | 自动化测试、安全、发布门禁 |

若由一人加 AI 开发，应保持相同阶段和验收门，只降低并行度，不降低质量标准。

## 5. 开发阶段总览

| 阶段 | 周期 | 关键结果 |
|---|---:|---|
| Phase 0 产品与证据基线 | 4—6 周 | 锁定范围、金标准案件、风险、契约和测试协议 |
| Phase 1 UX与视觉方向 | 2 周 | 选定视觉方向、完成关键流程原型 |
| Phase 2 技术基础 | 2 周 | 租户、权限、审计、存储、状态机可运行 |
| Phase 3 材料与证据工作台 | 3 周 | 上传、OCR、去重、相关页、溯源完成 |
| Phase 4 案情、诉请与交易台账 | 2—3 周 | 事实、诉请、争点、期限和台账完成 |
| Phase 5 法律知识库与研究 | 3 周 | 时态法源、规则卡、引用校验完成 |
| Phase 6 利息与冲抵计算 | 2 周 | 确定性计算、逐笔递减和复算完成 |
| Phase 7 文书与一致性审查 | 3 周 | 文书编译、来源映射、对抗检查完成 |
| Phase 8 提交包与封闭试点 | 6—8 周 | 唯一提交版、安全放行、真实案件试点和商业验证 |

各阶段工作量合计 27—32 周，另预留 2 周用于律师招募、合规/安全复核和阶段返工，因此总周期为 29—34 周。技术研究和设计活动只能按 §3.4 的边界并行，不得跳过阶段出口条件。

## 6. Phase 0：产品与证据基线

### 目标

把真实案件经验转化为可开发、可测试的数据和规则，不让工程团队依赖口头理解。

### 当前执行记录（2026-08-09）

- 已将唯一权威工作区迁移到非同步目录，并保留 `Documents` 原目录作为只读保全副本；迁移前后 14 个项目文件及 `PLAN.md` 的 SHA-256 一致。
- 已建立私有远程 Git，基线提交 `cdc92e5` 已推送至 `main` 分支；从远端执行独立浅克隆后，文件数、计划哈希和 Git 完整性检查均通过。
- 当前 GitHub 私有仓库套餐拒绝为该私有仓库启用分支保护；公开仓库不符合保密边界。因此在升级为支持私有仓保护的方案，或迁移至可自建保护规则的 Git 服务前，远端仅作恢复副本，生产代码不得直接合并到 `main`。
- 恢复证据、适用范围和未完成项记录于 `docs/DEVELOPMENT_RECOVERY_BASELINE.md`。独立加密的非 Git 资产备份尚未配置；在其完成前，不得把真实案件材料或不可由 Git 重建的资产放入本项目。
- 已准备可复现的田野研究、金标准、计算与法律规则、权限与外部调用、Phase 1 可用性测试契约，分别记录于 `research/PHASE_0_FIELD_PROTOCOL.md`、`docs/CALCULATION_AND_LEGAL_RULES_CONTRACT.md`、`docs/SECURITY_CONTROL_MATRICES.md` 和 `design/PHASE_1_USABILITY_PROTOCOL.md`；它们均待执业律师/安全负责人或真实研究证据确认。
- 已完成第一轮公开二级研究，结论、来源等级、竞品压力和待证伪假设记录于 `research/ONLINE_EVIDENCE_REVIEW.md`。公开材料不计入律师访谈、流程观察、金标准案件或付费试点证据。
- 经产品负责人明确调整，真实流程验证不再阻断合成数据开发；产品构建可按 Phase 1—7 的依赖关系并行推进，但真实资料接入、试点和生产放行仍受本计划既定的律师/安全门约束。该边界变更见 `decisions/ADR-0002-development-before-field-validation.md`。

### 工作

1. 访谈至少 10 名办理民间借贷或欠款纠纷的律师。
2. 观察至少 3 个历史案件从收件到提交的实际过程。
3. 选择 3—5 个已经办结、已脱敏案件作为金标准样本。
4. 建立首案由材料清单：诉状、证据附件、借条、送达材料、流水、聊天、委托材料。
5. 建立关键法律问题清单：合同成立、利息约定、已付利息、利率上限、过渡规则、本息冲抵、诉请范围、举证责任。
6. 建立标准输出清单：材料盘点、时间线、诉请表、事实表、交易台账、规则卡、计算表、答辩状、证据目录、核算说明、提交清单。
7. 为每个样本人工制作正确的页级相关性、重复组、关键字段、交易总额、法律规则和计算结果。
8. 确定隐私脱敏规则和真实材料使用协议。
9. 确定产品成功指标、停止指标和试点价格假设。
10. 冻结安全租户模型：`Firm` 是数据安全租户，`Workspace/Team` 仅为组织分组；建立覆盖数据库、对象键和签名 URL、索引、队列、缓存、审计、导出与备份的隔离矩阵。
11. 建立角色—动作—对象审批矩阵，明确承办、协办、助理、复核人、委托、离职回收、自审批限制、最终锁定和导出再认证。
12. 冻结 v1 计算规范：只正式计算人民币；外币材料保留原币种并阻断正式计算。明确中国标准时间、日期首尾包含、同日事件排序、多笔债务分配和逐期舍入规则。
13. 建立程序期限规则范围：送达方式、管辖地域、起算/届满、法定节假日、顺延、法院特别通知和不确定日期的人工确认门。
14. 建立法律适用锚点表，将合同成立、出借、付款、违约、起诉、受理和裁判等事件日期映射到候选规则与过渡条款。
15. 建立法源许可登记、外部模型/OCR 调用授权模板、恢复目标和试点 cohort/基线定义。
16. 冻结 Phase 1 可用性测试协议：目标角色、统一合成案件、任务脚本、时间上限、独立完成定义、观察表、同意与隐私说明、失败和重测规则；六项高风险任务固定为 `DESIGN_BRIEF.md` 原型任务 4—9，并采用其中定义的逐项通过证据。
17. 冻结分域状态词典和 UI 映射，分别定义案件流程、运行、审批、派生件有效性、提交包和外部请求状态，不用一套“全局状态”混用。
18. 建立开发资产恢复基线：配置私有远程 Git、独立加密版本备份、备份清单与哈希校验，并在隔离目录完成一次可复现恢复；不得把文件同步状态当作备份证据。

### 交付物

- PRD 和范围清单；
- 领域词典；
- 金标准案件数据包；
- 风险登记表；
- 首批 16 个 Skill 规格草案；
- 数据处理与保密模板需求；
- 租户隔离矩阵与角色—动作审批矩阵；
- v1 计算规范、程序期限规则范围和法律适用锚点表；
- 法源许可登记模板、外部调用 preflight 契约和恢复目标；
- 试点 cohort、人工基线、错误分母和停止条件；
- Phase 1 可用性测试协议、统一合成案件和观察记录模板；
- 分域状态词典及 UI 文案/颜色/操作映射；
- 开发资产备份矩阵、备份清单和首次恢复演练记录；
- `docs/PHASE_0_EXECUTION_LEDGER.md` 及田野研究、金标准、计算/法律、权限/外部调用和可用性测试执行契约；
- 本计划的评审记录。

### 阶段出口（试点/真实数据接入门）

- 承办律师确认首版业务边界；
- 3 个金标准案件能够独立复核；
- 所有关键输出都有明确责任人和批准人；
- 数据面隔离、审批证据、计算边界、期限边界、法源许可和外部调用授权均已形成可测试契约；
- Phase 1 的参与者、任务、样本、分母、通过/失败和重测规则能够由另一团队复现；
- 代码与计划已推送至私有远程 Git，独立加密备份可按清单和哈希在隔离目录恢复；
- 未解决问题已进入决策清单，而不是隐藏在提示词中。

## 7. Phase 1：UX、交互与视觉方向

### 目标

先确定律师如何完成工作，再决定页面如何呈现；避免做成聊天机器人或通用后台模板。完成三种方向和暂定选择后，立即与 Phase 2 合成数据开发并行；真实律师测试在试点前完成。

### 工作

1. 制作完整用户旅程和服务蓝图。
2. 建立信息架构、导航、页面层级和核心对象关系。
3. 为以下流程制作低保真线框：
   - 创建案件与上传材料；
   - 材料盘点、重复页和相关页确认；
   - 身份映射、诉请和争点确认；
   - 交易台账和付款性质审批；
   - 法律路径和计算参数审批；
   - 文书审改、错误修复和提交版锁定。
4. 使用同一案件首页和证据工作台生成三种高保真视觉方向。
5. 由用户选择一个方向，并建立颜色、排版、间距、图标、表格、状态和交互 Token。
6. 制作核心流程可点击原型，覆盖正常、空、加载、错误、冲突、失效和无权限状态。
7. 按 Phase 0 协议招募 5 名目标律师，其中至少 3 名承办律师、其余为协办/复核律师；所有人使用同一套脱敏合成案件和同一 9 项任务脚本，不提供主持人口头提示。
8. 每人总时限 45 分钟；通过定义为 6 项高风险任务全部正确、9 项任务至少完成 8 项、无越权/误批/误导出，且全过程能从观察记录复核。
9. 记录逐任务完成率、耗时、错误、误解点、信任问题和主持人干预；少于 4 人通过即 Phase 1 失败，修订后用至少 3 名未参加首测的目标律师重测，仍失败则缩小流程或重做交互决策。
10. 完成 WCAG 2.2 AA 和键盘操作检查计划。

### 交付物

- 用户旅程和信息架构；
- 三种视觉方向及选择记录；
- 高保真原型；
- 设计系统基础；
- 可用性测试报告；
- UI 状态矩阵；
- 前端验收基准图。

### 阶段出口（试点/真实数据接入门）

- 用户明确选定视觉方向；
- 5 名律师中至少 4 名满足可复现协议的通过定义；若发生重测，至少 3 名新参与者全部通过；
- “候选、确认、锁定、失效、阻断”不会被混淆；
- 用户能在 10 秒内说出案件下一项需要决定的事项；
- 所有高风险动作都有明确影响预览和审批。

## 8. Phase 2：技术基础

### 目标

以合成案件建立后续功能共享的安全底座，防止每个模块自行处理权限、版本和审计；本阶段可在 Phase 0/1 真实验证尚未完成时开始，但不得接入真实案件或宣称可生产使用。

### 工作

1. 建立单仓库结构、开发环境、测试环境和持续集成。
2. 实现律所、团队、案件、用户和角色模型。
3. 实现登录、MFA、会话、案件级权限和服务端强制鉴权。
4. 建立 PostgreSQL 数据模型、迁移机制和行级隔离防线。
5. 建立不可变对象存储、文件哈希、派生关系和保留策略。
6. 实现代码状态机、审批记录、依赖失效和幂等任务。
7. 建立 Tool Gateway、Skill Registry 和结构化输入输出协议。
8. 建立模型网关：模型白名单、数据最小化、成本上限、超时和不可盲重试策略。
9. 建立追加式审计日志、OpenTelemetry 追踪和基础告警。
10. 建立备份、恢复和环境配置基线。
11. 每个异步 `Run` 固化输入快照和版本指纹，完成时仅可通过 CAS 原子应用；幂等键按租户、案件、命令类型和调用者定域。
12. 为外部模型、OCR 和 MCP 请求建立持久请求账本与逐次 preflight：显示字段范围、供应商、处理地域、保留期、次数/成本上限并取得明确确认；未知提交状态禁止自动重试。
13. 将批准记录绑定到 actor、权限快照、案件版本、对象哈希和最终文本哈希；提交版锁定与导出必须重新认证并执行配置化职责分离。

### 交付物

- 可登录的空工作台；
- 租户、角色、案件权限；
- 状态机演示案件；
- 文件上传、哈希和派生件基础；
- Agent/Skill/Tool 调用审计；
- CI 质量门和开发手册。

### 阶段出口

- 跨律所和跨案件访问测试全部拒绝；
- 相同幂等键不会创建两份结果；
- 上游对象变更能够将下游结果标记为失效；
- 审计日志能够回答“谁在何时基于什么版本做了什么”。
- 任一旧版本任务结果均不能覆盖新版本，外部未知提交能够从请求账本恢复；
- 数据库、对象存储、索引、队列、缓存、审计和导出路径的跨租户负向测试全部拒绝；
- 审计不可写时锁定、导出、批准和外部调用均 fail closed。

## 9. Phase 3：材料接收与证据工作台

### 目标

把混乱文件夹转化为可审核的页级证据目录，同时保留完整原件。

### 工作

1. 支持 PDF、DOCX、图片、XLSX 和 ZIP 安全上传。
2. 进行恶意文件、宏、路径穿越、压缩炸弹、加密和损坏文件检查。
3. 按文件生成 SHA-256，并建立原件登记。
4. 拆分 PDF 页面，生成稳定页 ID、预览图、OCR 文本、表格和坐标。
5. 组合精确哈希、感知哈希、OCR 和版式特征识别页级重复。
6. 区分精确重复、疑似重复和相似但不同，不自动删除原件。
7. 识别与目标主体、账号和关键词相关的候选页面。
8. 在三栏证据工作台中展示页图、抽取内容和来源关系。
9. 支持律师确认保留/排除页面和红框坐标。
10. 生成只包含相关页的派生 PDF，并保留页级来源映射。
11. 实现材料缺口清单和解析异常队列。
12. 为每个原件生成全量页面处置清单，逐页记录保留、排除或待审及操作者；自动模型只排序候选，不得静默排除页面。

### 阶段出口

- 金标准案件相关页自动召回率达到 99% 目标；该指标只评价排序能力，不替代全量页面人工处置；
- 生成派生 PDF 前未审页面为零，关键主体、借据、法院文书和交易流水执行双人复核或盲审抽检；
- 精确重复页不会重复进入派生 PDF；
- 相似但不同页面不会被静默删除；
- 红框位置在不同分辨率渲染后保持准确；
- 原件、页码、坐标、派生件和哈希可完整追溯。

## 10. Phase 4：主体、诉请、程序事项与交易台账

### 目标

建立经律师确认的案件事实图，不让 OCR 文本直接成为诉讼事实。

### 工作

1. 提取姓名、微信昵称、账号、银行卡、电话和当事人角色候选。
2. 建立主体、别名、账号和证据位置映射，低置信度必须确认。
3. 从法院材料提取案号、法院、当事人、诉请、事实理由和送达候选；程序期限由版本化确定性期限引擎依据经确认的送达方式、地域、时区、节假日、起算/届满与顺延规则计算。
4. 诉请必须保留原文、金额、期间、币种、依据和来源坐标。
5. 建立承认、部分承认、争议、不属于本案审理范围等响应状态。
6. 提取借款、付款、利息支付、催收和关键沟通的候选时间线。
7. 建立统一交易台账并对微信、银行、聊天和借据进行来源关联。
8. 处理重复流水、退款、手续费、同日多笔、不明付款和缺失交易号；外币保留原币种、金额和来源，但 v1 阻断正式利息计算并进入律师人工流程。
9. 由律师确认每笔款项性质及多笔债务的分配关系。
10. 建立事实、诉请、争点、证据和举证责任矩阵。

### 阶段出口

- 关键人物、金额、日期和币种经人工确认后达到 100% 一致；
- 交易总额能与原始账单独立对账；
- 未确定付款性质不会进入正式计算；
- 原告未主张事项不会被系统自动变成需要法院处理的请求；
- 送达日期和程序期限均显示规则版本、来源和律师确认状态；未确认前只显示“期限候选”，不得显示可依赖倒计时。

## 11. Phase 5：法律知识库与法律研究

### 目标

让法律 Agent 只能在版本化官方法源和律师批准的内部知识中工作。

### 工作

1. 建立法源白名单和效力层级。
2. 通过官方允许的 API、下载、人工导入或已授权数据库接入国家法律法规数据库、最高人民法院、公报、人民法院案例库及特定官方数据源；不得绕过访问控制、服务条款或许可进行抓取。
3. 保存原文快照、条款结构、发布/施行/失效日期、修改链、地域和内容哈希。
4. 建立关键词、条号和向量混合检索，但在召回前先按事实日期、案由、地域和效力过滤。
5. 建立民间借贷规则卡：规则、适用条件、例外、过渡条款、所需事实和官方来源。
6. 将指导性案例、参考案例、典型案例和普通裁判文书分层。
7. 实现引用校验：官方 URL 可访问、原文匹配、版本有效、条号正确。
8. 新法或修订先进入待审核区，法律编辑批准后才发布。
9. 规则更新时标记受影响 Skill、案件和草稿为待复核。
10. 建立类型化 `ApplicabilityRule`，明确触发事件类型/日期、地域、优先级、条件、例外、过渡规则和冲突解决；案件形成经律师批准、不可变的 `CaseLegalBundle`。
11. 建立逐来源许可登记，记录访问方式、服务条款/API 依据、存储/再分发/商用范围、地域、期限和撤销处置；缺少有效许可证据时禁止采集、快照和入库。

### 阶段出口

- 正式法律结论 100% 包含官方来源和有效期间；
- 已失效版本不会因语义相似被误作为现行规则；
- 案例不会被标记为法律法规；
- 无法验证的引用不能进入正式文书；
- 法律编辑可以查看变更差异和受影响范围。
- 每个正式命题都能说明所用事件日期、适用锚点、冲突处理和案件 `CaseLegalBundle` 版本；
- 每个法源快照都有当前有效许可记录。

## 12. Phase 6：确定性利息与冲抵计算

### 目标

把法律判断与数学执行分离，并使每一步能够被律师和独立程序复算。

### 工作

1. 定义计算输入 Schema：人民币本金事件、付款事件、利率规则、起止日、时区、日期包含性、日计数、同日事件顺序、多债务分配、逐期舍入和冲抵顺序。
2. 所有金额使用 Decimal，不使用浮点数。
3. 支持分期间规则、按日计算、同日事件顺序、闰年、分币舍入和逐笔本金递减。
4. 支持约定利息、法定上限、已付利息、逾期利息等分类，但必须由律师批准规则。
5. 支持内部多个测算情景；正式提交只允许选定一个情景。
6. 每笔计算生成公式、参数、天数、本金余额、利息、抵充和证据来源。
7. 建立双实现或独立校验器进行复算。
8. 使用金标准样例、边界样例和性质不明付款进行属性测试。
9. 参数改变后自动使所有相关计算和文书失效。
10. 非人民币、事件时间精度冲突或分配关系未批准时阻断正式计算，不静默选取汇率、日期或债务。

### 阶段出口

- 相同输入和规则版本产生完全相同输出；
- 计算与独立实现结果一致；
- 本金余额不会无理由增加或成为负数；
- 每笔冲抵都能解释依据和顺序；
- 律师能够从总额下钻到任一期间和原始付款证据。

## 13. Phase 7：文书编译与一致性审查

### 目标

文书只编译已经批准的案件对象，不允许 Agent 重新想象事实或数字。

### 工作

1. 建立答辩状、证据目录、核算说明、授权材料等版本化模板。
2. 文书 Agent 只接收已批准事实、争点、法律规则和正式计算情景。
3. 每个关键段落保存事实 ID、证据 ID、规则 ID 和计算 ID。
4. 支持律师逐段接受、修改、评论和退回，不以聊天方式替代文书编辑。
5. 增加对抗审查 Agent，检查遗漏、超出诉请、无来源结论、角色混乱和法律引用风险。
6. 审查 Agent 只提出问题，不静默修改律师立场。
7. 建立跨文书一致性规则：姓名、身份、案号、法院、金额、日期、币种、代理权限、证据编号和总额。
8. 实现 DOCX、PDF、XLSX 渲染及分页、字体、表格和元数据检查。
9. 原始证据 PDF 不添加无关说明，提交材料采用简洁法院风格。
10. 律师编辑保存为不可变 `DocumentVersion` 差异；对新增或修改文字重新执行事实/证据来源、金额、币种、法条版本和跨文书一致性校验。
11. 任何编辑使原段落批准失效；只有重新批准最终渲染文本哈希后，文书才可进入提交包。

### 阶段出口

- 正式事实句和金额均可回指来源；
- 无未经批准事实进入文书；
- 跨文书阻断级矛盾为零；
- DOCX、PDF、Excel 在目标办公软件中真实打开并视觉检查；
- 律师可以清楚区分 AI 建议、自己修改和锁定文字。
- 最终文书哈希、渲染件哈希与批准证据完全绑定，编辑后旧批准不能复用。

## 14. Phase 8：提交包、封闭试点与商用门禁

### 目标

证明产品能够在真实期限和真实案件材料下安全交付，而不只是通过内部样例。

### 工作

1. 建立提交中心和完整发布清单。
2. 检查所有审批、阻断项、页码、证据编号、文件名、币种和版本。
3. 生成发布清单 Manifest：文件、页数、哈希、来源版本、生成时间和审批人。
4. 执行“律师锁定提交版”，以案件行上的唯一 `current_submission_bundle_id` 和原子事务产生每案唯一当前提交包；生命周期、有效性和 current 指针分离，包内文件和 Manifest 均不可变。
5. 生成 ZIP，但不自动发送或提交。
6. 支持锁定前后及导出后的撤销或失效：必须说明原因，原子清除 current 指针，旧包有效性进入不可逆 `REVOKED` 或 `STALE` 且永远禁止再次导出，新版本重新终审和锁定。
7. 使用 3 个历史案件盲测，比较律师原始成果。
8. 按预先登记的首案由、页数和交易量分层 cohort 完成至少 10 个真实封闭试点案件；30 案作为后续商业验证目标，不压缩进固定短周期。
9. 用同类案件历史工时或同案人工基线记录分母，分别统计事实、引用、计算行和提交包错误率，以及节省时间、人工修改比例、模型成本和复购意向。
10. 完成渗透测试、备份恢复演练、故障演练和数据删除演练。

### 商用 Beta 安全门槛

- 至少 10 个真实案件完成；
- 最终关键事实、法律引用和计算没有已知重大错误；
- 律师平均复核时间比原流程降低至少 50%；
- 单案内部人工逐步降至 2 小时以内；
- 未发生跨租户泄露、未经批准导出或原件覆盖；
- 备份恢复和审计回放通过；
- 律师签署产品适用边界和交付流程确认。

上述安全门槛不能被节时、复购或模型成功率替代。`至少 3 家律所/团队产生付费复用或明确续用`、`累计 30 案`属于独立商业验证指标；未达到时可以维持封闭 Pilot，不得降低安全门槛对外商用。

## 15. UI实现优先级

### P0 页面

1. 登录与律所空间；
2. 案件列表；
3. 新建案件与材料上传；
4. 案件控制台；
5. 材料与证据工作台；
6. 主体、诉请和争点；
7. 交易台账；
8. 法律依据与规则审批；
9. 计算工作台；
10. 文书工作室；
11. 提交中心；
12. 审计记录。

### P1 页面

- 律所模板管理；
- 法律规则编辑后台；
- 团队质量仪表板；
- 案件归档、保留和删除申请；
- 模型和成本管理。

### P2 页面

- 多案由流程包市场；
- 律所 DMS/OA 集成；
- MCP 管理；
- 本地或私有化部署控制台；
- 桌面端私有化运维工具。

v1 完全不提供移动端查看、审批、锁定或导出；若未来纳入，必须单独立项并补充设备绑定、阶梯认证、离线禁用和专项安全验收。

## 16. 技术栈基线

最终依赖版本在实际初始化时锁定并生成 SBOM，计划阶段不把短期版本号写死。

| 层 | 基线选择 | 原因 |
|---|---|---|
| Web | React + TypeScript + Next.js | 适合复杂交互、服务端能力和可访问性建设 |
| UI基础 | Headless 可访问组件 + 自建 Token | 避免默认模板视觉并保持可访问性 |
| API | Python + FastAPI + Pydantic | 适合结构化 Schema、AI/文档生态和 OpenAPI |
| 数据库 | PostgreSQL | 事务、JSONB、全文、行级安全和成熟运维 |
| 向量检索 | PostgreSQL 扩展或独立检索层 | 第一版保持简单，必要时再拆分 |
| 文件 | S3 兼容对象存储 | 原件、派生件、版本和生命周期管理 |
| 后台任务 | PostgreSQL 状态机 + 持久任务队列 | MVP 易审计；复杂集成后再评估 Temporal |
| 文档处理 | 沙箱化 Python Worker | OCR、PDF、DOCX、XLSX 生态成熟 |
| Agent | 模型无关 Gateway + 结构化输出 | 可更换供应商、限制数据和成本 |
| 观测 | OpenTelemetry + 指标/日志平台 | 统一追踪请求、Agent、Tool 和 Worker |
| 开发部署 | 容器化本地开发；中国区受控环境 | 便于复现、试点和后续私有化 |

## 17. 质量策略

### 每次提交必须通过

- 格式、类型和静态检查；
- 单元测试；
- API/Schema 合同测试；
- 数据库迁移测试；
- 核心安全规则；
- 不包含秘密、真实客户数据和未授权样本。

### 每个阶段必须通过

- 金标准案件回归；
- 关键用户旅程端到端测试；
- 权限和越权测试；
- 文档真实渲染检查；
- 阶段验收会议；
- 对照本计划确认已完成、未完成和下一步。

### 发布前必须通过

- 计算属性测试和独立复算；
- 100% 正式法律引用校验；
- 跨文书一致性检查；
- 视觉回归和 WCAG 2.2 AA；
- 渗透测试、恢复演练和数据删除演练；
- 律师终审流程实测。

## 18. 项目管理规则

1. 每个阶段开始时确定目标、交付物、风险和验收人。
2. 每个阶段结束时复核已完成工作，再对照本计划决定下一步。
3. 不将“已配置、已连接、模型返回成功”描述为用户价值已经完成。
4. 不陷入无休止验证：验证必须对应风险、验收门或已发现问题。
5. 所有重大架构和产品决策写入 `decisions/`。
6. 所有真实客户材料不得进入 Git、长期 Agent Memory、公开 issue 或演示录像。
7. 任何付费模型或大批量文档调用必须有预算上限、预估和明确授权。
8. 每个阶段只维护一个正式基线，不建立“最终版2、最新修订版”等文件命名。
9. 文件同步不等于备份；每个阶段出口必须记录权威提交、备份时间、清单哈希和恢复验证结果，缺一项不得声称可恢复。

## 19. 关键风险与处理

| 风险 | 早期信号 | 应对 |
|---|---|---|
| 律师不愿上传真实材料 | 访谈认可但无人试用 | 先做保密协议、境内部署和小样本付费服务 |
| 法律结论可靠性不足 | 律师频繁推倒重做 | 收窄规则、强化官方法源和审批，不增加自由 Agent |
| OCR/证据漏页 | 金标准召回不稳定 | 低置信人工队列、页级双检、不得自动排除 |
| 计算争议 | 同案不同参数产生差异 | 参数审批、版本化规则、独立复算和逐行解释 |
| 流程过重 | 律师绕过系统回到 WPS | 首页只显示下一决定，减少重复确认和表单 |
| 视觉像通用后台 | 用户认为只是套壳 | 三方向视觉探索、真实数据密度原型、律师测试 |
| 单案人工成本过高 | 第 10 案仍需 6 小时 | 限定范围、优化高频异常；必要时收缩为证据/计算模块 |
| 数据泄露 | 过宽工具权限或模型日志 | 最小权限、无训练条款、脱敏、审计和网络出口限制 |
| 法律更新污染旧案 | 新版本覆盖旧版本 | 时态库不可覆盖，规则变更标记受影响案件待复核 |
| 本地/iCloud 迁移导致项目残缺 | 工作目录突然只剩部分文件、Git 对象缺失或同步状态与实体文件不一致 | 停止写入并保全残缺副本；从经哈希验证的私有 Git/加密备份恢复；完成恢复演练后再继续 |

## 20. 当前执行顺序

1. 让用户确认本计划的产品边界和项目名称。
2. 配置私有远程 Git 和独立加密版本备份，完成首次隔离恢复并保存清单、哈希和恢复记录。
3. 以合成数据完成三种视觉方向；由产品负责人或其明确委托的项目负责人作出暂定选择。
4. 立即初始化应用代码和技术基础，并按 Phase 2—7 的依赖关系实现完整内部合成 Alpha。
5. 同步整理 3—5 个脱敏金标准案件，并完成真实律师访谈、流程观察和高保真原型任务测试；真实个人信息不得进入项目仓库。
6. 只有通过 Phase 0/1 出口后，才接入真实案件、开启封闭试点或声称产品已由律师接受。

代码开发开始后，每完成一个阶段，都必须更新本文件顶部的当前阶段和验收状态。

## 21. 2026-08-12 Web 商用切片执行记录

本轮已完成并复核：

- 浏览器 Web 主入口已隔离桌面入口；登录、案件选择、PDF 接收、逐页证据审阅、红框、应诉候选审批和服务端材料包锁定均走同源 Web API。
- PDF 接收异常会进入“待核验”，不会诱导律师重复上传；分页查询已允许受限查询参数。
- 新增 ZIP 材料包安全接收垂直：服务器私有暂存、大小/条目/解压总量/压缩比上限、路径穿越、符号链接、加密 ZIP、重复文件名、非 PDF 条目和真实解压 CRC/哈希校验均 fail-closed。
- 新增 `0027_web_material_archive_uploads.sql` 及 RLS 状态机。ZIP 入库后只返回 `STORED_PENDING_PROCESSING`，原始 ZIP 私有对象和逐文件清单留在服务端；尚未把 ZIP 冒充为已完成的 PDF 证据，也尚未开放重复上传或法院导出。
- Web UI 已支持 PDF、ZIP、多文件、拖放和 Chrome/Edge 文件夹增强；ZIP 状态明确显示“已安全接收，待后台逐文件处理”。
- 新增 PDF/ZIP 接收状态查询接口。律师可用服务器接收编号刷新状态；完成回执、处理中、结果待核验和过期均有明确状态，接口始终禁止客户端以“重传”作为未知结果的默认动作。
- 新增 Web 文书候选垂直：服务端只从已确认事实、诉请和收付款快照生成固定类型的案件核对摘要（Word/PDF）或收付款核对表（Excel/PDF）；候选哈希、私有对象、服务端 LibreOffice 隔离渲染和律师审批接口已接通。没有配置 LibreOffice 的部署保持该功能关闭，不伪造生成成功。

本轮聚焦与全量复核结果：后端 645 项测试通过（4 项既有跳过），ZIP/Web 路由、文书候选与运行时聚焦测试通过，前端 ESLint/TypeScript/生产构建通过，`git diff --check` 通过。

仍未完成的真实商用门：ZIP 子文件持久 Worker/断点恢复、真实 OIDC/数据库/对象存储/ClamAV/Poppler/LibreOffice 连通、已批准 Office 候选到法院 PDF 工作产品的绑定、跨浏览器端到端和匿名化律师案件验收。不能因为本轮代码和测试通过就宣称已商用放行。
