# 开发资产恢复基线

状态：`Phase 0 执行中；远程 Git 恢复已验证，受保护主分支与独立加密非 Git 备份待配置`

日期：`2026-08-09`

## 1. 范围与边界

本记录只覆盖本项目的代码、计划和脱敏文档。项目内不包含真实客户材料、身份证明、电话、账户、法院文件、诉讼策略、密钥或模型凭证；这些内容不得推送到 GitHub 或作为本次恢复演练的输入。

## 2. 权威位置

- 权威工作区：`/Users/cft/Projects/律所案件AI工作台`
- 版本权威：私有 Git 远端 `origin/main`
- `Documents` 下的同名目录：仅作 iCloud/File Provider 迁移后的保全副本；不得继续在其中开发，也不得以其同步状态证明备份成功。

## 3. 已验证的恢复证据

| 项目 | 结果 | 证据 |
|---|---|---|
| 迁移完整性 | 通过 | 原目录与权威目录均为 14 个非 Git 文件 |
| 计划完整性 | 通过 | `PLAN.md` SHA-256：`c14612c3c3ca4d618147219a3742f9e6f9984343a7b04594f754fc885b99d4bf` |
| 私有远程提交 | 通过 | `cdc92e5 docs: establish approved product planning baseline` 已推送到私有 `origin/main` |
| 隔离恢复 | 通过 | 从远端独立浅克隆，恢复目录包含 14 个非 Git 文件 |
| Git 对象完整性 | 通过 | 恢复目录执行 `git fsck --full` 无错误 |
| 受保护主分支 | 未通过 | 当前 GitHub 私有仓库套餐拒绝启用分支保护；公开仓库不符合保密边界，不能用公开化规避 |

## 4. 未完成项与门禁

独立加密、版本化、与本机故障域分离的非 Git 资产备份目标尚未配置；受保护主分支也尚未可用。当前项目没有非 Git 资产，且仍处于不写生产代码的 Phase 0，因此不阻断计划文档与脱敏代码基线的恢复；但在以下任一操作前必须完成两项配置并再次演练：

1. 引入不可由 Git 重建的设计源文件、二进制资产或测试资料；
2. 接收任何真实案件材料；
3. 进入预发布、试点或生产环境。

受保护主分支的可接受实现为：支持私有仓规则保护的 GitHub 付费方案，或由组织控制、可审计并能禁止强推/删除和未审合并的自建或企业 Git 服务。不得将仓库公开化作为替代方案。

完成门槛：备份清单和哈希可核对；指定恢复点可在隔离目录恢复；恢复文件可打开；恢复记录注明时间、操作者、提交/快照和异常情况。仅显示“同步完成”不视为备份完成。

## 5. 运行规则

- 所有后续项目写入只在权威工作区进行。
- 每次阶段出口记录远程提交、备份时间、清单哈希和恢复验证结果。
- 若工作区、远端或备份不一致，立即停止改写，保全现场，从经哈希验证的来源恢复。
- 清理 `Documents` 保全副本只能在独立加密备份首次恢复通过后进行，并应采用可恢复的方式。
