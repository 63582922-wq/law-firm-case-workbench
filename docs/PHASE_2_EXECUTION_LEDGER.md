# Phase 2 合成 Alpha 执行台账

状态：`进行中；仅合成数据`

本台账记录开发状态，不替代 Phase 0/1 的律师验证、真实金标准案件、试点或生产放行证据。

| 工作包 | 当前状态 | 已有证据 | 未完成项 |
|---|---|---|---|
| 案件状态机核心 | 已完成第一段 | `backend/case_kernel/`；8 项自动化检查通过 | PostgreSQL 持久化、事务 Outbox、可配置职责分离和完整审批类型 |
| 审计、乐观并发与幂等 | 已完成内存语义 | 创建/推进/审批/锁定/失效均带版本与案件范围幂等；审计事件可查询 | 持久审计、不可篡改存储、请求追踪和服务端身份认证 |
| 唯一提交版与上游失效 | 已完成内存语义及提交编译持久化第一段 | 锁定要求当前文本哈希批准；上游变化使当前包 `STALE` 并清空指针；`0007_submission_compilation.sql` 和 `submission_postgres.py` 将当前证据 Manifest、法律规则包、正式计算、最终文本审批、法院文件名/顺序及 QA 哈希绑定到唯一当前包 | 完整文书生成依赖图、专用 PostgreSQL 实迁移、持久下载审计和真实再认证 |
| 法院提交包编译与本机交付 | 已完成合成正式边界第一段 | `submission_bundle_compiler.py`、`submission_coordinator.py`、`submission_access.py`：仅接收已批准法院 PDF/CNY，拒绝歧义版本名和不安全路径，法院 ZIP 与内部清单分离，双重结构/哈希核验后分别加密登记；主办/复核律师使用 OS 绑定会话、最长 30 秒一次性 loopback 许可下载；`/bundle` 不在合成模式伪造下载或提交成功 | 真实文书生成、Keychain/OS 身份装配、下载事件持久审计、真实大包兼容性、专用实库、法院平台和执业律师验收；不自动上传法院 |
| 合成证据页处理 | 已通过并关闭 | `labs/evidence_pipeline/`；简体中文夹具 4/4 检查及视觉复核 | 并入正式 Worker 后补充 OCR、版面、性能和金标准回归 |
| 证据 Manifest、页级处置与红框谱系 | 已完成正式派生链、原件单页预览、桌面选择桥、前端写审批及大案页级分页 | `case_kernel/evidence_manifest.py`、`evidence_manifest_postgres.py`、`evidence_derivative_worker.py`、`evidence_derivative_coordinator.py`、`original_page_access.py`：原件/来源页不可变、逐页处置、重复规范页、红框坐标、零未审页锁定、最小 PDF 派生、主动内容剥离、144 DPI 渲染核验、可恢复登记/验证和上游失效；首屏读取全案就绪摘要与前 50 页，后续页使用案件版本绑定的键集游标；服务端锁定事务拒绝未决页、新候选处置、未决重复组及就绪哈希变化，不依赖浏览器载入全部页面；原件绝对路径仅在进程内短时 Registry，浏览器只经一次性 loopback 许可取得哈希核验的当前页 PNG；Tauri 2 壳用专用 Rust 命令返回原生对话框选中的目录，前端无通用文件系统/对话框权限，根目录和用户主目录阻断；`0004_evidence_derivative_runs.sql` 保存版本/Manifest 哈希绑定的排队任务、30—300 秒租约、最多三次恢复和双派生件成功门；本机对象库以 AES-256-GCM 加密并按内容哈希寻址；`ADR-0006` 至 `ADR-0009`、`ADR-0019` | Keychain 初始化/轮换/恢复 UI、真实律师登记身份与持久 API 装配、桌面 Worker、旋转/裁切页显式变换、恶意 PDF 隔离、真实大文件性能、跨分辨率回归和律师验收；未在专用 PostgreSQL 实库执行迁移 |
| 本地案卷文件夹与材料接收 | 已完成版本化只读盘点、范围批准及 PDF 接收第一段 | `local_case_folder.py`、`local_access_grants.py`、`evidence_intake_worker.py`、`evidence_intake_coordinator.py`、`0010/0011`、持久 API 与中文证据工作台：绝对路径/grant 不落库；差异候选经主办律师批准；排队前用当前 grant 重验 Manifest；逐文件短租约、本机扫描器谱系、哈希和活动 PDF 检查通过后登记真实页数；其他格式进入转换/人工处理；新范围使旧任务/证据/提交链失效；`ADR-0020/0021` | 生产扫描器与病毒库运营、sidecar 常驻领取及重新授权恢复、DOCX/XLSX/图片/邮件/ZIP 隔离转换、专用实库和真实案卷验收 |
| 公开研究、法源与官方案例注册 | 已完成持久队列、一次执行 Worker、正式登记代码链及真实案例元数据目录第一段 | `official_source_capture.py` 在律师授权后执行 credential-free TLS、公网对端、官方域名、类型/大小/签名门禁并把原字节直接加密；确定性解析器区分现行/历史司法解释、民法典与 LPR；`0008`、抓取 Store 与 coordinator 新增 15 分钟授权、一次尝试、短租约、稳定失败码、内容/解析哈希和 append-only 律师复核；`0009` 与法律 Store 要求独立许可依据/哈希；`public_case_catalog.py` 与 `knowledge/official_cases/registry.json` 登记人民法院案例库、最高法官网/国际商事法庭和裁判文书网的来源策略及 6 个已核验真实案例候选，禁止越域、未知字段、批量抓取、全文仓库、商业训练和未经律师复核的正式使用；`research/OFFICIAL_CASE_DATA_CATALOG.md` 分离事实、推测、建议与版权边界 | 案例库正式许可/API、律师主动下载导入 UI、Keychain/桌面常驻 Worker、2015 公报全文成功捕获、历史 LPR 公告发现、专用实库和真实法律编辑验收；元数据/发现/冒烟不得替代正式来源快照 |
| 确定性利息与冲抵计算 | 已完成正式持久化第二段 | `calculation_engine.py` 与 `formal_calculation_postgres.py`：Decimal、半开期间、连续规则段、同日顺序、CNY 阻断、逐期分币舍入、明确冲抵和独立日步进复算；正式 API 只接收已批准规则包标识/哈希，服务端读取规则段；`0020_official_lpr_observations.sql` 将已复核中国货币网抓取从认证原件重解析为不可变观察记录，LPR 规则只按官方定位读取基准值，浏览器不能提交利率；中文计算页缺少正式结果时不回退合成金额 | 属性/金标准扩展、专用 PostgreSQL 实库迁移、真实复杂付款路径和执业律师验收 |
| 时态法源规则卡与案件规则包 | 已完成持久化审批链第四段 | `legal_rules.py` 保留合成领域契约；法律 Store/API 保存官方快照、规则版本、法律事件、关键事实绑定、连续规则包、版本/哈希审计与上游失效；规则所需事实键必须绑定同案 `CONFIRMED` 事实；`/legal` 新增抓取、律师复核、许可核验与正式快照登记。中国货币网 LPR 只能通过复核捕获后服务端重解析登记，解析哈希或精确定位不一致即阻断；中文页可建立 LPR 倍数规则，但只能选已核验双快照、已批准事实键、触发事件、期间、精确定位与倍数，绝无基准利率字段 | LPR 历史完整性、法律事件/事实绑定/连续规则包写审批 UI、冲突候选比较、实库迁移和真实法律编辑验收；合成/发现清单不构成法律结论 |
| 事实、诉请与争点台账 | 已完成合成领域契约及数据库结构第一段 | `case_kernel/fact_claim_ledger.py`：候选与律师确认状态分离；每项均绑定原始证据哈希/页码；确认事实才能支撑回应与争点；上游变化自动撤销回应、使争点失效；`migrations/0002_case_ledgers.sql` 将事实、诉请、回应、争点及其同案关联正规化并启用强制 RLS | 持久化命令适配器、主体/别名/账号、程序事项与期限、时间线、规则包/计算/文书完整失效图和真实律师验收；未在专用 PostgreSQL 实库执行迁移 |
| 交易台账、重复对账与付款性质 | 已完成合成领域契约及数据库结构第一段 | `case_kernel/transaction_ledger.py`：流水候选、律师确认、分类、分配、同日顺序、重复组和计算快照；`migrations/0002_case_ledgers.sql` 保存原交易、正规化债务分配、重复组与成员，状态和批准信息受约束且全部强制 RLS | 持久化命令适配器、主体/别名绑定、真实流水解析和对账汇总、完整失效图、金标准和真实律师验收；未在专用 PostgreSQL 实库执行迁移 |
| 主体、别名、账号与代理关系 | 已完成合成领域契约第一段 | `case_kernel/party_identity_ledger.py`：主体、微信昵称/账号、银行卡/电话等标识与代理关系均须有原件回链；相同标识不能静默指向两个确认主体；上游主体失效自动使标识和代理关系失效；4 项合成检查通过 | 加密持久化、最小展示、真实材料解析、主体合并工作流、案件版本失效图、API/UI 和真实律师验收 |
| 法院材料、送达与程序期限 | 已完成合成领域契约第一段 | `case_kernel/procedure_deadline_ledger.py`：法院/案号、送达、规则、节假日日历和期限候选分层；期限需已确认精确送达日、律师批准的规则与官方快照、日历、时区和二次律师确认；上游变化自动失效；4 项合成检查通过 | 真实法院/节假日规则源接入、地域/法院特别规则、提醒服务、加密持久化/API/UI、真实律师验收 |
| 时间线与事实—争点—证据矩阵 | 已完成合成领域契约第一段 | `case_kernel/timeline_evidence_matrix.py`：借款、付款、送达和沟通事件必须引用当前已确认事实与原始证据；日期精度不可伪造；矩阵项绑定事实快照、证据用途与举证责任；每个确认争点需要已确认矩阵项，事实快照变化即阻断旧项；4 项合成检查通过 | 交易/程序对象直接联结、材料抽取、证明力复核、持久化/API/UI、文书段落映射和真实律师验收 |
| Web 视觉方向 | 已完成暂定选择 | A/B/C 三套原型均已在浏览器查看；选择 A「现代案卷室」，见 `decisions/ADR-0003-visual-direction.md` | 在 Phase 1 加入律师可用性验证，不把暂定选择误称为律师接受 |
| 中文 Web/桌面壳层、证据、法律、利息计算、提交材料与身份安全台 | 已完成第二十九段 | `web/`：证据页新增案卷候选盘点、差异计数、相对路径清单、主办律师范围批准和材料接收任务；页面显示已登记 PDF、等待/处理中、需转换/人工处理、阻断/失败计数，并明确非 PDF 不伪造页数；事实、交易与证据来源页继续按案件版本渐进加载；全部正式请求保持 WebView 短时会话与一次性文件许可 | 真实激活/签发服务、生产扫描器/常驻 Worker、其他格式隔离转换、持久 API/Store 专用数据库装配、规则/提交写审批、Developer ID 签名/公证和律师可用性测试 |
| API 与数据库服务 | 已完成合成 API 第十四段 / PostgreSQL 案件核心、事实交易、证据、法律规则、法源抓取、正式计算与提交编译结构段 | 事实、交易与证据来源页均采用最大 100 条的稳定键集分页、案件版本游标、首批预期版本绑定和字段最小投影；证据摘要计算全案页级阻断计数与就绪哈希，Manifest 锁定事务重新校验全部页、新候选决定、红框和重复组；跨案件/跨列表游标、版本变化及多余私密字段均 fail-closed；身份短时会话、启动重验、WebView grant 与严格 Tauri CORS 保持不变；默认 `NOT_CONFIGURED` 不开放案件或远程登记路由 | 真实激活/续期/撤销/状态服务、生产根运营与 TLS SPKI 轮换、持久 API/Worker/Store 的专用数据库正式装配、其他长列表分页、专用测试数据库实迁移/性能/并发整链、迁移部署及备份恢复；专用 PostgreSQL 实库检查仍按设计跳过 |

## 当前禁止事项

- 不接入真实客户、法院、身份、账户、聊天或诉讼策略数据；
- 不把内存仓库、合成 PDF 实验或自动检查描述为生产服务；
- 不调用外部模型、OCR、MCP 或 SaaS 处理案件数据；
- 不使用真实案件生成、签章、发送或上传法院材料；合成编译器输出不称为真实可提交文件；
- 不因尚未完成真实验证而停止合成 Alpha 的开发。

## 下一目标

下一步把同一版本绑定的稳定分页扩展到诉请、争点、研究结果、审计记录及提交组件，并继续收紧桌面持久 API/Worker/Store 的正式装配入口。生产演练仍需律所确定生产根保管/门限/轮换仪式、签发服务域名、TLS SPKI 及管理员/MFA 流程；专用 PostgreSQL 实迁移、性能和逐案权限整链仍需要获授权的 `_test` 数据库。没有真实签发身份和专用数据库时继续保持案件路由关闭。
